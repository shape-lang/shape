// Simple test to verify Shape is working
print("Shape is running!");

// Test basic arithmetic
let x = 10;
let y = 20;
print("x + y = " + (x + y));

// Test function
function add(a, b) {
    return a + b;
}

print("add(5, 3) = " + add(5, 3));

// Test built-in functions
print("abs(-5) = " + abs(-5));
print("sqrt(16) = " + sqrt(16));

print("Test completed!");