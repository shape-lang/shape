// Test function references without print

// Define a simple function
function my_strategy() {
    return 42;
}

// Test 1: Function reference as value
let fn_ref = my_strategy;

// Test 2: Built-in function reference  
let abs_ref = abs;

// Return the function reference (will show its type)
fn_ref