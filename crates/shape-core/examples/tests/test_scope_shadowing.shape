// Test variable shadowing in blocks

let x = 10
let y = 20

// This works - uses outer x
let result1 = x + y

// Test shadowing (commented out for now as semantic analyzer doesn't support it yet)
// let result2 = {
//     let x = 100;  // Shadow outer x
//     x + y         // Should be 100 + 20 = 120
// }

// After block, x should still be 10
let result3 = x * 2  // Should be 20

find hammer last(5 candles)