// Test mean via stdlib wrapper
import { mean } from std::core::math

let data = series([10.0, 20.0, 30.0, 40.0, 50.0]);

let average = mean(data);

print("Mean of [10,20,30,40,50]:", average);

{
    test: "mean",
    result: average,
    expected: 30.0,
    passed: average == 30.0
}
