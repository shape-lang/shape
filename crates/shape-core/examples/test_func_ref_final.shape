// @skip — uses unimplemented syntax (if-else expression assignment)
// Test function references

// Define a simple function
function my_strategy() {
    let close = 100.0;
    let signal = if (close > 95.0) { 1.0 } else { 0.0 };
    return signal;
}

// Test 1: Function reference as value
let fn_ref = my_strategy;

// Test 2: Function reference from built-in
let len_ref = length;

// Return success indicator
42