// Test __intrinsic_rolling_mean (SMA)
// This is the foundation for Simple Moving Average indicators

// Create test price data
let prices = series([100.0, 102.0, 104.0, 103.0, 105.0, 107.0, 106.0, 108.0, 110.0, 109.0]);

// Calculate 3-period SMA using intrinsic
let sma3 = __intrinsic_rolling_mean(prices, 3);

print("Prices:", prices);
print("3-period SMA:", sma3);

// Test: First 2 values should be NaN, then moving averages
{
    test: "rolling_mean_intrinsic",
    data_points: 10,
    window: 3,
    result: sma3,
    // (100+102+104)/3 = 102, (102+104+103)/3 = 103, etc.
    first_valid_index: 2,
    status: "success"
}
