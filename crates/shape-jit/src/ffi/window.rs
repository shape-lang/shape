// v2-boundary: Window FFI functions deleted — no callers from MirToIR or executor.
// All functions were only registered in ffi_symbols (dead wiring).
// If window functions are needed in v2, implement them against the new typed runtime.
