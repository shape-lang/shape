//! Tests for the `json` stdlib module.
//!
//! The json module provides: json::parse(text), json::stringify(value, pretty?),
//! json::is_valid(text). Imported via `use std::core::json`.

mod parse;
mod stringify;
