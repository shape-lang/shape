//! Tests for the `regex` stdlib module.
//!
//! The regex module provides: regex.is_match, regex.match, regex.match_all,
//! regex.replace, regex.replace_all, regex.split.

mod basic;
mod operations;
