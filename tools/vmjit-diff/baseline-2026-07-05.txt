--------------------------------------------------------------------------------
WF-0B VM-vs-JIT differential harness — BASELINE 2026-07-05
--------------------------------------------------------------------------------
Binary   : worktree release build, shape 0.3.2 @ 1fb805b3 (branch wave0/vmjit-diff-harness)
Corpus   : 467 programs (240 book runnable fences, 224 acceptance, 3 synthetic)
Timeout  : 10s per mode per program
Runner   : tools/vmjit-diff/run-diff.mjs (resumable via reports/progress.jsonl)
Exit     : 0 — zero unexpected non-MATCH

SUMMARY COUNTS
  MATCH    = 458
  DIVERGED =   4   (SYN__jit-double-execution, ACC__annotations__probe4,
                    ACC__functions__finding_s1_unknown_hof_return_kind_confusion,
                    ACC__operators__large)
  VM_FAIL  =   2   (SYN__i64-overflow-wrap, SYN__hashmap-filter-garbage)
  JIT_FAIL =   2   (ACC__objects-arrays__small, ACC__operators__small)
  TIMEOUT  =   1   (ACC__pattern-matching__large — compile-time blowup, both modes)
  known-red hits = 9, known-red now matching = 0, unexpected non-MATCH = 0

AUDIT-CLASS SANITY CHECK (audit-2026-07-04-claimed-vs-real.md §4)
  (a) jit-double-execution   : PRESENT via SYN__jit-double-execution (STEP1/STEP2
      printed twice under jit after runtime signal -1 whole-program re-run).
      Zero book-tier hits: within the 240 runnable-fence subset every candidate
      deopts at COMPILE time (before side effects), after which both modes agree.
  (b) i64-overflow-wrap      : PRESENT via SYN__i64-overflow-wrap (vm checked
      overflow exit 1; jit wraps to -9223372036854775808 exit 0).
  (c) annotation-hooks-dropped: PRESENT via ACC__annotations__probe4 (vm prints
      [math] before/after hook lines; jit drops them).
  (d) hashmap-filter-garbage : PRESENT via SYN__hashmap-filter-garbage (vm errors
      'no method filterIndexed on Ptr(HashMap)'; jit prints a different garbage
      integer each run, exit 0). ACC__collections__{small,large} contain the same
      construct but MATCH: earlier Some(_) patterns force compile-time deopt and
      both modes then fail identically (deterministic agreement).

NEW DIVERGENCE CLASSES DISCOVERED BY THIS BASELINE (recorded, not fixed)
  - hof-return-kind-raw-bits : jit prints raw f64 bit pattern (4618441417868443648)
    where vm prints 6.0 (ACC__functions__finding_s1_...). Deterministic.
  - jit-object-merge-field-add: overlapping-key object merge `a + b` returns wrong
    field values under jit mode (3 acceptance programs; deterministic; stderr shows
    W39 F1 LoadModuleBinding / c4-4B TryUnwrap SURFACE fallbacks).
  - compile-blowup-timeout   : ACC__pattern-matching__large needs ~27s to COMPILE
    (both modes identically; E0100 'cannot push a Tok value into an array whose
    element type is Tok' with a 60s budget) — harness-budget artifact of the
    audit-confirmed parser blowup, not a VM/JIT divergence.

All 9 non-MATCH ids are pinned in tools/vmjit-diff/known-red.json after per-id
hand verification (no bulk import). Full per-program details follow.

--------------------------------------------------------------------------------
FULL REPORT (reports/report.md at baseline)
--------------------------------------------------------------------------------
# VM-vs-JIT differential report

- generated: 2026-07-05T05:14:32.329Z
- binary: `/home/dev/dev/shape-lang/worktrees/wf0b-diff-harness/target/release/shape` (shape 0.3.2)
- corpus: tools/vmjit-diff/corpus (generated 2026-07-05T04:45:03.196Z)
- programs run: 467, timeout 10s/mode
- counts: MATCH=458, DIVERGED=4, VM_FAIL=2, JIT_FAIL=2, TIMEOUT=1
- known-red hits: 9; known-red now matching: 0; **unexpected non-MATCH: 0**

## Non-MATCH programs

### DIVERGED (known-red: annotation-hooks-dropped) — `ACC__annotations__probe4.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/annotations/probe4.shape
- known-red reason: vm prints the @math before/after hook lines then '5 / PROBE4_OK'; jit prints only '5 / PROBE4_OK' — the annotation hooks are silently dropped when the caller JIT-compiles. Classified DIVERGED, both exit 0. Verified per-id in the 2026-07-05 baseline run.
- vm: exit=0 signal=null timedOut=false (317ms)
- jit: exit=0 signal=null timedOut=false (342ms)
- vm stdout:
```
[math] before
[math] after
5
PROBE4_OK

```
- jit stdout:
```
5
PROBE4_OK

```

### DIVERGED (known-red: hof-return-kind-raw-bits) — `ACC__functions__finding_s1_unknown_hof_return_kind_confusion.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/functions/finding_s1_unknown_hof_return_kind_confusion.shape
- known-red reason: vm prints '6.0'; jit prints '4618441417868443648' (a raw f64 bit pattern) — the acceptance-gate S1 finding this probe program was written to capture. Classified DIVERGED, both exit 0, deterministic. Verified per-id in the 2026-07-05 baseline run.
- vm: exit=0 signal=null timedOut=false (315ms)
- jit: exit=0 signal=null timedOut=false (343ms)
- vm stdout:
```
6.0
0

```
- jit stdout:
```
4618441417868443648
0

```

### JIT_FAIL (known-red: jit-object-merge-field-add) — `ACC__objects-arrays__small.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/objects-arrays/small.shape
- known-red reason: vm prints ALL_CHECKS_PASSED (exit 0); jit prints 'CHECK_FAILED: merge.x expected=1 got=3' (exit 1) with a W39 F1 LoadModuleBinding SURFACE fallback on stderr — overlapping-key object merge misbehaves in jit mode. Classified JIT_FAIL, deterministic. Verified per-id in the 2026-07-05 baseline run.
- vm: exit=0 signal=null timedOut=false (365ms)
- jit: exit=1 signal=null timedOut=false (398ms)
- vm stdout:
```
ALL_CHECKS_PASSED

```
- jit stdout:
```
CHECK_FAILED: merge.x expected=1 got=3

```
- jit stderr:
```
[jit-fallback] function main failed JIT compile: Runtime error: JIT compilation failed: W39 F1 module-binding function-body SURFACE (ADR-006 §2.7.14): function 'checkInt' contains LoadModuleBinding at bytecode instruction 2023. Module bindings are not MIR places, so the JIT function-body lowering has no compile-time side table for this storage. Running native top-level code and then interpreting such a function through the trampoline VM would read an unsynchronized module-binding array (observed
… [truncated, 865 bytes total]
```

### DIVERGED (known-red: jit-object-merge-field-add) — `ACC__operators__large.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/operators/large.shape
- known-red reason: Both modes fail, differently: vm errors 'no method add on receiver kind Ptr(TypedObject) (line 736)' with empty stdout (exit 1); jit prints 'CHECK_FAILED: T.merge3_b expected=20 got=30' + 'T.merge3_c expected=30 got=4' (exit 1) after a c4-4B TryUnwrap SURFACE fallback. Classified DIVERGED. Verified per-id in the 2026-07-05 baseline run.
- vm: exit=1 signal=null timedOut=false (3309ms)
- jit: exit=1 signal=null timedOut=false (3520ms)
- vm stderr:
```
Error: Runtime error: no method 'add' on receiver kind Ptr(TypedObject) (line 736)

```
- jit stdout:
```
CHECK_FAILED: T.merge3_b expected=20 got=30
CHECK_FAILED: T.merge3_c expected=30 got=4

```
- jit stderr:
```
[jit-fallback] function main failed JIT compile: Runtime error: c4-4B TryUnwrap (`?` operator) SURFACE (ADR-006 §2.7.14): the program contains an `OpCode::TryUnwrap` (the `?` operator) whose unwrap-or-early-return semantics MIR collapses to a transparent copy at `mir/lowering/expr.rs:2594`. The JIT-emitted code calls the inner expression via the trampoline (`dispatch_call_via_trampoline_vm`), stores the trampoline's heap-Result/Option `u64` into a slot whose parallel-kind tracker records the SUC
… [truncated, 1405 bytes total]
```

### JIT_FAIL (known-red: jit-object-merge-field-add) — `ACC__operators__small.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/operators/small.shape
- known-red reason: vm prints ALL_CHECKS_PASSED (exit 0); jit prints 'CHECK_FAILED: merge_collide_keep' + 'CHECK_FAILED: merge_collide_override' (exit 1) with a c4-4B TryUnwrap SURFACE fallback on stderr — `{x:1,y:2} + {y:20,z:30}` loses merge-override semantics in jit mode. Classified JIT_FAIL, deterministic (re-run twice by hand 2026-07-05).
- vm: exit=0 signal=null timedOut=false (423ms)
- jit: exit=1 signal=null timedOut=false (475ms)
- vm stdout:
```
ALL_CHECKS_PASSED

```
- jit stdout:
```
CHECK_FAILED: merge_collide_keep
CHECK_FAILED: merge_collide_override

```
- jit stderr:
```
[jit-fallback] function main failed JIT compile: Runtime error: c4-4B TryUnwrap (`?` operator) SURFACE (ADR-006 §2.7.14): the program contains an `OpCode::TryUnwrap` (the `?` operator) whose unwrap-or-early-return semantics MIR collapses to a transparent copy at `mir/lowering/expr.rs:2594`. The JIT-emitted code calls the inner expression via the trampoline (`dispatch_call_via_trampoline_vm`), stores the trampoline's heap-Result/Option `u64` into a slot whose parallel-kind tracker records the SUC
… [truncated, 1405 bytes total]
```

### TIMEOUT (known-red: compile-blowup-timeout) — `ACC__pattern-matching__large.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/pattern-matching/large.shape
- known-red reason: Both modes SIGKILL at the 10s harness budget during COMPILE (identical behavior); with a 60s budget the vm run completes in ~27s and exits 1 with the compile error both modes would print ('E0100: cannot push a Tok value into an array whose element type is Tok'). Not a VM/JIT divergence — a known compile-time blowup exceeding the harness budget. Classified TIMEOUT. Verified per-id by a 60s hand run 2026-07-05.
- vm: exit=null signal=SIGKILL timedOut=true (10013ms)
- jit: exit=null signal=SIGKILL timedOut=true (10012ms)

### VM_FAIL (known-red: hashmap-filter-garbage) — `SYN__hashmap-filter-garbage.shape`
- tier: synthetic; source: tools/vmjit-diff/synthetic/SYN__hashmap-filter-garbage.shape
- known-red reason: Synthetic repro committed by WF-0B baseline 2026-07-05: vm errors 'no method filterIndexed on receiver kind Ptr(HashMap)' (exit 1); jit prints a different garbage integer each run (exit 0) — e.g. 4816060185762492416 vs 4816146652915635200 on consecutive runs. Classified VM_FAIL, jit stdout nondeterministic. Verified by hand against the worktree binary.
- vm: exit=1 signal=null timedOut=false (318ms)
- jit: exit=0 signal=null timedOut=false (339ms)
- vm stderr:
```
Error: Runtime error: no method 'filterIndexed' on receiver kind Ptr(HashMap) (line 19)

```
- jit stdout:
```
4816500292211283968

```

### VM_FAIL (known-red: i64-overflow-wrap) — `SYN__i64-overflow-wrap.shape`
- tier: synthetic; source: tools/vmjit-diff/synthetic/SYN__i64-overflow-wrap.shape
- known-red reason: Synthetic repro committed by WF-0B: add_pair(i64::MAX, 1) — vm raises checked overflow (exit 1), jit prints -9223372036854775808 (exit 0). Classified VM_FAIL.
- vm: exit=1 signal=null timedOut=false (315ms)
- jit: exit=0 signal=null timedOut=false (338ms)
- vm stderr:
```
Error: Runtime error: integer addition overflow: result of 9223372036854775807 and 1 exceeds the int (i64) range; widen explicitly with `as number` or `as bigint` (line 10)

```
- jit stdout:
```
-9223372036854775808

```

### DIVERGED (known-red: jit-double-execution) — `SYN__jit-double-execution.shape`
- tier: synthetic; source: tools/vmjit-diff/synthetic/SYN__jit-double-execution.shape
- known-red reason: Synthetic repro committed by WF-0B baseline 2026-07-05: vm prints STEP1 STEP2 AFTER; jit fails natively at runtime with signal -1 after the prints, re-runs the whole program under the interpreter and prints STEP1 STEP2 STEP1 STEP2 AFTER (exit 0). Classified DIVERGED. Verified by hand against the worktree binary.
- vm: exit=0 signal=null timedOut=false (316ms)
- jit: exit=0 signal=null timedOut=false (369ms)
- vm stdout:
```
STEP1
STEP2
AFTER

```
- jit stdout:
```
STEP1
STEP2
STEP1
STEP2
AFTER

```
- jit stderr:
```
[jit-fallback] function main failed JIT compile: Runtime error: JIT execution error (code: -1); running under interpreter

```
