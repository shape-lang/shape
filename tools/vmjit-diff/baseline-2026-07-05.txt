--------------------------------------------------------------------------------
VM-vs-JIT differential harness — BASELINE 2026-07-05
  (WF-1A-followup REFRESH — jit-fallback-engine-isolation)
--------------------------------------------------------------------------------
Binary   : worktree release build, shape 0.3.2 (branch wave1/jit-fallback-isolation)
Corpus   : 467 programs (240 book runnable fences, 224 acceptance, 3 synthetic)
Timeout  : 10s per mode per program
Runner   : tools/vmjit-diff/run-diff.mjs (regenerated with --fresh; no resume)
Exit     : 0 — zero unexpected non-MATCH

SUMMARY COUNTS (post WF-1A-followup jit-fallback-engine-isolation)
  MATCH    = 466   (465/466 — flaps by 1 with the pb3 sample below)
  DIVERGED =   1   (ACC__functions__finding_s1_unknown_hof_return_kind_confusion)
  VM_FAIL  =   0
  JIT_FAIL =   0
  TIMEOUT  =   0
  known-red hits = 1, known-red now matching = 1, unexpected non-MATCH = 0

  NOTE on the flap: ACC__comptime__pb3.shape is a PRE-EXISTING nondeterministic
  flake (comptime typed-string-array emitted without a FrameDescriptor → both
  modes fail V2 verification identically, then execute the mis-verified bytecode
  into UB whose crash-mode flaps exit 101 / exit 1 across runs). It samples MATCH
  (both exit 101) OR DIVERGED (vm 101 / jit 1) run-to-run, so MATCH is 465 or 466
  and known-red-now-matching is 0 or 1. Either sample is EXIT 0 (pb3 is pinned in
  known-red.json as comptime-typedstring-verify-nondeterministic). This fresh run
  sampled the MATCH side (MATCH=466, now-matching=1). Do NOT auto-remove the pb3
  pin on a single now-matching sample — it is nondeterministic. Root: a comptime
  typed-string-array FrameDescriptor gap (comptime-marshal follow-up), NOT engine
  isolation; PROVEN pre-existing on the UNPATCHED base binary fbfd050e.

DELTA vs the WF-1A baseline (was MATCH=462, DIVERGED=2, JIT_FAIL=3; preserved below)
  MATCH 462 -> 466 (+4).  WF-1A-followup (jit-fallback-engine-isolation) closed the
  `jit-object-merge-field-add` class — all 4 of its ids now DETERMINISTICALLY MATCH:
    ACC__objects-arrays__small : JIT_FAIL -> MATCH
    ACC__operators__small      : JIT_FAIL -> MATCH
    ACC__collections__large    : JIT_FAIL -> MATCH
    ACC__operators__large      : DIVERGED -> MATCH
  Root cause (fix-plan §6quinquies): the [jit-fallback] interpreter path re-COMPILED
  `program` via `execute_program` on the SAME engine already mutated by the JIT
  inspection compile (`compile_program_for_inspection`, executor.rs:149). The second
  compile advanced `engine.runtime.schema_registry.next_id`, shifting ambient-domain
  merged/named-type SchemaIds UP while compiler-LOCAL inline-object ids reset to their
  per-compile base — colliding in the shared SchemaId namespace and resolving a wide
  merged object against a narrow inline schema (`MakeFieldRef field_idx N out of
  bounds`). The merge codegen is correct in isolation; the bug was the double-compile.
  Fix: `execute_compiled` extracted from `execute_program` (crates/shape-vm/src/
  execution.rs) and the JIT fallback (crates/shape-jit/src/executor.rs) now runs the
  ALREADY-BUILT inspection `bytecode` instead of a second compile — no counter
  re-advance, byte-identical to `--mode vm`. NOT a tag-decode/dynamic shim.
  These 4 ids' ACC__ repro programs stay in the corpus as regression MATCH cases;
  their known-red.json entries were REMOVED (a MATCH on a listed id is a stale pin).

REMAINING known-red (both reclassified — root deeper than engine isolation)
  - hof-return-kind-raw-bits (1: ACC__functions__finding_s1_...) -> WF-3A
    type-system-edges + let-generalization. `apply(f,x)` has an unprovable return
    (untyped param f); the sound fix is HOF return-kind inference / HM
    let-generalization + a D2 number->int compile error, NOT a JIT stamp.
    Classified DIVERGED, both exit 0, deterministic.
  - comptime-typedstring-verify-nondeterministic (1: ACC__comptime__pb3) -> a
    comptime-marshal follow-up. See the flap NOTE above. Pinned so the flake does
    not spuriously red/green the gate; a MATCH sample is EXPECTED.

The 3 registry_cross_check-only JIT return-kind drifts (Array<int>.mean, HashMap.iter,
HashMap.get) have no corpus program; they are pinned/retired in
crates/shape-jit/src/mir_compiler/types.rs (mean + iter retired by WF-1A; get
reclassified to WF-3A / ADR-006 §2.7.17 uniform-Option-return).

Both remaining non-MATCH ids stay pinned in tools/vmjit-diff/known-red.json. Full
fresh per-program report follows; the historical WF-0B baseline (MATCH=458) is
preserved in git history and summarized at the very bottom for provenance.

--------------------------------------------------------------------------------
FULL REPORT (reports/report.md — WF-1A-followup --fresh run 2026-07-05)
--------------------------------------------------------------------------------
# VM-vs-JIT differential report

- generated: 2026-07-05T21:11:57.122Z
- binary: `/home/dev/dev/shape-lang/worktrees/wf-jitfallback/target/release/shape` (shape 0.3.2)
- corpus: tools/vmjit-diff/corpus (generated 2026-07-05T04:45:03.196Z)
- programs run: 467, timeout 10s/mode
- counts: MATCH=466, DIVERGED=1, VM_FAIL=0, JIT_FAIL=0, TIMEOUT=0
- known-red hits: 1; known-red now matching: 1; **unexpected non-MATCH: 0**

## Non-MATCH programs

### DIVERGED (known-red: hof-return-kind-raw-bits) — `ACC__functions__finding_s1_unknown_hof_return_kind_confusion.shape`
- tier: acceptance; source: docs/cluster-audits/v0.3.3-book-acceptance/programs/functions/finding_s1_unknown_hof_return_kind_confusion.shape
- known-red reason: vm prints '6.0'; jit prints '4618441417868443648' (a raw f64 bit pattern). RECLASSIFIED WF-1A -> WF-3A type-system-edges: `apply(f, x)` has an unprovable return type (untyped `f`), so `let bad: int = r` is admitted where r is number-valued; the sound fix is HOF return-kind inference / let-generalization + a D2 number->int compile error, not a JIT return-kind-table stamp. Classified DIVERGED, both exit 0, deterministic. Verified per-id in the 2026-07-05 baseline run.
- vm: exit=0 signal=null timedOut=false (152ms)
- jit: exit=0 signal=null timedOut=false (172ms)
- vm stdout:
```
6.0
0

```
- jit stdout:
```
4618441417868443648
0

```

## Known-red entries now MATCHING (candidates for allowlist removal)
- ACC__comptime__pb3.shape
  (EXPECTED — nondeterministic flake, do NOT remove the pin on a single MATCH
   sample; see the flap NOTE in SUMMARY COUNTS above.)

--------------------------------------------------------------------------------
HISTORICAL PROVENANCE
--------------------------------------------------------------------------------
WF-1A REFRESH (superseded by this WF-1A-followup refresh) recorded MATCH=462,
DIVERGED=2, VM_FAIL=0, JIT_FAIL=3, TIMEOUT=0, known-red hits=5, unexpected=0. It
closed four audit-2026-07-04 §4 divergence classes (jit-double-execution,
i64-overflow-wrap, annotation-hooks-dropped, hashmap-filter-garbage) and the
compile-blowup TIMEOUT, and reclassified the object-merge (4 ids) + hof-return-kind
(1 id) known-reds. Its full per-program report lives in this file's git history
(pre WF-1A-followup refresh). The WF-1A-followup work above then closed the
object-merge class (dirty-engine fallback recompile), moving its 4 ids to MATCH.

The prior WF-0B baseline snapshot (binary shape 0.3.2 @ 1fb805b3, branch
wave0/vmjit-diff-harness) recorded MATCH=458, DIVERGED=4, VM_FAIL=2, JIT_FAIL=2,
TIMEOUT=1, known-red hits=9, unexpected non-MATCH=0. Its full per-program report
also lives in this file's git history.
