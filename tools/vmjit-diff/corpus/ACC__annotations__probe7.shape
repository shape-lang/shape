// Probe: comptime pre that reads scalar target fields only (target.name)
annotation note_name() on function {

  comptime pre(target, ctx) {
    print(f"defining: {target.name}")
  }
}

@note_name()
fn greet() -> int {
  7
}

print("PROBE7_OK")
