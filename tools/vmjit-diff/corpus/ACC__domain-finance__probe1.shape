from std::finance::risk use { sharpe_ratio, max_drawdown, kelly_criterion }

fn main() {
    let returns = [0.01, 0.02, -0.01, 0.03, 0.005]
    let s = sharpe_ratio(returns)
    println(f"sharpe={s}")
}

main()
