export const meta = {
  name: 'wf-gc-on-readiness',
  description: 'GC-on readiness validation (the go/no-go input BEFORE flipping the gc feature on by default). ALL GC phases are merged (0/1/2/3a/§3.5/3c/3b/4/5 + JIT typed-pointer migration). This lane does NOT flip the default — it VALIDATES gc-on end-to-end by building --features gc and proving: (1) the REAL Finding #31 leak (a per-iteration fresh closure-in-array garbage cycle that goes out of scope) has BOUNDED RSS with gc on vs UNBOUNDED with gc off — the load-bearing end-to-end proof; (2) the FULL --features gc test suite is green unfiltered (no regression vs gc-off) across shape-value/shape-vm/shape-runtime/shape-jit + shape-test; (3) gc-off is byte-identical (default build unchanged); (4) valgrind-clean on real gc-on workloads (not just unit tests); (5) the gc-on hot-path/safepoint perf overhead is acceptable. Then a clear GO/NO-GO readiness report + residual risks + the exact flip mechanism. Independent Opus verify. Produces a validation-report doc (no default flip — that is the user go/no-go).',
  phases: [
    { title: 'Validate', detail: 'build --features gc; #31 RSS bounded (on) vs unbounded (off); full gc-on suite green; gc-off byte-identical; perf' },
    { title: 'IndepVerify', detail: 'independent Opus adversarial re-run: #31 bounded from scratch, valgrind on real gc-on workloads, no regression, gc-off parity' },
    { title: 'Report', detail: 'GO/NO-GO readiness + residual risks + flip mechanism, committed as a validation-report doc' },
  ],
}
const WT = '/home/dev/dev/shape-lang/shape-w7-gcon'
const DX = 'direnv exec /home/dev/dev/shape-lang'
const CTX = [
  'Work IN ' + WT + ' (branch wave7/gc-on-readiness, off main HEAD — ALL GC phases merged). Build/test via: ' + DX + ' <cmd>. gc is a Cargo feature (crates/shape-value/Cargo.toml gc=[]; shape-vm gc=["shape-value/gc"]), default OFF; the safepoint trigger is dispatch.rs maybe_collect(GC_CANDIDATE_THRESHOLD=256). Flipping on = adding gc to default features. This lane VALIDATES gc-on; it does NOT flip the default.',
  '',
  'THE REAL FINDING #31 (the load-bearing proof): the leak is a PER-ITERATION FRESH garbage cycle that goes out of scope — e.g. a loop where each iteration creates a LOCAL closure-in-array cycle that becomes unreachable at the end of the iteration: `fn leak() { var arr = []; arr.push(|| arr.len()) }` called in a loop `for i in 0..N { leak() }`, OR a top-level `for i in 0..N { var arr = []; arr.push(|| arr.len()) }`. Each iteration leaks a 4-node closure↔array cycle (RC cannot reclaim it). With gc OFF, RSS grows ~linearly with N (the 33MB→4.1GB blowup). With gc ON, the safepoint CollectCycles reclaims each dead cycle → RSS stays BOUNDED. MEASURE this (RSS over N=large iterations, gc-off vs gc-on) — it is the end-to-end proof that gc-on actually fixes the leak. If the exact closure-in-array form is not expressible/collectable end-to-end in current Shape source, find the nearest expressible per-iteration garbage cycle (object↔object var-field cycle, SharedCell cycle) that IS collectable, and note precisely what #31 shape remains not-yet-collectable-end-to-end (honest).',
  '',
  'PHASE 1 VALIDATE (build + measure, minimal edits): (1) build ' + DX + ' cargo build --release --bin shape --features gc; (2) write the #31 per-iteration-garbage-cycle repro + a bounded-RSS harness (run N=1e5..1e6 iterations, sample RSS via /proc/self/status or the OS, gc-off vs gc-on) → prove RSS BOUNDED with gc on, GROWING with gc off; (3) run the FULL --features gc suite UNFILTERED: ' + DX + ' cargo test -p shape-value -p shape-vm -p shape-runtime -p shape-jit --features gc (--test-threads=1 for shape-jit) + the shape-test integration suite → all green, compared to the gc-off baseline (no regression); (4) confirm gc-off is byte-identical (default cargo test green, no new hot-path cost); (5) measure the gc-on hot-loop overhead (the safepoint poll) vs gc-off on a tight loop — is it acceptable (single relaxed load)?',
  '',
  'CONSTRAINTS: do NOT flip the gc default (that is the user go/no-go). Do NOT weaken any test. Honest measurement — if #31 is only partially collectable end-to-end or a residual leaks, REPORT it precisely (do not overclaim bounded). ' + DX + ' just check-no-dynamic EXIT 0 (should be unaffected). ',
  '',
  'REPORT REQUIREMENT: a clear GO / NO-GO readiness verdict with: the #31 RSS-bounded measurement (gc-off vs gc-on numbers), the full-gc-suite result, gc-off-parity, valgrind-on-real-workloads result, the gc-on perf overhead, any RESIDUAL RISK (a #31 sub-shape still leaking, a perf concern, a behavior change), and the exact one-line flip mechanism (add gc to default features + where). Commit the report as docs/cluster-audits/gc-on-readiness-report.md.',
  '',
  'STRUCTURED-OUTPUT: ONE clean JSON object, 1-4 plain sentences per field, NO XML/code blocks.',
].join('\n')
phase('Validate')
const V = { type:'object', additionalProperties:false, required:['status','rss_bounded','full_suite_green','gc_off_parity','perf','evidence'], properties:{ status:{type:'string',enum:['done','partial','blocked']}, rss_bounded:{type:'boolean',description:'the real #31 per-iteration garbage cycle has BOUNDED RSS with gc on (vs unbounded gc off)'}, full_suite_green:{type:'boolean',description:'full --features gc suite green unfiltered, no regression vs gc-off'}, gc_off_parity:{type:'boolean',description:'gc-off byte-identical / default build unchanged'}, perf:{type:'string',description:'gc-on hot-loop overhead vs gc-off (safepoint poll cost)'}, evidence:{type:'string',description:'#31 RSS numbers gc-off vs gc-on; suite counts; check-no-dynamic EXIT'} } }
const v = await agent(CTX + '\n\nPHASE 1 — VALIDATE (build --features gc; measure #31 RSS bounded; full gc-on suite; gc-off parity; perf). Report honestly. Do NOT flip the default.', { label:'validate', phase:'Validate', effort:'xhigh', schema:V })
phase('IndepVerify')
const IV = { type:'object', additionalProperties:false, required:['verdict','rss_bounded','no_regression','valgrind_clean','residual_risk','evidence'], properties:{ verdict:{type:'string',enum:['GO','NO-GO','GO-WITH-CAVEATS']}, rss_bounded:{type:'boolean',description:'from YOUR OWN run: #31 RSS bounded gc-on, unbounded gc-off'}, no_regression:{type:'boolean',description:'full gc-on suite green + gc-off byte-identical (you re-ran)'}, valgrind_clean:{type:'boolean',description:'valgrind clean on real gc-on workloads (a running Shape program, not just unit tests)'}, residual_risk:{type:'string',description:'any #31 sub-shape still leaking / perf concern / behavior change (or "none")'}, evidence:{type:'string',description:'your own from-scratch #31 RSS + valgrind + suite runs; concise'} } }
const iv = await agent(CTX + '\n\nVALIDATE: ' + JSON.stringify(v) + '\n\nYou are an INDEPENDENT adversarial reviewer, FRESH context. This gates a SHIPPED-BEHAVIOR change (gc-on collection runs by default). From scratch: (1) is the real #31 per-iteration garbage cycle RSS BOUNDED with gc on and GROWING with gc off (measure both, real numbers)? (2) is the FULL --features gc suite green unfiltered with NO regression vs the gc-off baseline, and gc-off byte-identical? (3) VALGRIND on a REAL running gc-on Shape workload (a program that churns garbage cycles under gc, not just unit tests) — 0 UAF/leak/double-free? (4) any residual: a #31 sub-shape still leaking, an unacceptable gc-on hot-loop overhead, or a behavior change? Give a GO / NO-GO / GO-WITH-CAVEATS verdict with the residual risks. Any unbounded #31, regression, valgrind error, or unacceptable overhead = NO-GO (or GO-WITH-CAVEATS naming the caveat).', { label:'indep-verify', phase:'IndepVerify', effort:'xhigh', schema:IV })
phase('Report')
const R = { type:'object', additionalProperties:false, required:['verdict','summary','flip_mechanism','residual_risks','report_committed'], properties:{ verdict:{type:'string',enum:['GO','NO-GO','GO-WITH-CAVEATS']}, summary:{type:'string',description:'the readiness in 2-3 sentences (the #31 numbers + suite + valgrind)'}, flip_mechanism:{type:'string',description:'the exact one-line change to flip gc on by default + where'}, residual_risks:{type:'string',description:'residual risks the user should weigh before flipping'}, report_committed:{type:'boolean',description:'docs/cluster-audits/gc-on-readiness-report.md committed'} } }
const r = await agent(CTX + '\n\nVALIDATE: ' + JSON.stringify(v) + '\nVERIFY: ' + JSON.stringify(iv) + '\n\nPHASE 3 — REPORT. Write docs/cluster-audits/gc-on-readiness-report.md with the GO/NO-GO verdict, the #31 RSS-bounded numbers, the full-gc-suite + gc-off-parity + valgrind results, the gc-on perf overhead, residual risks, and the exact flip mechanism (add gc to default features + where). Commit it (git add -A && git commit --no-verify -m "GC-on readiness report: <verdict> — end-to-end validation for the gc-default flip").', { label:'report', phase:'Report', effort:'high', schema:R })
return { v, iv, r }
