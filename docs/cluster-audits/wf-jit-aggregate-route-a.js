export const meta = {
  name: 'wf-jit-aggregate-route-a',
  description: 'Book cluster C1 (user-greenlit): the JIT MirToIR surface-and-stops on Rvalue::Aggregate in the "Route A" path (crates/shape-jit/src/mir_compiler/rvalues.rs:341 "Route A surface-and-stop: SURFACE — Rvalue::Aggregate"), causing a whole-program deopt to the interpreter for programs that hit it (book ownership-deep-dive:459, content:341 — move-semantics/aggregate construction). Output is correct via deopt, but the JIT never natively compiles these. Implement the Rvalue::Aggregate route in MirToIR so the JIT natively compiles it (VM==JIT, no deopt) rather than surface-and-stop. Independent Opus verify vm+jit.',
  phases: [
    { title: 'Diagnose', detail: 'what Rvalue::Aggregate shapes reach rvalues.rs:341 Route-A surface; what native lowering they need' },
    { title: 'Fix', detail: 'lower Rvalue::Aggregate in MirToIR (the move/aggregate construction) natively; no surface-and-stop' },
    { title: 'Verify+Finish', detail: 'independent Opus: the aggregate programs JIT natively (no deopt) VM==JIT; gates + tests' },
  ],
}
const WT = '/home/dev/dev/shape-lang/shape-w7-jitagg'
const DX = 'direnv exec /home/dev/dev/shape-lang'
const CTX = [
  'Work IN ' + WT + ' (branch wave7/jit-aggregate-route-a, off main HEAD — has the JIT typed-pointer migration merged, so TypedObject is on the v2 carrier). Build/test via: ' + DX + ' <cmd>.',
  'THE GAP: crates/shape-jit/src/mir_compiler/rvalues.rs:320-341 — the Rvalue::Aggregate arm returns `Err("Route A surface-and-stop: SURFACE — Rvalue::Aggregate ...")`, so any function whose MIR contains an un-handled Rvalue::Aggregate (the "Route A" shape) fails Phase-4 JIT compile and whole-program-deopts to the interpreter. Some Aggregate shapes ARE handled (v2_array.rs:239/258/405 handles Aggregate operands for array/object construction; statements.rs:21/55 handles Aggregate for typed-object/array store) — Route A is the RESIDUAL un-lowered shape (move-semantics / aggregate scratch step per the comments). The book fences ownership-deep-dive:459 + content:341 hit it. DIAGNOSE exactly which Aggregate shape reaches rvalues.rs:341 and what native Cranelift lowering it needs (likely: materialize each operand + assemble the aggregate value into the destination place, mirroring the already-handled array/object-construction Aggregate paths on the v2 carrier).',
  'FIX: implement the native lowering for the Route-A Rvalue::Aggregate in MirToIR (rvalues.rs) — emit the operand materialization + aggregate assembly directly (reuse the v2_array.rs / statements.rs Aggregate machinery where applicable), so the JIT natively compiles these programs with output IDENTICAL to the VM (no deopt). NO surface-and-stop for the handled shape; if a genuinely-different Aggregate sub-shape remains unlowerable, keep a clean deopt for THAT precise residual (documented) rather than a broad surface.',
  'CONSTRAINTS (CLAUDE.md): NO forbidden patterns (no is_heap/tag-decode/ValueWord/Bool-default); the carrier is the v2-raw TypedObjectStorage (post-migration); NO kind-injection. VM==JIT output. ' + DX + ' just check-no-dynamic EXIT 0; ' + DX + ' just check-clean EXIT 0. JIT tests --test-threads=1.',
  'REGRESSION-TEST (user rule): the ownership-deep-dive:459 / content:341-shape programs (aggregate/move construction) JIT-compile NATIVELY (no [jit-fallback] deopt) with output == VM; a spread of aggregate-construction programs VM==JIT. No new #[ignore].',
  'STRUCTURED-OUTPUT: ONE clean JSON object, 1-4 plain sentences per field, NO XML/code blocks.',
].join('\n')
phase('Diagnose')
const D = { type:'object', additionalProperties:false, required:['aggregate_shape','lowering_plan','residual'], properties:{ aggregate_shape:{type:'string',description:'the exact Rvalue::Aggregate shape reaching rvalues.rs:341'}, lowering_plan:{type:'string',description:'the native Cranelift lowering (operand materialize + assemble)'}, residual:{type:'string',description:'any sub-shape that stays a clean deopt (or "none")'} } }
const d = await agent(CTX + '\n\nPHASE 1 — DIAGNOSE. Pin the Route-A Aggregate shape + lowering. Do NOT commit.', { label:'diagnose', phase:'Diagnose', effort:'high', schema:D })
phase('Fix')
const F = { type:'object', additionalProperties:false, required:['status','files_changed','jit_native','evidence'], properties:{ status:{type:'string',enum:['done','partial','blocked']}, files_changed:{type:'string',description:'the Rvalue::Aggregate native lowering'}, jit_native:{type:'boolean',description:'the aggregate programs JIT natively (no deopt), VM==JIT'}, evidence:{type:'string',description:'ownership/content fences JIT native + output==VM; check-no-dynamic EXIT 0'} } }
const f = await agent(CTX + '\n\nDIAGNOSIS: ' + JSON.stringify(d) + '\n\nPHASE 2 — FIX: native lowering for Route-A Rvalue::Aggregate. ' + DX + ' just check-no-dynamic EXIT 0. Commit (git add -A && git commit --no-verify -m "JIT: natively lower Rvalue::Aggregate Route-A (move/aggregate construction) instead of surface-and-stop deopt (book C1)").', { label:'fix', phase:'Fix', effort:'xhigh', schema:F })
phase('Verify+Finish')
const V = { type:'object', additionalProperties:false, required:['verdict','jit_native','vm_jit_parity','no_regression','gates','merge_ready'], properties:{ verdict:{type:'string',enum:['CONFIRMED','REFUTED','PARTIAL']}, jit_native:{type:'boolean',description:'from YOUR OWN run: the aggregate programs JIT-compile natively (no [jit-fallback] deopt)'}, vm_jit_parity:{type:'boolean',description:'output identical VM vs JIT for the aggregate programs'}, no_regression:{type:'boolean',description:'existing JIT (array/object construction, other Aggregate paths) unregressed'}, gates:{type:'string',description:'check-clean + check-no-dynamic + shape-jit tests'}, merge_ready:{type:'boolean'} } }
const v = await agent(CTX + '\n\nFIX: ' + JSON.stringify(f) + '\n\nYou are an INDEPENDENT adversarial reviewer, FRESH context. From scratch: do the Route-A aggregate programs (ownership-deep-dive:459 / content:341 shapes + your own aggregate-construction programs) now JIT-compile NATIVELY with NO [jit-fallback] deopt, output IDENTICAL to VM? Are the already-handled Aggregate paths (array/object construction) + other JIT programs unregressed? No forbidden pattern (check-no-dynamic EXIT 0)? Add regression tests; no new #[ignore]. Run ' + DX + ' just check-clean, ' + DX + ' just check-no-dynamic, ' + DX + ' cargo test -p shape-jit --test-threads=1. Any remaining deopt on the handled shape, VM!=JIT, or regression = REFUTED. Commit tests (git commit --no-verify -m "JIT aggregate route-A finalize: native-lowering regression tests").', { label:'verify-finish', phase:'Verify+Finish', effort:'xhigh', schema:V })
return { d, f, v }
