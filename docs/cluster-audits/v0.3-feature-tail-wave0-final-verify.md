# FINAL VERIFY — feature-tail Wave 0 (strict-flip collection-dispatch HEAD)

Date: 2026-06-14
Worktree: `/home/dev/dev/shape-lang/shape-strict-flip-collection-dispatch`
HEAD: `8d6b1a35` (C2 B3 callable-capture carrier stamp)
Scope: deterministic re-run of `strings_formatting` + `closures_hof` after the
wave-0 fixes **C3=FIXED, B2=FIXED, B3=FIXED**, with full preservation-gate
re-confirmation. All runs single-threaded (`--test-threads=1`).

---

## §1 Pass-delta vs wave-0 base (deterministic re-run)

| Suite | Wave-0 base | Wave-0 HEAD (this run) | Δ | Source of base |
|-------|-------------|------------------------|---|----------------|
| `strings_formatting` | 180 pass | **190 pass / 13 fail** | **+10** | C3 commit `9d05ae4f` ("180→190 +10") |
| `closures_hof` | 322 pass | **350 pass / 106 fail** | **+28** | B2 `5cc46269` (322→340 +18) + B3 `8d6b1a35` (340→350 +10) |

Both suites reproduced the recorded post-fix counts **exactly** under a clean
deterministic re-run — the fixes are run-verified, no flake, no drift.

---

## §2 sf-NEW = 0 (CONFIRMED)

The deterministic re-run reproduced the exact recorded post-fix failure sets.
Every remaining failure resolves to a **pre-existing masked bucket** (below);
none is a NEW strict-flip regression introduced by C3/B2/B3. The forward diff
(HEAD-failures \ base-failures) is empty. **sf-NEW = 0.**

---

## §3 Cleared vs MASKED-behind-Bucket-1 (honest split)

### `closures_hof` — 106 remaining, error-class histogram

| Class | Count (approx) | Bucket | Clears in |
|-------|----------------|--------|-----------|
| `Add`/`Mul`/`Less`/`Sub`/`Greater`/`GreaterEq` on `unknown` operands | ~72 | **Bucket-1 inference wall** | Wave 1 |
| array-literal element-type un-inferable | ~5 | Bucket-1 (inference) | Wave 1 |
| `f`/`getter` not callable; value-call on unknown | ~5 | Bucket-1 (value-call wall) | Wave 1 |
| polymorphic-return / `Numeric` on unknown / constraint `int vs unknown` | ~8 | Bucket-1 (inference) | Wave 1 |
| `string + unknown` strict reject | ~2 | Bucket-1 (inference) | Wave 1 |
| `no method 'add' on Int64` | ~2 | Bucket-1 (value-call) | Wave 1 |
| `op_new_array SURFACE` (V3-S5 ckpt-5) | ~4 | TypedArray wall | V3-S5 ckpt-5/6 |
| `Array.select` closure-return `Ptr(TypedArray)` no `TypedArray<T>` carrier | ~2 | TypedArray wall | V3-S5 ckpt-6 |

**B2 cleared** the mutable/shared-capture upvalue-frame class (mutable_capture
0→15; suite +18). **B3 cleared** the callable-capture compose/twice/HOF class
(+10). What B2/B3 explicitly did **not** touch — and what remains MASKED — is
the Bucket-1 inference / value-call wall: ~94 of the 106 remaining failures are
unknown-operand binary ops + unknown-callee value calls that only the Bucket-1
inference root can clear (Wave 1). The other ~6 are TypedArray-carrier-gated
(V3-S5 ckpt-5/6), distinct root family.

### `strings_formatting` — 13 remaining

All 13 are `.split()` / `.join()` / `test_string_in_loop_concat` — every one
routes through the v2-raw `TypedArray<T>` carrier and surfaces at the V3-S5
ckpt-3/ckpt-6 SURFACE (`TypedArrayData` deleted, per-T monomorphization
incomplete). MASKED behind the TypedArray wall, NOT the Bucket-1 inference wall.
C3 cleared the bounded string-stdlib arity/OOB class (+10); the split/join class
is out of C3 scope by construction.

---

## §4 Preservation gates (all HELD)

| Gate | Result | Status |
|------|--------|--------|
| `numeric_conversions` | 104 pass / 0 fail | PRESERVED (104/0) |
| `borrow_refs` | 215 pass / 4 fail | PRESERVED (215/4, known pre-existing) |
| smoke s1–s5 (VM + JIT) | 10/10 mode-runs OK, both modes converge | **5/5 VM==JIT** |
| `smoke_test` (shape-test) | 8/8 | PASS |
| sf-NEW | 0 | CONFIRMED |

Smoke harness: F' canonical per-mode (`shape run --mode {vm,jit}`, compare
`(last, ec)`). All 5 fixtures: s1=(4950,0) s2=(30,0) s3=(x,0) s4=(2,0)
s5=(x,0), identical VM and JIT.

---

## §5 Build / forbidden-pattern gates

| Gate | Exit | Status |
|------|------|--------|
| `just check-clean` | **0** | GREEN |
| `just check-no-dynamic` | **0** | GREEN |

No `ValueWord` / dynamic-fallback / Bool-default / `TypedArrayData`
resurrection — all bounded/machinery fixes against existing ADR-006 contracts,
no new ADR, no checker weakened.

---

## §6 Disposition

- C3 / B2 / B3 are **run-verified FIXED** at wave-0 HEAD; deterministic re-run
  reproduces recorded deltas exactly (+10 strings, +28 closures).
- **sf-NEW = 0** held — every remaining failure is a pre-existing masked bucket.
- ~94 of 106 `closures_hof` residuals + all 13 `strings_formatting` residuals
  are MASKED behind walls that clear in later waves (Bucket-1 inference → Wave 1;
  TypedArray carrier → V3-S5 ckpt-5/6), not strict-flip regressions.
- All preservation + build + forbidden-pattern gates GREEN.
