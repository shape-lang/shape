// THE BREACH: find's predicate must return `bool`, but this closure returns
// `int` (`x - 5`). Strict typing must reject (predicate type is `int -> bool`).
// Instead the first element with a nonzero `x - 5` (i.e. 6) is returned.
let arr = [5, 6, 7]
let r = arr.find(|x| x - 5)
print(r)
