export const meta = {
  name: 'wf-gc-cross-worker-stw',
  description: 'GC Phase 3b (user-ratified 2026-07-07, re-confirmed with the no-cross-thread-heap finding: BUILD THE FULL CROSS-WORKER STW NOW, forward-correct). Today CollectCycles runs at a SAME-THREAD safepoint (dispatch.rs maybe_collect at the &0x3FF gates). Phase 3b adds the cross-worker stop-the-world rendezvous so collection is safe against concurrent heap mutation from other workers: a thread registry + a stop-request AtomicBool + per-thread safepoint-ack + a barrier (all workers parked at a safepoint before collection) + collect-under-global-stop + resume — halting the VM loop, async task workers, and JIT threads. IMPORTANT REALITY (design-refined): there is NO cross-thread shared mutable heap today (async task VMs have disjoint heaps, JIT workers are heap-inert, SharedAtomic absent from BindingStorageClass), so no garbage cycle spans threads yet — 3b is FORWARD MACHINERY. "Done" = the rendezvous MECHANICS are correct + deadlock-free + gc-off byte-identical (near-zero-cost safepoint poll on the hot path), proven with a synthetic multi-thread scenario; NOT a real cross-thread cycle (which cannot be constructed today). DESIGN-FIRST: the rendezvous adds real concurrency complexity + a hot-path poll — surface the design + any fork before full impl. Independent Opus verify (mechanics + no-deadlock + no-hot-path-regression). All behind default-off gc feature.',
  phases: [
    { title: 'Design', detail: 'the rendezvous architecture: registry + stop-request + safepoint-ack barrier + collect-under-stop + resume; how to test without a real cross-thread cycle; hot-path cost; fork check' },
    { title: 'Implement', detail: 'thread registry + STW rendezvous, gc-gated; single-thread CollectCycles runs under the global stop' },
    { title: 'Verify', detail: 'independent Opus: mechanics correct (synthetic MT rendezvous) + NO deadlock + gc-off byte-identical + single-thread collection unregressed' },
    { title: 'Finish', detail: 'gates + rendezvous mechanics tests + no new #[ignore]' },
  ],
}
const WT = '/home/dev/dev/shape-lang/shape-w7-gc3b'
const DX = 'direnv exec /home/dev/dev/shape-lang'
const CTX = [
  'Work IN ' + WT + ' (branch wave7/gc-cross-worker-stw, off main HEAD — has GC Phases 0/1/2/3a + §3.5 + 3c merged). Build/test via: ' + DX + ' <cmd>. Design: docs/design/real-gc-cycle-collection.md (§0 ratification, OQ#4 MT-rendezvous-required, "Phase 3 (refined)" no-cross-thread-heap finding, §R2 CollectCycles).',
  '',
  'CURRENT STATE: CollectCycles (crate::gc, gc.rs) runs at a SAME-THREAD safepoint — dispatch.rs calls shape_value::gc::maybe_collect(GC_CANDIDATE_THRESHOLD) at the &0x3FF fast-path + call gates (dispatch.rs:205/372). jit_gc_safepoint (crates/shape-jit/src/ffi/gc.rs) is a real poll (flag currently unraised). All behind the default-off gc feature.',
  '',
  'PHASE 3b — the cross-worker stop-the-world rendezvous: (1) a THREAD REGISTRY of all GC-mutator threads (the main VM loop + async task worker VMs + any JIT execution threads) — each registers on start, deregisters on exit; (2) a global STOP-REQUEST AtomicBool that a collector-initiating thread raises; (3) each mutator, at its safepoint (the existing &0x3FF gate + jit_gc_safepoint), checks the stop-request and PARKS, incrementing a stopped-count and waiting on a condvar/barrier; (4) the initiator waits until ALL registered threads are parked (the ack barrier), then runs CollectCycles under the GLOBAL STOP (no mutator can touch the heap); (5) RESUME — clear the stop-request, wake all parked threads. The collection itself is the existing single-thread CollectCycles (unchanged); 3b is the SAFE-POINT RENDEZVOUS wrapper around it.',
  '',
  'TEST STRATEGY (critical — no real cross-thread cycle exists today): prove the MECHANICS with a SYNTHETIC multi-thread scenario — spawn N worker threads that register + loop hitting the safepoint; from another thread raise the stop-request; assert ALL workers park (stopped-count == N) within a bound, that CollectCycles runs exactly once under the stop, and that all workers resume and make progress after. Also a NO-DEADLOCK test (a worker that exits mid-rendezvous deregisters cleanly; nested/re-entrant collection is prevented). Do NOT fabricate a cross-thread garbage cycle (unconstructible); test the rendezvous protocol itself.',
  '',
  'CONSTRAINTS (CLAUDE.md + design, CRITICAL): behind the gc feature (feature-off = strict no-op — the safepoint poll must compile to the SAME near-zero-cost check as today when gc is off; NO new hot-path cost gc-off). NO forbidden patterns. The hot-path safepoint poll must be a single relaxed atomic load (cheap); parking only on the rare stop-request. NO DEADLOCK: a thread that never reaches a safepoint (e.g. blocked on IO) must not hang the rendezvous forever — bound the wait / handle non-safepointing threads (document the policy). Single-thread collection (the common case) must still work (a lone registered thread self-rendezvouses trivially). ' + DX + ' just check-no-dynamic EXIT 0; ' + DX + ' just check-clean EXIT 0.',
  '',
  'REGRESSION-TEST REQUIREMENT (user rule): rendezvous-mechanics tests (N workers all park + collect-once-under-stop + resume; clean deregister on exit; no deadlock; single-thread trivial rendezvous) + confirm the existing single-thread collection + all gc tests still pass + gc-off byte-identical. No new #[ignore].',
  '',
  'STRUCTURED-OUTPUT: ONE clean JSON object, 1-4 plain sentences per field, NO XML/code blocks in fields.',
].join('\n')
phase('Design')
const D = { type:'object', additionalProperties:false, required:['architecture','test_strategy','hotpath_cost','deadlock_policy','fork'], properties:{ architecture:{type:'string',description:'registry + stop-request + ack-barrier + collect-under-stop + resume, concretely'}, test_strategy:{type:'string',description:'how the synthetic MT rendezvous proves the mechanics without a real cross-thread cycle'}, hotpath_cost:{type:'string',description:'the gc-off + gc-on safepoint poll cost (must be a single relaxed load gc-off)'}, deadlock_policy:{type:'string',description:'how a non-safepointing/blocked thread is handled (bounded wait / policy)'}, fork:{type:'string',description:'any genuine architecture fork needing ratification (or "none")'} } }
const d = await agent(CTX + '\n\nPHASE 1 — DESIGN ONLY (no impl). The rendezvous architecture + test strategy + hot-path cost + deadlock policy + fork check. Do NOT commit.', { label:'design', phase:'Design', effort:'xhigh', schema:D })
phase('Implement')
const I = { type:'object', additionalProperties:false, required:['status','files_changed','mechanics_work','gc_off_noop','evidence'], properties:{ status:{type:'string',enum:['done','partial','blocked']}, files_changed:{type:'string',description:'the thread registry + STW rendezvous, brief'}, mechanics_work:{type:'boolean',description:'synthetic MT rendezvous: N workers park, collect-once-under-stop, resume'}, gc_off_noop:{type:'boolean',description:'gc-off byte-identical / no new hot-path cost'}, evidence:{type:'string',description:'rendezvous test green; single-thread collection unregressed; check-no-dynamic EXIT 0'} } }
const impl = await agent(CTX + '\n\nDESIGN: ' + JSON.stringify(d) + '\n\nPHASE 2 — IMPLEMENT the STW rendezvous (gc-gated, no-deadlock, near-zero-cost gc-off). ' + DX + ' just check-no-dynamic EXIT 0. Commit (git add -A && git commit --no-verify -m "GC Phase 3b: cross-worker stop-the-world rendezvous (thread registry + stop-request + safepoint-ack barrier + collect-under-stop + resume; gc feature)").', { label:'implement', phase:'Implement', effort:'xhigh', schema:I })
phase('Verify')
const V = { type:'object', additionalProperties:false, required:['verdict','mechanics_correct','no_deadlock','gc_off_identical','no_collection_regression','evidence'], properties:{ verdict:{type:'string',enum:['CONFIRMED','REFUTED','PARTIAL']}, mechanics_correct:{type:'boolean',description:'from YOUR OWN run: N workers all park, CollectCycles runs once under global stop, all resume + progress'}, no_deadlock:{type:'boolean',description:'no deadlock/hang — clean deregister on exit, bounded wait for non-safepointing threads, single-thread trivial rendezvous, re-entrancy prevented'}, gc_off_identical:{type:'boolean',description:'gc-off byte-identical, no new hot-path cost'}, no_collection_regression:{type:'boolean',description:'existing single-thread CollectCycles + all gc tests still pass (incl valgrind on the collection tests)'}, evidence:{type:'string',description:'your own MT-rendezvous + deadlock + single-thread + gc-off runs; concise'} } }
const v = await agent(CTX + '\n\nDESIGN: ' + JSON.stringify(d) + '\nIMPL: ' + JSON.stringify(impl) + '\n\nYou are an INDEPENDENT adversarial reviewer, FRESH context. This is CONCURRENCY code — be maximally skeptical about deadlock + hot-path cost. (1) MECHANICS: spawn your own N-worker synthetic scenario — do ALL workers park on stop-request (stopped-count==N), does CollectCycles run EXACTLY once under the global stop, do all workers resume + make progress? (2) NO DEADLOCK: a worker exiting mid-rendezvous, a single-thread trivial rendezvous, re-entrant/nested collection, and a non-safepointing thread — none hang (run with a timeout; a hang = REFUTED). (3) gc-OFF byte-identical + no new hot-path cost (the poll is a single relaxed load gc-off). (4) the existing single-thread collection + all gc tests still pass (valgrind on the collection tests clean). Any deadlock/hang, collection regression, or gc-off hot-path cost = REFUTED.', { label:'independent-verify', phase:'Verify', effort:'xhigh', schema:V })
phase('Finish')
const F = { type:'object', additionalProperties:false, required:['gates','tests_added','merge_ready'], properties:{ gates:{type:'string',description:'check-clean + check-no-dynamic + gc tests (off + gc) + valgrind + the rendezvous tests'}, tests_added:{type:'string',description:'the rendezvous-mechanics + no-deadlock + single-thread tests'}, merge_ready:{type:'boolean'} } }
const finish = await agent(CTX + '\n\nIMPL: ' + JSON.stringify(impl) + '\nVERDICT: ' + JSON.stringify(v) + '\n\nFINISH (only if CONFIRMED; else merge_ready:false + what remains). Ensure the rendezvous-mechanics + no-deadlock tests are committed; no new #[ignore]. Run ' + DX + ' just check-clean, ' + DX + ' just check-no-dynamic, ' + DX + ' cargo test -p shape-value + -p shape-vm (feature-off) + --features gc, and valgrind on the gc/rendezvous tests. Commit (git commit --no-verify -m "GC Phase 3b finalize: cross-worker STW rendezvous mechanics tests").', { label:'finish', phase:'Finish', effort:'high', schema:F })
return { d, impl, v, finish }
