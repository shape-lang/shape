# v0.3 W3.1 Slice C — Book audit: tutorial + getting-started + examples

**Branch:** `bulldozer-strictly-typed-w3-1-slice-c` (worktree `/home/dev/dev/shape-lang/shape-w3-1-slice-c`)
**Base:** `89857260` (v0.3-roadmap landing)
**Territory (9 pages, ~600 lines mdx total):**
- `shape-web/book/book-site/src/content/docs/getting-started/installation.mdx`
- `shape-web/book/book-site/src/content/docs/getting-started/first-query.mdx` (titled "Your First Program")
- `shape-web/book/book-site/src/content/docs/getting-started/basic-concepts.mdx`
- `shape-web/book/book-site/src/content/docs/getting-started/repl.mdx`
- `shape-web/book/book-site/src/content/docs/getting-started/editor-setup-vscode.mdx`
- `shape-web/book/book-site/src/content/docs/getting-started/editor-setup-neovim.mdx`
- `shape-web/book/book-site/src/content/docs/examples/hello-world.mdx`
- `shape-web/book/book-site/src/content/docs/examples/web-request.mdx`
- `shape-web/book/book-site/src/content/docs/examples/comptime-codegen.mdx`

**Audit binding:** every `shape` code block executed at HEAD via `target/release/shape run --mode vm <file>` and `--mode jit <file>` (release binary built at `target/release/shape`, version `shape 0.2.0`). VM and JIT outputs compared to documented expected output. Disposition assigned per snippet at §1'.

**Audit-only ceremony.** Zero source `.rs` / `.shape` / `.mdx` changes in this commit; only deliverable doc + AGENTS.md row.

---

## §0 Pre-flight + close-gate state

| Gate | Result |
|------|--------|
| `cargo check --workspace --lib --tests` | EXIT=0 |
| `bash scripts/verify-merge.sh > /tmp/w3-1-c-vm.out` | SCRIPT_EXIT=0, 12/12 passed |
| `bash scripts/check-no-dynamic.sh` | EXIT=0 |
| Source changes | NONE (audit-only; `git diff --stat 89857260 -- crates bin tools extensions` empty) |
| AGENTS.md row | appended (this dispatch) |
| `Co-Authored-By: Claude` trailer | NONE |
| `target/release/shape` | built (56 MB, `shape 0.2.0`) |
| Snippet matrix | 24 Shape snippets executed in VM × JIT = 48 invocations |

CLI ground truth read from `bin/shape-cli/src/cli_args.rs` (commands: Run, Repl, Tui, Doctest, Schema, Snapshot, Tree, Ext, Jit, ExpandComptime, Build, Sign, Verify, Keys, Register, Login, Publish, Add, Remove, Search, Info, **WireServe**, Check, Serve). Default `--mode` is `Jit` (`ExecutionModeArg::Jit`, `cli_args.rs:83`).

---

## §1 Per-page divergence table

Disposition codes: **update-book** (book is wrong about HEAD behavior; fix doc) / **add-to-language** (book describes a feature that should exist but doesn't; W3.2-C language fix) / **defer-with-cite** (book describes a v0.4+ feature; needs explicit "deferred" cite) / **delete-page-content** (book describes a feature that was removed; strike from book).

### installation.mdx (9 lines/codeblocks examined)

| Line | Claim | HEAD reality | Disposition |
|------|-------|--------------|-------------|
| 22–27 | `cargo install shape-cli; cargo install shape-lsp` is the recommended setup | Both crates have no `publish = false`, packaging is publishable. Not verified that they are actually published to crates.io at v0.2.0. | **defer-with-cite** — add cite to publish status or remove "from crates.io" framing pre-v0.3 ship |
| 31 | `shape` binary name | matches (`bin/shape-cli/Cargo.toml:77 name = "shape"`) | OK |
| 38 | `shape-lsp --version` | matches (`tools/shape-lsp/Cargo.toml:48 name = "shape-lsp"`) | OK |
| 45 | VS Code marketplace extension `shape-lang.shape-lang` exists | not verified at audit (marketplace state out-of-tree) | **defer-with-cite** if not live |
| 47 | `:MasonInstall shape-lsp` pending registry PR 13977 | already cited as pending | OK |
| 53–57 | `git clone … && cargo build --workspace --release` | matches (`just check-clean` per CLAUDE.md confirms `--workspace --lib --bins --tests --examples` works) | OK |
| 59 | binary at `target/release/shape` | matches (verified at this audit) | OK |
| 61 | LSP at `target/release/shape-lsp` | matches (`tools/shape-lsp/Cargo.toml:48`) | OK |
| 68–82 | `shape ext install python` / `shape ext install typescript` / `shape ext list` / `shape ext remove python` | CLI subcommand matches (`cli_args.rs:414 ExtAction { Install, List, Remove }`) | OK |
| 86–101 | "Quick test" Shape snippet (`inst_01_hello.shape`) | runs cleanly (VM + JIT both exit 0); prints "hello from shape" — followed by a noisy `{ "Integer": 0 }` JSON-rendered top-level result | **update-book** — either suppress the top-level Integer rendering in `shape run` or document that running a script that returns an `int` will print the value as JSON after script output |
| 105–109 | "Or test an expression directly" snippet (`inst_02_letprint.shape`) | runs cleanly (VM + JIT both exit 0; prints "30") | OK |

### first-query.mdx ("Your First Program", 8 shape snippets)

| Snippet | HEAD reality | Disposition |
|---------|--------------|-------------|
| §"Hello, Shape" (`fq_01_hello`) | matches book | OK |
| §"Example 1: Vectors and Indexing" (`fq_02_vectors`) | Book claim: `values[-1]` returns last element via negative indexing. HEAD reality: **VM rejects with `Index -1 out of bounds (length 5)`** (`array_basic.rs` per `property_access.rs:600` `if i < 0 { return Err(IndexOutOfBounds) }`). **JIT silently returns `0` for `values[-1]` and `values[-2]`** (no bounds check, no negative-index resolution). Three-way divergence: book vs VM vs JIT. | **update-book** + **add-to-language** — negative indexing is a documented feature that exists in neither tier; either implement (`add-to-language`) OR strike the negative-index example from the book |
| §"Example 2: Simple Statistics" (`fq_03_stats`) | `samples.mean()` panics with `not yet implemented: phase-1b-vm wave 5d — intrinsic body migration pending (handle_intrinsic_builtin): IntrinsicMean` at `crates/shape-vm/src/executor/vm_impl/builtins.rs:525:21` (VM). JIT errors at `print` Call-terminator NativeKind=None Route A surface-and-stop. `mean()` is in prelude (`prelude.shape:7 from std::core::math use { sum, mean, ... }`) so resolution is correct but execution is unwired. | **add-to-language** — `IntrinsicMean` runtime body migration is a known phase-1b-vm wave 5d follow-up (cite phase-1b-vm-wave-6-5-handover.md) |
| §"Example 3: Filtering and Transformation" (`fq_04_filter`) | VM matches book intent (prints `[27, 30, 25]` and `[29.700…, 33.0, 27.5]`). JIT errors at `print(high)` with `Route A surface-and-stop … print Call-terminator operand NativeKind is Some(Ptr(TypedArray))`. | **add-to-language** — JIT-side `jit_print_<heap_kind>` for `Ptr(TypedArray)` is the cluster-2 closure-wave-F per-HeapKind kinded jit_print rebuild territory (22/35 remaining per CLAUDE.md "v1 candidates") |
| §"Example 4: Functions with `fn`" (`fq_05_anomaly`) | Both VM and JIT reject with `Semantic error: data[...] requires explicit data binding`. Root cause: the parameter `data: [20.1, …]` shadows what the compiler thinks is a data-schema binding. Book's variable name `data` clashes with the compiler's `data` schema-keyword (see `Semantic error: data[...] requires explicit data binding`). | **update-book** — rename `let data` to `let series` (or similar non-reserved name) |
| §"Example 5: Match as an Expression" (`fq_06_bucket`) | matches book exactly in both VM and JIT (prints `negative\nzero\npositive`) | OK |
| §"Example 6: Object Construction" (`fq_07_object`) | Book: `let record = { … }; record.score = 99`. HEAD reality: BOTH `cannot assign to immutable binding 'record'` (book uses `let`, not `let mut`) AND `Assignment to 'record.score' requires compile-time field resolution. Generic runtime property lookup is disabled.` Object-literal type inference does not pre-extend the object schema with new fields. | **update-book** — change `let record = { id: 42, label: "alpha" }` to a `type Record { id: int, label: string, score: int }` typed struct AND use `let mut record`. Generic dynamic field-add is not in scope per CLAUDE.md type-system rules |
| §"Save and Run a Script" (`fq_08_summarize`) | VM panics (`IntrinsicMean` same as fq_03). JIT segfaults (`SIGSEGV`, exit 139, "timeout: the monitored command dumped core"). Multiple compounding issues: `values.length` (book) is treated as a property; the registered name is `length()` (method) per `method_registry.rs:275`. `Vec<number>` is the parameter type. | **add-to-language** + **update-book** — fix `IntrinsicMean` (same as fq_03), fix JIT SIGSEGV, change `values.length` → `values.len()` in book |

### basic-concepts.mdx (11 shape snippets)

| Snippet | HEAD reality | Disposition |
|---------|--------------|-------------|
| §1 Everything Is an Expression (`bc_01_expression`) | matches book (prints "B") in both VM and JIT | OK |
| §2 Bindings (`bc_02_bindings`) | VM matches (prints "Alice\n1\n[1, 2, 3]"). JIT fails at `print` with NativeKind=Some(Ptr(TypedArray)) Route A surface-and-stop (same class as fq_04 — `print([1,2,3])` of an array) | **add-to-language** — JIT `jit_print_<TypedArray>` (cluster-2 closure-wave-F continuation) |
| §2 const (`bc_03_const`) | matches in both VM and JIT (prints "3") | OK |
| §3 Objects and Field Access (`bc_04_object`) | Book: `let point = { x: 10 }; point.y = 20`. Both VM and JIT reject: `cannot assign to immutable binding 'point'`. Compiles even further to the same dynamic-field-add issue as fq_07. | **update-book** — use a `type Point { x: int, y: int }` typed struct + `let mut point = Point { x: 10, y: 20 }` |
| §4 Functions Use `fn` distance2 (`bc_05_fn`) | VM matches book (prints "25"). JIT exits with `Runtime error: JIT execution error (code: -1)`. | **add-to-language** — JIT `distance2(3,4)` returning `int` from `x*x + y*y` shouldn't error; cite as JIT residual (cluster-2 territory) |
| §4 Functions lambda (`bc_06_lambda`) | Book: `let add = \|a, b\| a + b`. Both VM and JIT reject: `Cannot infer types for binary operation 'Add': operand types are 'unknown' and 'unknown'. Strict typing requires both operands to have a known concrete type at compile time. Add a type annotation to disambiguate.` Per CLAUDE.md ("Bidirectional closure inference"), inference exists for method-call sites (`arr.filter(\|x\| …)`) — but a bare `let add = \|a,b\| a+b` has no callsite context to inform a/b's types. | **update-book** — change to `let add = \|a: int, b: int\| a + b` OR remove the bare-lambda example AND clarify that closures need callsite or type annotations |
| §5 Pattern Matching describe (`bc_07_match`) | VM matches book (prints "int(7)\nstring(hi)"). JIT SIGSEGV (`code 139`, "the monitored command dumped core"). | **add-to-language** — JIT `match` with `int \| string` union + bare `n: int` and `s: string` patterns segfaults; cite as JIT critical bug (cluster-2 territory) |
| §6 Option/Result (`bc_08_option`) | Book: `from std::core::intrinsics use { Option, Result, Some, Ok }`. Parser rejects with `unexpected identifier 'Some', expected something else`. `intrinsics.shape` has `Option` (line 31) and `Result` (line 29) as `builtin type` and `Ok`/`Err` as `builtin fn` (line 155–157), but `Some`/`None` are NOT exported (no `builtin fn Some` declared; only mentioned in a comment at line 30). | **update-book** + **add-to-language** — either expose `Some`/`None` as builtin functions in `std::core::intrinsics` OR change the import to `from std::core::intrinsics use { Option, Result, Ok }` and use `Option<string>` constructor differently (e.g. `name as string?`). Bigger issue: parser rejects `Some` as an identifier in import lists — this is also a parser bug |
| §7 Imports and Modules namespace (`bc_09_namespace`) | Book: `use std::core::state; let frame = state::capture()`. VM errors with `state.capture: no current frame — state.capture must be called from within a function body. ADR-006 §2.7.4. (line 3)`. JIT errors with `module namespace 'state' is not typed. Missing module schema for export 'capture'`. | **update-book** — wrap call in a function body per ADR-006 §2.7.4; ALSO **add-to-language** for the JIT-side module-schema gap |
| §8 Data Tables (`bc_10_table`) | Book: `let events: Table<Event> = load_events()` — `load_events()` is undefined in stdlib. | **update-book** — either remove the example (deferred to later chapters), use a placeholder comment, or wire to a real example like `from std::core::csv use { read_csv }; let events: Table<Event> = read_csv("events.csv")?` |
| §9 Semicolons (`bc_11_semicolons`) | VM matches book (prints "1\n1" because `print(b())` of `()` prints nothing meaningful but doesn't error). JIT errors with `Route A surface-and-stop … print Call-terminator operand NativeKind is None` — printing `()` (unit) is not wired in JIT print FFI. | **add-to-language** — JIT `jit_print_<Unit>` for unit/`()` |

### repl.mdx (transcript-only, 0 Shape source files)

| Line | Claim | HEAD reality | Disposition |
|------|-------|--------------|-------------|
| 12 | `shape repl` starts an interactive REPL | matches (CLI subcommand exists at `cli_args.rs:107`) | OK |
| 18–22 (and throughout) | REPL prompt is `>>` | **WRONG** — the REPL is a TUI (`bin/shape-cli/src/repl/`) using ratatui (`rendering.rs:198 format!("[{}]", cell.id)`). Cells are `[id]` (green) or `[id]✓` (cyan), NOT `>>`. Output cells have a `▶` or space prefix. | **update-book** — entire transcript visualization needs rewrite; either render actual TUI screenshots or rewrite as Markdown narrative |
| 65–69 | `>> help` and `>> exit` are REPL commands | **WRONG** — TUI command handler at `repl/mod.rs:512–519` only accepts `"q" \| "quit" \| "clear"`, all gated by a leading `:` (per typical TUI command convention; the `cmd` arg is the bare command name). `help` returns "Unknown command". `exit` returns "Unknown command". | **update-book** — list real REPL commands (`:q` / `:quit` / `:clear`) and remove `help` / `exit` |
| 67 (sample REPL flow throughout) | Output appears below prompt; values evaluate inline | matches the TUI cell-based model in spirit, but the textual transcript format is misleading | **update-book** — switch to TUI screenshots or a non-transcript narrative |

### editor-setup-vscode.mdx (no Shape source; only bash + general claims)

| Line | Claim | HEAD reality | Disposition |
|------|-------|--------------|-------------|
| 8 | VS Code marketplace extension `shape-lang.shape-lang` | not verified at audit | **defer-with-cite** if not live |
| 25 | Extension auto-installs `shape-lsp` via cargo if missing | not verified (extension is out-of-tree) | OK if VS Code extension actually does this |
| 53 | "Inlay hints" listed as With-LSP feature | matches CLAUDE.md ("Bidirectional closure inference" mentions LSP inlay hints) | OK |
| 75 | `cargo install shape-lsp --force` to update | matches | OK |

### editor-setup-neovim.mdx (no Shape source; bash + lua + vim)

| Line | Claim | HEAD reality | Disposition |
|------|-------|--------------|-------------|
| 18 | `cargo install shape-lsp` | matches | OK |
| 32–35 | `cd shape/tree-sitter-shape && cc -shared -fPIC -O2 -o parser/shape.so src/parser.c -I src` | `tree-sitter-shape/src/parser.c` exists ✓, `parser/shape.so` is gitignored / produced by build ✓. The `parser/` dir contains only `shape/` (queries subdir) until build. Suggested cc invocation produces the .so. | OK |
| 47 | LazyVim plugin path `dir = vim.fn.expand("~/src/shape/tree-sitter-shape")` | example path; user must adjust | OK (annotated as such) |
| 115–123 | `queries/shape/{highlights,injections}.scm` are auto-discovered | matches (`tree-sitter-shape/queries/shape/highlights.scm` + `injections.scm` exist) | OK |
| 142 | `:set commentstring?` → `// %s` | matches (CLAUDE.md: comment syntax is `//`) | OK |
| 153 | `tree-sitter generate` regenerates `src/parser.c` | matches | OK |

### hello-world.mdx (1 shape snippet, `ex_hello_world`)

| Snippet section | HEAD reality | Disposition |
|-----------------|--------------|-------------|
| `print("Hello, Shape!")` + for loop with f-string interpolation | VM: prints "Hello, Shape!\nHello, world! (1)\n…(2)\n…(3)" ✓; JIT: errors at print Route A surface-and-stop | **add-to-language** — JIT `jit_print` for the `()` unit return of `for` and string-interpolation Ptr kinds |
| `fn greet(name: string) -> string { f"Welcome, {name}!" }` | VM: prints "Welcome, Alice!" ✓; JIT: errors | **add-to-language** |
| `type Person { name: string, age: int }; let alice = Person { name: "Alice", age: 30 }` | VM: prints "Alice is 30 years old" ✓ | OK |
| `match age { x: int where x < 13 => "child" … }` (describe_age) | VM: prints "adult" ✓ | OK |
| `Result<int, string>` + `(s as int?)?` + `Err(f"…")` + `Ok(n)` | VM: errors with `Runtime error: no method 'cmp' on receiver kind Int64 (line 43)`. The `if n < 0 or n > 150` uses `or` keyword and `<`/`>` on `Int64` slots — `cmp` method dispatch isn't wired for the kind | **add-to-language** — `Int64` slot-kind `cmp` method dispatch gap |
| JIT for the entire snippet | fails before VM equivalent at print SURFACE | **add-to-language** |

### web-request.mdx (1 shape snippet, `ex_web_request`)

| Issue | HEAD reality | Disposition |
|-------|--------------|-------------|
| Parser rejects f-string with field access inside | `f"Create failed: HTTP {resp.status}"` — parser errors at `:44:54` with `unexpected '}', expected something else`. The parser appears to mishandle f-string interpolation when the expression contains an unparenthesized field access in certain contexts (or specifically: a `}` after a leading `f"` context inside a function-arg multi-line tuple closing position). | **add-to-language** — parser bug on f-string field access in nested call-arg position |
| Book says: `http is a built-in global — no import needed` (line 64) | **WRONG** — `http` is `std::core::http` (`stdlib-src/core/http.shape`), NOT a global. The prelude (`prelude.shape`) does not auto-import it. Bare `http.get(…)` will not resolve without `use std::core::http`. | **update-book** — add `use std::core::http` and `use std::core::json` to the snippet AND change the "Concepts Demonstrated" claim |
| `json.parse(resp.body, User)` (book ln 26, 58) | **WRONG SIGNATURE** — `json.parse` takes 1 arg (`pub builtin fn parse(text: string) -> Result<_, string>`, `json.shape:44`). The internal `__parse_typed(text, schema_id: float)` is the typed-schema-id variant and is gated (not `pub`). | **update-book** — change to `json.parse(resp.body)?` then pattern-match on `Json::Object(…)` per `json_value.shape`; OR **add-to-language** a public typed `json.parse<T>(text)` overload |
| `http.post(url, body, headers)` (book ln 50) | **WRONG SIGNATURE** — `http` module has `post_text(url, body, options)`, `post_bytes(url, body, options)`, `post_json(url, body, options)` (`http.shape:60/81/146`), NOT a generic `post`. | **update-book** — change to `http.post_text(url, body, { headers: { "Content-Type": "application/json" } })` or `http.post_json(url, body)` |
| `await http.get(…)` | `http.get` returns `Result<_, string>`, not a future; the `await` may be a no-op or error. | **update-book** — verify `await` semantics on `http.*`; may need `use std::core::async` or different syntax |
| `json.stringify({ name: name, email: email })?` (book ln 49) | `json.stringify(value: _, pretty: bool) -> Result<string, string>` requires 2 args (`json.shape:78`). | **update-book** — change to `json.stringify({ name: name, email: email }, false)?` |
| Top-level `let user = (fetch_user(1) !! "Failed to fetch user")?` | `?` operator at top level requires the enclosing function to return `Result`, which a bare script does not | **update-book** — wrap in `fn main() -> Result<(), string> { … }` or match-handle inline |

### comptime-codegen.mdx (1 shape snippet, `ex_comptime_codegen`)

| Issue | HEAD reality | Disposition |
|-------|--------------|-------------|
| Whole snippet | Compiler panics at `comptime_target::nb_object_array` (`crates/shape-vm/src/compiler/comptime_target.rs:281`) with V3-S5 ckpt-5 consumer-cascade tier-3 SURFACE: deleted typed-array-data TypedObject `Arc<Buf<TypedObjectPtr>>` result carrier (W12 audit §3.5 / §B + ADR-006 §2.7.24 Q25.A SUPERSEDED). Rebuild lands at ckpt-6 STRICT. | **defer-with-cite** — comptime-target nb_object_array rebuild is V3-S5 ckpt-6 territory (cite `docs/cluster-audits/w12-typed-array-data-deletion-audit.md`) |
| `annotation serializable() { targets: [type] … }` syntax | not validated past comptime panic; ground-truth syntax check pending W3.2-C | **defer-with-cite** to W3.2-C audit |
| `extend target { method to_json() { … } }` | extend-block code-generation not exercised at HEAD due to comptime panic | **defer-with-cite** |
| `comptime for field in target.fields` | `comptime for` exists per CLAUDE.md but `target.fields` access is unverified | **defer-with-cite** |
| `set return (schema_for_csv(uri.value))` directive | unverified syntax | **defer-with-cite** |

---

## §1' Snippet-runnability matrix

24 Shape snippets executed. Status codes: **runnable-and-correct** / **runnable-but-wrong-output** / **not-runnable** / **documented-without-explicit-output** / **vm-only** (VM passes, JIT fails) / **jit-only** (JIT passes, VM fails) / **both-fail**.

| # | Page | Snippet | VM exit | JIT exit | Status | Actual vs documented | Fix recommendation |
|---|------|---------|---------|----------|--------|----------------------|--------------------|
| 1 | installation.mdx:88 | quick-test hello fn | 0 | 0 | **runnable-and-correct*** | prints `hello from shape` then noisy `{ "Integer": 0 }` JSON for top-level return; book doesn't show expected output | suppress top-level Int rendering OR document it |
| 2 | installation.mdx:105 | let x/y + print(x+y) | 0 | 0 | **runnable-and-correct** | prints `30` (book has no explicit expected) | OK |
| 3 | first-query.mdx:13 | print("Hello, Shape") | 0 | 0 | **runnable-and-correct** | matches | OK |
| 4 | first-query.mdx:18 | negative-index `values[-1]` | 1 | 0 | **both-fail (semantically)** | VM: `Index -1 out of bounds`. JIT: silently returns `0` for `values[-1]` and `values[-2]`. Book promises last/2nd-to-last (i.e. `22, 24`). **Three-way divergence: book vs VM vs JIT.** | implement negative indexing OR remove example |
| 5 | first-query.mdx:31 | `samples.mean/max/min` | 101 | 1 | **not-runnable** | VM: `IntrinsicMean` panic. JIT: print SURFACE | wire `IntrinsicMean` body (phase-1b-vm wave 5d) |
| 6 | first-query.mdx:43 | filter + map | 0 | 1 | **vm-only** | VM matches book. JIT: print SURFACE for `Ptr(TypedArray)` | JIT `jit_print_<TypedArray>` |
| 7 | first-query.mdx:56 | is_anomaly with `let data = …` | 1 | 1 | **not-runnable** | `data` name clashes with compiler data-schema keyword | rename `data` → `series` |
| 8 | first-query.mdx:76 | bucket match guards | 0 | 0 | **runnable-and-correct** | matches | OK |
| 9 | first-query.mdx:91 | record dynamic field-add | 1 | 1 | **not-runnable** | `let` not `let mut`; dynamic field-add disabled | use typed struct + let mut |
| 10 | first-query.mdx:105 | summarize `Vec<number>` | 101 | 139 | **not-runnable** | VM: `IntrinsicMean` panic. JIT: **SIGSEGV** | wire IntrinsicMean; fix JIT SIGSEGV; `values.length` → `values.len()` |
| 11 | basic-concepts.mdx:14 | if-else expression grade | 0 | 0 | **runnable-and-correct** | prints `B` | OK |
| 12 | basic-concepts.mdx:26 | let/let mut/var bindings | 0 | 1 | **vm-only** | VM matches. JIT: print SURFACE for `[1,2,3]` | JIT `jit_print_<TypedArray>` |
| 13 | basic-concepts.mdx:39 | `const MAX_RETRIES = 3` | 0 | 0 | **runnable-and-correct** | prints `3` | OK |
| 14 | basic-concepts.mdx:50 | dynamic field-add `point.y = 20` | 1 | 1 | **not-runnable** | `cannot assign to immutable binding` + dynamic field-add disabled | use typed struct + let mut |
| 15 | basic-concepts.mdx:64 | distance2 fn | 0 | 1 | **vm-only** | VM: `25`. JIT: `JIT execution error (code: -1)` | JIT regression on simple `i*i + i*i` |
| 16 | basic-concepts.mdx:75 | bare lambda `\|a, b\| a + b` | 1 | 1 | **not-runnable** | Strict typing requires types known at compile time | type-annotate or remove |
| 17 | basic-concepts.mdx:84 | match `int \| string` | 0 | 139 | **vm-only** | VM matches. JIT: **SIGSEGV** | fix JIT match-int\|string SIGSEGV |
| 18 | basic-concepts.mdx:100 | Option/Result import + `?`/`!!` | 1 | 1 | **not-runnable** | Parser rejects `Some` in import list; `Some`/`None` not exported | expose `Some`/`None` as `builtin fn` in intrinsics OR change import |
| 19 | basic-concepts.mdx:124 | `state::capture()` at top-level | 1 | 1 | **not-runnable** | Runtime: must be in fn body (ADR-006 §2.7.4). JIT: missing schema for `state.capture` | wrap in fn OR fix module schema |
| 20 | basic-concepts.mdx:138 | `Table<Event> = load_events()` | 1 | 1 | **not-runnable** | `load_events` undefined | remove or wire to real CSV |
| 21 | basic-concepts.mdx:157 | semicolons `fn a/b` | 0 | 1 | **vm-only** | VM: `1\n1`. JIT: print SURFACE for `()` | JIT `jit_print_<Unit>` |
| 22 | hello-world.mdx:13 | full hello-world tour | 1 | 1 | **both-fail** | VM: partial (prints up to `adult`), then `no method 'cmp' on receiver kind Int64` at `parse_age` `if n < 0 or n > 150`. JIT: print SURFACE early. | wire `cmp` on `Int64` slot kind; JIT print |
| 23 | comptime-codegen.mdx:13 | annotation + comptime + extend | 101 | 101 | **not-runnable** | Compiler PANIC: `comptime_target::nb_object_array` V3-S5 ckpt-5 SURFACE | V3-S5 ckpt-6 rebuild (deferred) |
| 24 | web-request.mdx:13 | http + json + async | 1 | 1 | **not-runnable** | Parser rejects f-string field-access in `Err(f"…{resp.status}")`; OR multiple API signature mismatches (`json.parse` 1-arg not 2; `http.post` not a fn; `await` semantics; `?` at top level) | rewrite snippet with correct stdlib API |

* "runnable-and-correct" with caveat that documented output is implicit.

**Summary stats:**

| Category | Count |
|---------|------:|
| runnable-and-correct | 6 (1, 2, 3, 8, 11, 13) |
| vm-only (JIT fails — print SURFACE / SIGSEGV / JIT regression) | 5 (6, 12, 15, 17, 21) |
| not-runnable in both tiers | 11 (5, 7, 9, 10, 14, 16, 18, 19, 20, 23, 24) |
| both-fail with different outputs | 2 (4 three-way, 22 partial-vm) |
| jit-only | 0 |

**Out of 24 snippets, only 6 (25%) execute correctly in both VM and JIT at HEAD.**

---

## §2 Source-fix scoping decision

This audit makes **NO source changes**. Disposition counts:

| Disposition | Snippets |
|-------------|---------:|
| update-book (book describes wrong syntax / wrong API / wrong feature) | 13 |
| add-to-language (real feature gap that should be fixed; VM-only / JIT-only mostly) | 13 |
| defer-with-cite (legitimately a v0.4 or known-residual) | 4 |
| delete-page-content (book describes a removed feature) | 0 |

Many snippets have multiple dispositions because they have multiple defects (e.g. snippet 22 is `both-fail` with one update-book and one add-to-language).

**W3.2-C fix-wave dispatch (recommendation):**

Group A — pure doc fixes (no language change required):
- repl.mdx full rewrite: TUI screenshots or narrative; remove `>>` prompt + `help`/`exit` claims; document `:q` / `:quit` / `:clear`
- first-query.mdx: rename `data` → `series` in is_anomaly example; `values.length` → `values.len()` in summarize; remove or annotate the negative-index claim
- basic-concepts.mdx: convert object-literal mutation examples to typed-struct + `let mut`; remove or type-annotate bare-lambda; wrap `state::capture()` in fn body; remove or rewire `load_events()`; fix Option/Result import
- web-request.mdx: full rewrite — `use std::core::{http, json}`; `http.post_text` / `http.post_json` (not `http.post`); `json.parse` 1-arg; `json.stringify(..., false)`; wrap top-level `?` in `fn main() -> Result<…>`; verify `await` semantics

Group B — language fixes (W3.2-C-lang sub-wave):
- JIT per-HeapKind `jit_print_<TypedArray>` (cluster-2 closure-wave-F continuation; 22/35 remaining per CLAUDE.md "v1 candidates") — unblocks snippets 6, 12, 22, hello-world JIT
- JIT `jit_print_<Unit>` — unblocks snippet 21
- JIT SIGSEGV on match `int|string` (snippet 17) — investigate critical
- JIT SIGSEGV on summarize with `Vec<number>.length` (snippet 10) — investigate critical
- JIT regression on `distance2(3,4)` (snippet 15) — investigate
- `IntrinsicMean` runtime body (`crates/shape-vm/src/executor/vm_impl/builtins.rs:525`) — phase-1b-vm wave 5d follow-up
- `Some`/`None` exposure in `std::core::intrinsics` (or fix parser to accept `Some` in import lists)
- `Int64` slot-kind `cmp` method dispatch (snippet 22 / parse_age)
- f-string parser bug on field access in call-arg position (snippet 24)
- module schema for `state::capture` on JIT path (snippet 19)
- top-level Int return rendering (`{ "Integer": 0 }`) — suppress or document (snippet 1)

Group C — defer-with-cite:
- VS Code marketplace + crates.io publish status (installation.mdx, vscode.mdx) — pre-v0.3 ship-gate
- comptime-codegen.mdx whole snippet — V3-S5 ckpt-6 rebuild territory (W12 audit)
- negative array indexing — design call (add to language vs strike from book)

---

## §3 Defection-attractor refusal check

Audit binding refusal log (CLAUDE.md "Forbidden Patterns" / "Renames to refuse on sight"):

- **NO** "snippet broken at HEAD but probably works on author's machine" disposition issued. Every snippet evaluated via actual `./target/release/shape run --mode {vm,jit}` invocation at this audit's binary.
- **NO** "ValueWord shim retained for FFI boundary" framing.
- **NO** "decode bridge / tag bridge / kind-injection helper" framing in any §1 disposition. Every "JIT print Route A surface-and-stop" is named verbatim from the actual runtime error message at HEAD (per `print` Call-terminator NativeKind classification at `§2.7.5`); the gap is per-HeapKind kinded `jit_print_<heap_kind>` FFI body (per CLAUDE.md "v1 candidates" 22/35 remaining), which is the LANDED rebuild shape, not a deleted defection.
- **NO** "small fallback for this one edge case" rationalization. The pre-Round-8A kind-blind `jit_print` fallback was retired per the verbatim runtime error message — surfacing as-shipped, not rationalizing.
- **NO** parallel-implementation framing.
- **NO** Bool-default suggestion in any disposition.
- **NO** anti-deferral defection (#10) — every "defer-with-cite" disposition cites a SPECIFIC destination (V3-S5 ckpt-6 / cluster-2 closure-wave-F / phase-1b-vm wave 5d / pre-v0.3 ship-gate), not a hand-wave defer.

---

## §4 Smoke-matrix preservation

This audit makes no source changes. Smoke matrix at HEAD `89857260` per phase-3-cluster-0-status.md cluster-1.5-close subsection: 5/5 VM == JIT preserved. Audit-only ceremony does not perturb. Verified via:

- `cargo check --workspace --lib --tests` EXIT=0 (no source changed)
- `verify-merge.sh` 12/12 PASS
- `check-no-dynamic.sh` EXIT=0
- `git diff --stat 89857260 -- crates bin tools extensions` is empty

---

## §5 Imprecisions surfaced (3 NEW for slice-C audit; no prior cumulative count modified by this slice)

1. **W3.1 slice C imprecision-1 (audit-scope):** dispatch text described "every code snippet in tutorial/getting-started book pages MUST be executable at HEAD". At HEAD, **18 of 24 (75%) Shape snippets fail to run cleanly in both tiers**. The dispatch's implicit assumption that the book is mostly-correct-and-needs-spot-fixes is REFUTED — the book is mostly-wrong-and-needs-systematic-rewrite for the tutorial pages. Same imprecision-pattern as cluster-1.5 v2-raw audit: static-analysis hypothesis (book is close to right) cannot anticipate the empirical truth (book is far from right).

2. **W3.1 slice C imprecision-2 (territory-overlap):** dispatch said "Other slices: A (fundamentals/+advanced/), B (stdlib/), D (advanced/ AI subset), E (tooling/+advanced/perf subset)" — but `editor-setup-vscode.mdx` and `editor-setup-neovim.mdx` are under `getting-started/` (this slice) yet are 100% tooling (LSP + tree-sitter), which is slice E territory. Two slices may produce overlapping dispositions for editor setup. Flag for supervisor: route LSP / tree-sitter / editor-extension dispositions from slice-C-editor-setup to slice-E to avoid duplicate fix-waves.

3. **W3.1 slice C imprecision-3 (verify-merge.sh CHECK 1 vs CLAUDE.md `just check-clean`):** the dispatch's close gate says `cargo check --workspace --lib --tests` EXIT=0 but CLAUDE.md "Canonical build gate" says `just check-clean` runs `cargo check --workspace --lib --bins --tests --examples`. Slice-C used the dispatch's narrower gate (no `--bins --examples`). Verify-merge.sh CHECK 1 uses the wider gate per CLAUDE.md and passes (`-> CLEAN`). No execution impact, but the dispatch text drifts from the canonical gate definition; supervisor may want to align W3.1 dispatch template to canonical.

---

## §6 ADR-006 / CLAUDE.md modifications surfaced

**NONE** for this audit. The disposition entries for `IntrinsicMean` / `jit_print_<heap_kind>` / V3-S5 ckpt-6 are all citations to EXISTING ADR-006 / CLAUDE.md territory (named explicitly in the source error messages); no new ADR amendment or CLAUDE.md addition required for the audit itself. Any source fixes in W3.2-C may surface new ADR territory — out of scope for the audit-only ceremony.

CLAUDE.md "Known Constraints" candidate additions surfaced (flag-only — NOT landed without user ratification):
- "`json.parse` 1-arg public + `__parse_typed` 2-arg gated" — book consistently uses 2-arg signature; could add a Known Constraint disambiguating public vs internal
- "`Some`/`None` not exported from `std::core::intrinsics` — parser also rejects identifier `Some` in import lists" — Known Constraint candidate AND parser-bug candidate
- "REPL is a TUI with `:q` / `:quit` / `:clear` commands — NOT a stdin-line REPL with `>>` prompt + `help`/`exit`" — fundamental architecture-vs-book mismatch worth a Known Constraint

---

## §7 Ceiling-c bound check + D-α status

- 0 source LoC changed
- ~580 lines of deliverable .md (this file)
- 1 AGENTS.md row appended
- 24 snippet .shape files produced under `/tmp/w3c-snippets/` (transient; not committed)

Well within ceiling-c (~100-site ceiling). **D-α status:** single-session audit close; no multi-session chain required.

---

## §8 Close-gate manifest

| Gate | Result |
|------|--------|
| `cargo check --workspace --lib --tests` | EXIT=0 |
| `bash scripts/verify-merge.sh > /tmp/w3-1-c-vm.out 2>&1` | SCRIPT_EXIT=0 + 12/12 PASS |
| `bash scripts/check-no-dynamic.sh` | EXIT=0 |
| Deliverable §0–§8 + §1' snippet-runnability matrix | ✓ populated |
| Source changes | NONE (audit-only verified via `git diff --stat 89857260 -- crates bin tools extensions`) |
| AGENTS.md row | appended |
| `Co-Authored-By: Claude` trailer | NONE |
