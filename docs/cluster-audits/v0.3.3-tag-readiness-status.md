# v0.3.3 TAG-READINESS STATUS — FINAL VERIFY (finish-correctness round)

Worktree: `shape-strict-flip-collection-dispatch` @ `f2e364a0`
Base of this round: `3bdeb579` (cumulative strict-flip collection-dispatch FINAL VERIFY; 5/6 roots FIXED, +19 tests, sf-NEW=0)
Date: 2026-06-14
Method: per-binary `--test-threads=1` (deterministic). Smoke VM==JIT via release binary.

Commits on top of base 3bdeb579 (4):

| Commit | What |
|---|---|
| `fcb7210e` | R1 empty-array-push: resolve empty-array element type via `.push` at inference + module-scope self-push without SIGSEGV |
| `59550fd2` | R3-subcase struct-array HOF: record named-struct element ConcreteType at array-of-structs construction so HOF closure field access resolves |
| `acbfb1e0` | strict-flip string-concat: reject `string + non-string` at compile time (6 TP rebaselines) |
| `f2e364a0` | conservative clippy-hygiene warnings sweep (behavior-neutral, pre-tag housekeeping) |

---

## §1 Preservation gates — ALL HELD

| Gate | Base 3bdeb579 | HEAD f2e364a0 | Status |
|---|---|---|---|
| numeric_conversions (`--test-threads=1`) | 104 / 0 | **104 / 0** | PRESERVED |
| smoke_test binary (shape-test, `--test-threads=1`) | 8 / 0 | **8 / 0** | PRESERVED |
| smoke s1–s5 VM==JIT (release binary) | 5/5 | **5/5** | PRESERVED (s1=4950, s2=30, s3=x, s4=2, s5=x; all converge VM==JIT) |
| borrow_refs (`--test-threads=1`) | 215 / 4 | **215 / 4** | PRESERVED (4 = documented Seam-2 residual, unchanged names) |
| `just check-clean` | EXIT 0 | **EXIT 0** | GREEN |
| `scripts/check-no-dynamic.sh` | EXIT 0 | **EXIT 0** | GREEN |
| `scripts/verify-merge.sh` | 13/13 | **13/13** | GREEN |

borrow_refs 4 residual (unchanged): `complex::complex_array_pop_via_index`,
`drop::test_drop_for_empty_iterable`, `infer::infer_array_passed_to_read_then_mutate`,
`ref_params::test_ref_return_binding_preserves_reference_identity`.

---

## §2 sf-NEW = 0 (no NEW regression vs base 3bdeb579)

- **arrays_vectors:** 328 pass / 59 fail at HEAD. R1 commit added 2 NEW tests
  (`test_array_push_to_empty_module_scope`, `test_array_push_to_empty_loop_accumulator_squares`),
  both PASS; the R1 inference fix additionally cleared pre-existing empty-array-push
  FP failures (base-fail → HEAD-pass). The 59 residual failures are pre-existing
  out-of-scope empty-array classes (F3 push/concat/flatten accumulators, `take`/`drop`/
  `flatten`/`includes`/`for_in_empty`, V3-S5 op_new_array/range SURFACE, OOB runtime
  semantics) — NONE is the R1 `.push`-self-accumulator class. Monotonic, no regression.
- **shape-vm lib:** 1867 pass / 259 fail at HEAD — failing-set byte-identical to the
  pre-string-concat baseline (commit `acbfb1e0` documented exactly 1867/259). The
  string-concat rebaselines (`emit_binary_op_string_string_add_emits_string_concat_typed`,
  `compound_assign_string_concatenation`, helpers.rs rejection asserts) PASS. The 259
  failures are pre-existing on this branch.
- **string-concat rebaselines are TP, not regressions.** `string + int/number/bool` is
  now a clean compile error (run-verified, both operand orders, var+var and literal forms),
  with the diagnostic pointing to f-string interpolation; `string + string` still works;
  f-string interpolation works. These are correctness wins (TP-rejections), not FP regressions.

**sf-NEW = 0 HELD.** No checker weakened; no `ValueWord` / dynamic-fallback / Bool-default
reintroduced; `int` and `number` do not unify; lossless-only numeric preserved.

---

## §3 Round fixes — run-verified

| Fix | Verification | Result |
|---|---|---|
| **R1 empty-array-push (module scope)** | `let mut a=[]; a=a.push(42); a.first()` ⇒ 42; release-binary stress loop (5000× module-scope self-push) ⇒ length=5000, first=0, a[4999]=24990001, exit 0 in BOTH VM and JIT | **FIXED — no module-scope SIGSEGV under stress** |
| **R3-subcase struct-array HOF** | `pts.map(\|p\| p.x + p.y)` over array-of-structs ⇒ first=3, last=11 (VM==JIT; JIT logs documented W11-jit-new-array SURFACE fallback → interpreter, agrees with VM) | **FIXED — single-method run-verified** |
| **string-concat reject** | `string + int` ⇒ compile error w/ f-string hint; `string + string` ⇒ "ab"; f-string ⇒ "n=5" | **FIXED — run-verified** |
| **Warnings sweep** | behavior-neutral; check-clean EXIT 0; touched crates' compiler warnings cleared; no codegen change | **FIXED — verified** |

**SURFACED (NOT forced):** chained `filter().map()` / `map().reduce()` over struct-arrays
remains a compile error (`unknown` operand types / empty-`result`-element-type). This was
correctly SURFACED, not patched with broad suppression — per the F7/R1-SIGSEGV lesson.

---

## §4 R1 module-scope SIGSEGV — CONFIRMED CLEAN

Release-binary stress (`/tmp/r1_stress3.shape`, 5000-iteration module-scope `a = a.push(i*i)`
over a bare empty array, no enclosing function):

```
VM:  length=5000  first=0  a[4999]=24990001  exit 0
JIT: length=5000  first=0  a[4999]=24990001  exit 0  (documented FrameDescriptor SURFACE → interpreter)
```

No exit-139 / SIGSEGV. The prior prototype's module-binding-slot-reads-None crash is resolved
(element type resolves so the module-binding slot is constructed with the proven element kind).

---

## §5 cargo clippy — PRE-EXISTING DENY-LEVEL ERRORS SURFACED (not introduced by this round)

`cargo clippy --workspace --all-targets -- -D warnings` (the `just clippy` recipe) is **RED**,
but every failure is a **pre-existing structural `deny`-by-default lint** that predates base
3bdeb579 and is unrelated to the strict-flip work:

| Lint | Site | Origin (git-blame) | Touched this round? |
|---|---|---|---|
| `not_unsafe_ptr_arg_deref` | `crates/shape-value/src/heap_value.rs:2967` (`TraitObjectStorage::from_parts`) | `d8bf9a6f` (Phase 3 cluster-0) | NO |
| `not_unsafe_ptr_arg_deref` | `extensions/python/src/lib.rs:42`, `extensions/typescript/src/lib.rs:42` (`language_runtime_plugin!` macro) | pre-existing (untouched crates) | NO |
| `mut_from_ref` | `crates/shape-gc/src/allocator.rs:210` (`regions_mut`) | pre-existing (file unchanged since base) | NO |

These were correctly **left out of scope** by the warnings-sweep commit because fixing them
would change public signatures / behavior (marking functions `unsafe`, changing borrow shape).
Per the F7/R1-SIGSEGV lesson, surfacing the pre-existing deny-level state is correct; forcing a
signature-changing fix in a finish-correctness round is not. The round introduced ZERO new
clippy errors or warnings; touched-crate warnings were swept (shape-value 0 warnings).

**Disposition required from supervisor:** whether the pre-existing clippy deny-level errors
(3 sites) block the v0.3.3 tag or are tracked as a separate clippy-hygiene lane for v0.4.

---

## §6 Tag-readiness — strict-suite correctness assessment

Across the v0.3.3 finish-correctness surface:

| Lane | Status |
|---|---|
| strict suite (collection-dispatch group) | 5/6 roots FIXED + R1 + R3sub; FP-fixes monotonic, sf-NEW=0 |
| numeric conversions | 104/0 — lossless-only, int≠number, explicit-cast-for-lossy — COMPLETE |
| reference serialization (ref-ser) | escape-RC-promote + serialize-with-identity landed (prior rounds); preserved |
| W17 deep-restore | core DoD met (non-empty call_stack + module_bindings, run-proven); K3 SURFACED pending ADR (see §7) |
| s5 (V3-S5 / trait-carrier) | 3/3 CONFIRMED VM==JIT; W12 deletion done; Seam-1/2 done |
| FP-fixes | collection-dispatch group cleared; string-concat TP-rejection added |
| R1 (empty-array-push module-scope) | FIXED, no SIGSEGV under stress |
| R3sub (single-method struct-array HOF) | FIXED, run-verified |
| string-concat | FIXED, run-verified (reject + f-string + string+string) |

**Assessment: v0.3.3 is correctness-COMPLETE across the strict suite, numeric, ref-ser, W17
(core DoD), s5, the FP-fixes, R1, R3sub, and string-concat — modulo the documented v0.4
known-limitations (§7) and two supervisor dispositions (§5 clippy deny-level + §7 K3 ADR).**

---

## §7 Documented KNOWN-LIMITATIONS for v0.4

Feature-tail, out-of-scope sibling classes, surfaced (all clean SURFACE — `NotImplemented` /
compile-error, no shim, no Bool-default, no dynamic fallback):

1. **Lazy iterator protocol** (`iter().map/filter/skip/take/enumerate/chain/collect`) —
   distinct feature class; iterators binary residual.
2. **HOFs — chained / closure-returning / curry / compose / mutable-capture** — closures-S1/c8
   class (`project_v0_3_3_c8_rescoping.md`). Includes chained `filter().map()` / `map().reduce()`
   over struct-arrays (SURFACED this round, not forced).
3. **string-stdlib methods** (`split` / `substring` / `pad` / `join`) — stdlib SURFACE.
4. **JIT v2 FrameDescriptor / new-array element-kind threading** — W11-jit-new-array +
   R8W6 FrameDescriptor SURFACE (ADR-006 §2.7.14); JIT falls back to interpreter, VM==JIT agreement
   preserved. Tracked: `v0.3-r8w6-jit-typedarray-push-framedescriptor-audit.md`.
5. **W17 deep-restore tail** — resume-via-VmState non-empty `frames` (FrameState schema lacks
   `return_ip`/`locals_base`/`locals_count`), resume-IP (no VmState IP field), loop/timeframe/
   exception stacks. Native `snapshot()` multi-frame path already round-trips.
6. **OOB / negative-index runtime semantics under strict typing** (`runtime_err_*_returns_null`,
   `complex_array_pop_via_index`) — runtime-semantics class, OOS.
7. **Pre-existing clippy deny-level lints** (§5) — 3 sites; clippy-hygiene lane.

### K3 ADR — FLAGGED, SUPERVISOR-PENDING

**`HashMapStringHeapValue` = K3** (the polymorphic-value HashMap) is the **sole keystone arm
SURFACED pending an ADR amendment.** It needs the **ADR-006 `HashMapData` kinded-value-track
amendment** — a parallel `Vec<NativeKind>` over HashMap *values* — before it can carry
`Arc<HeapValue>` payloads without a Bool-default kind (which Forbidden Patterns prohibits).
Surfaces in both the K1 module-return projector (`project_concrete_return`) and the snapshot
`slot_to_serializable` HashMap arm. Proven-clean by `hashmap_string_heap_value_surfaces_clean_k3`.
Until the ADR lands, only `HashMap<string,string>` round-trips through deep-restore; every other
value type **surfaces clean** (no corruption, no shim). Ref: `w17-deep-restore-verify-status.md`
§3/§4, `v0.3-r8w6-hashmap-key-kind-audit.md`.

**This is an ADR-level value-model decision and is left for supervisor disposition — NOT
auto-introduced** (per ADR-006 §Q9 / Forbidden-Patterns: no Bool-default, no kind fabrication).

---

## §8 Verdict

v0.3.3 finish-correctness round is **tag-ready on the correctness axis**: all preservation gates
held (numeric 104/0, smoke 5/5 VM==JIT, borrow_refs 215/4, check-clean / check-no-dynamic EXIT 0,
verify-merge 13/13), sf-NEW=0, R1/R3sub/string-concat FIXED and run-verified, no module-scope
SIGSEGV under stress, and no `ValueWord` / dynamic-fallback / Bool-default reintroduced.

Two items require supervisor disposition before tag:
1. **Pre-existing clippy deny-level errors** (§5) — block-or-defer-to-v0.4 decision.
2. **K3 `HashMapData` kinded-value-track ADR** (§7) — the one keystone arm SURFACED clean,
   pending ADR amendment.

All other known-limitations are documented v0.4 feature-tail (iterators, HOFs, string-stdlib,
JIT FrameDescriptor, W17 resume-frames tail), each SURFACED clean.
