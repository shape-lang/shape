# FINAL VERIFY — strict-flip migration-debt close + s5 gate

Worktree: `shape-strict-flip-collection-dispatch` @ `4fe8afea`
Base: `1b66dd8f` (cumulative strict-flip; V3-S5 Cascade)
Date: 2026-06-06
Method: per-binary `--test-threads=1` (deterministic; parallel `-P4` is flake-inflated). Base counts from a detached worktree at `1b66dd8f` (`shape-sf-base-verify`), same `--test-threads=1` harness.

Commits on top of base (7): F5 match-payload-binder fix (`efea510d`) + 5 TP-migration test commits + 1 docs.

---

## §1 Six binary-groups — before (base 1b66dd8f) vs after (HEAD) — deterministic

| Group | Binary | BASE pass/fail | HEAD pass/fail | Δpass | Class of movement |
|---|---|---|---|---|---|
| G1-arrays | arrays_vectors | 299 / 86 | **324 / 61** | **+25** | TP-migrate (untyped-[]→annotate, hetero→neg, homogeneous-lit) |
| G1-arrays | objects_arrays | 67 / 26 | **71 / 22** | **+4** | TP-migrate |
| G2-iter | iterators | 49 / 116 | 49 / 116 | 0 | untouched (lazy-iterator-protocol class — out of scope) |
| G2-iter | window_functions | 1 / 7 | **8 / 0** | **+7** | TP-migrate + harness helper (fully cleared) |
| G3-closures | closures_hof | 319 / 137 | 319 / 137 | 0 | untouched (closures-S1/c8 + mutable-capture class — out of scope) |
| G4-inference | type_inference | 265 / 30 | **268 / 27** | **+3** | TP-migrate |
| G5-stress | complex_integration | 49 / 51 | **50 / 50** | **+1** | TP-migrate (2 cross_feature; net +1 vs compound roots) |
| G6-misc | control_flow | 468 / 12 | **469 / 11** | **+1** | TP-migrate (1 loops) |
| G6-misc | literals | 183 / 1 | 183 / 1 | 0 | untouched |
| G6-misc | strings_formatting | 180 / 23 | 180 / 23 | 0 | untouched (string-method stdlib SURFACE — out of scope) |
| G6-misc | error_handling | (slow*) | 4 fail (323-area) | n/a | migration touched 1 diagnostics test; 4 fails OOS (const-eval + OOB-returns-null) |

**Total cleared across the 6 groups: +41 passes** (av 25 + oa 4 + win 7 + ti 3 + ci 1 + cf 1). Monotonic: every binary HEAD-pass ≥ BASE-pass; no binary regressed.

\* error_handling diagnostics binary is anomalously slow / aborts on a runtime test under
`--test-threads=1` at BOTH base and HEAD (known `project_bulk_test_hang.md` per-test resource
accumulation; the `runtime_err_*_returns_null` OOB tests). Exact HEAD pass count not captured
(non-decisive). The 4 HEAD failures (`const_complex_expression`, 3× `runtime_err_*_returns_null`)
are pre-existing OOS, not migration-introduced.

### FixRoots F5 (compiler fix `efea510d` — match-payload binder type stamp)
| Binary | pre-F5 (commit-claimed) | HEAD | Δ |
|---|---|---|---|
| enums | 353 | **367** | +14 |
| pattern_matching | 200 | **202** | +2 |

F5 stamps Ok/Some/Err match-arm payload-binder ConcreteType from the proven scrutinee type
(verbatim, no fabrication). Strict-safe: string-payload×int still rejects (Numeric trait).

---

## §2 sf-NEW = 0 (no NEW regression vs base 1b66dd8f)

**Method:** per-binary set-diff of FAILED test names, HEAD vs base, restricted to the 5 churned
binaries (arrays_vectors, objects_arrays, complex_integration, type_inference, control_flow).

```
comm -23 (HEAD failures) (BASE failures)  →  EMPTY
```

**Every HEAD failure name is also a base failure name.** All movement is base-failures→HEAD-passes;
zero passes flipped to failures. Reverse diff (BASE\HEAD = fixed): av 26, oa 5, ti 4, ci 2, cf 2 —
matches the Δpass deltas.

- window_functions: 1/7 → 8/0 (fully cleared, trivially no new failures).
- iterators / closures_hof / literals / strings_formatting: pass+fail counts IDENTICAL base↔HEAD,
  test files byte-untouched → identical failure sets → sf-NEW=0.

**sf-NEW = 0 HELD. FP-roots cleared for the collection-dispatch group (no checker weakened —
test-only migrations + one additive compiler fix).**

---

## §3 Preservation gates

| Gate | Result |
|---|---|
| numeric_conversions (`--test-threads=1`) | **104 / 0** PRESERVED |
| smoke_test binary (`--test-threads=1`) | **8 / 0** |
| smoke s1–s5 VM==JIT (release-binary corrected harness) | **5/5** (s1=4950, s2=30, s3=x, s4=2, s5=x) |
| borrow_refs (`--test-threads=1`) | **215 / 4** PRESERVED (4 = documented Seam-2 residual) |
| `just check-clean` | **EXIT 0** |
| `scripts/check-no-dynamic.sh` | **EXIT 0** |
| `scripts/verify-merge.sh` | **13/13 PASS** |

borrow_refs 4 residual = `complex::complex_array_pop_via_index`, `drop::test_drop_for_empty_iterable`,
`infer::infer_array_passed_to_read_then_mutate`, `ref_params::test_ref_return_binding_preserves_reference_identity`
(unchanged baseline).

---

## §4 s5 close-gate — 3/3 CONFIRMED

s1–s5 fixtures (`tests/smokes/`) run through release-binary corrected harness
(`out=$(... 2>&1); ec=$?; tail`), `--mode vm` vs `--mode jit`:

| Smoke | VM | JIT | VM==JIT |
|---|---|---|---|
| s1 (loop-sum) | 4950 | 4950 | YES |
| s2 (map.sum) | 30 | 30 | YES |
| s3 (trait method) | x | x | YES |
| s4 (Set.len) | 2 | 2 | YES |
| s5 (Array<dyn HasX>) | x | x | YES |

All 5 converge VM==JIT, matching the documented W16.2-B trait-carrier values exactly. This is the
3rd independent confirmation owed by `v0.3-w16-2-b-v3s5-close-verify.md` §2.

**s5 gate: 3/3 CONFIRMED.**

---

## §5 Honest remainder (NOT masked)

The large residual failure counts at HEAD are the pre-existing strict-flip FP baseline whose roots
are SIBLING classes out of the collection-dispatch group's scope — present identically at base
1b66dd8f. Classified from FAILED-name inspection:

1. **iterators (116) — out-of-scope, distinct feature class.** Lazy iterator protocol
   (`iter().map/filter/skip/take/enumerate/chain/collect`). Untouched (base==HEAD). Separate root.
2. **closures_hof (137) — out-of-scope, closures-S1/c8 + mutable-capture class.** Closure-returning-
   closure, HOF-compose/curry, mutable-capture accumulators, lambda-pipe. Untouched (base==HEAD).
   Tracked under `project_v0_3_3_c8_rescoping.md` (closures ≠ collection-dispatch).
3. **arrays_vectors residual (61) — mixed, mostly OOS/blocked + F3-roots.** F3 push/concat/flatten-
   via-reassign accumulators (skipped FP-roots, per `c5880de8`), V3-S5 op_new_array/range SURFACE,
   OOB/negative-index runtime semantics (OOS), `contains` unstable-message (blocked).
4. **complex_integration (50) — compound.** Large programs combining the iterator + closure + F3-push
   roots above; clear when the upstream roots clear.
5. **type_inference (27) — out-of-scope, `type()`-method + string-split inference-loss class.**
6. **strings_formatting (23) — out-of-scope, string-method stdlib SURFACE** (split/substring/pad).
7. **error_handling (4) — OOS.** const-eval `const_complex_expression`; 3× OOB-returns-null runtime
   semantics under strict typing.
8. **literals (1) — None-literal class** (`none_literal`; let-gen/generic-types-require-<T>).

None of (1)–(8) are collection-dispatch FP-roots and none are NEW regressions. They are genuine
residual / out-of-scope-v0.4 / blocked — owned by their own root lanes, not this migration.

---

## §6 Disposition

- 6 binary-groups: **+41 passes cleared** across the collection-dispatch group; F5 FixRoots adds
  enums +14 / pattern_matching +2. Monotonic, no binary regressed.
- **sf-NEW = 0** rigorously held (per-binary HEAD-failures ⊆ base-failures; empty forward diff).
- Preservation: numeric 104/0, smoke 8/8 + s1–s5 5/5, borrow_refs 215/4 — all PRESERVED.
- Gates: check-clean EXIT 0, check-no-dynamic EXIT 0, verify-merge 13/13.
- **s5 gate: 3/3 CONFIRMED** (VM==JIT convergence on all smokes; W16.2-B trait carrier).
- No checker weakened; no `ValueWord` / dynamic-fallback / Bool-default reintroduced.
- Honest remainder surfaced: residual failures are pre-existing sibling-class FP (iterator-protocol,
  closures-S1, string-stdlib, OOS runtime-index-semantics), not collection-dispatch roots.
