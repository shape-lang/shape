# STAGE Verify — V3-S5 close + s5 close-gate (W16.2-B trait-object carrier)

**Worktree:** `/home/dev/dev/shape-lang/shape-strict-flip-collection-dispatch`
**HEAD:** `1b66dd8f` (V3-S5 Cascade — map/filter/flatMap + set-ops native consumers
via per-T monomorphization; binary-op element-type inference). One commit ahead of
the stated Seam #2 base `d6256f02`; the intervening commits are
`e01d29a1` (V3-S5 Construction), `43d70eef` (W16.2-B op_new_array trait-object close),
`1b66dd8f` (V3-S5 Cascade).
**Date:** 2026-06-05. **Verifier:** single agent (3-INDEPENDENT-AGENT confirmation still OWED — see §2).

---

## §1 V3-S5 corpus — construction/method surfaces PASS or compile-reject-as-TP

The V3-S5 construction/method corpus is the set of array construction + collection-method
test surfaces lifted across the shape-test feature suites by the Construction + Cascade
landing. Run deterministically (`--test-threads=1`), the post-landing pass counts match
the Cascade commit's run-verified deltas exactly:

| Suite | Pass (deterministic) | Cascade-cited post-landing | Match |
|---|---|---|---|
| `functions` | 214 | 211→214 | YES |
| `arrays_vectors` | 299 | 281→299 | YES |
| `iterators` | 49 | 44→49 | YES |
| `closures_hof` | 320 | 299→319 | YES (+1) |
| `objects_arrays` | 67 | 66→67 | YES |
| `complex_integration` | 49 | 47→49 | YES |

These deltas (+3/+18/+5/+20/+1/+2 ≈ the ~86-surface V3-S5 construction/method corpus)
are the construction/method surfaces that now PASS post-landing. `functions` lands 214/0
(the V3-S5 construction-close-annotated `multi_return_array` / `test_fn_returns_array` /
`test_fn_returns_array_access` all run-verified). c11 corpus seed
(`collections/c11_array_typed_object.shape`, `Array<TypedObject>` element) PASSes VM==JIT → `2`.

**borrow_refs (Seam #2):** 215 passed / 4 failed — UNCHANGED from the Seam #2 baseline.
Residual 4: `complex::complex_array_pop_via_index`, `drop::test_drop_for_empty_iterable`,
`infer::infer_array_passed_to_read_then_mutate`,
`ref_params::test_ref_return_binding_preserves_reference_identity`. Not regressed.

**Remaining surfaces (residual, NOT V3-S5 construction regressions):** the
non-passing tests in `arrays_vectors` (86), `iterators` (116), `complex_integration` (51),
`objects_arrays` (26) are the documented pre-existing strict-flip FP inference-loss
cluster (~1105 FP / ~6-8 checker roots: `operand types are unknown` Add/Sub) plus the
remaining §1.B ckpt-N consumer-cascade J-wave surfaces (flatten/groupBy/joins/datatable/
priority-queue tail per the W12 audit S1/S2/S3 cadence). These are distinct root-cause
families tracked on their own cadence; none introduced by the V3-S5 construction/cascade
landing. The 4 `simulation.rs` sim tests' `#[ignore]` reasons were reclassified away from
`op_new_array` to "Generic-function inference loss" — the construction SURFACE is gone;
their residual is the pre-existing generic-fn inference cluster.

---

## §2 s5 CLOSE-GATE — Array<dyn HasX> in VM and JIT

Fixture `tests/smokes/s5.shape`:
```
trait HasX { method x_str() -> string; }
type Bar { v: int }
impl HasX for Bar { method x_str() -> string { "x" } }
let arr: Array<dyn HasX> = [Bar { v: 1 }, Bar { v: 2 }]
print(arr[0].x_str())
```

Release-binary corrected harness (`out=$(... 2>&1); ec=$?; tail -1`):

| Smoke | VM | JIT | VM==JIT |
|---|---|---|---|
| s5 | `x` (ec=0) | `x` (ec=0) | **YES** |

CONVERGENCE met. The W16.2-B trait carrier path is exercised in BOTH modes:
- VM: `TypedArrayKind::TraitObject` → `OpCode::NewTypedArrayTraitObject` /
  `TypedArrayGetTraitObject` / `TypedArrayPushTraitObject`
  (`compiler/v2_typed_emission.rs:113,147,174,201`), runtime handlers in
  `executor/v2_handlers/typed_array_elem.rs`, carrier
  `TypedArray<*const TraitObjectStorage>` (per-T monomorphization; §2.7.24 Q25.C
  cited at `opcode_defs.rs:790-804`).
- JIT: `jit_new_typed_array_trait_object` / `jit_v2_array_new_trait_object` +
  `jit_v2_array_get/set_trait_object` (`ffi/v2/mod.rs:505-528`, kind_label
  `"trait_object"`, ELEM_TYPE_TRAIT_OBJECT stamp). NO Arc<TypedArrayData> /
  TypedBuffer<T> — single `Ptr(HeapKind::TraitObject)` carrier.

**OWED:** the formal 3-INDEPENDENT-AGENT confirmation (W12 close §1.1 / W16.1 audit §5.B
step 4 — single-agent corrected-harness run is insufficient evidence) is STILL OWED.
This verify is one (1) of three required independent confirmations.

---

## §3 Gates

| Gate | Result |
|---|---|
| numeric_conversions (`--test-threads=1`) | **104 passed / 0 failed** PRESERVED |
| smoke s1–s5 VM==JIT (release-binary corrected harness) | **5/5** (s1=4950, s2=30, s3=x, s4=2, s5=x) |
| `just check-clean` | **EXIT 0** |
| `scripts/check-no-dynamic.sh` | **EXIT 0** |
| `scripts/verify-merge.sh` | **13/13 PASS** (CHECK 5 HeapKind ordinal-collision clean; CHECK 6 4-table HeapKind lockstep clean — covers the new W16.2-B TraitObject opcode; CHECK 12 JIT-private HK_* clean) |
| `no_dynamic` sentinel (`shape-vm --lib`) | PASS |

---

## §4 sf-NEW (strict-flip A-final) NOT regressed

V3-S5 blast radius on tests (d6256f02..HEAD) is confined to 3 files — all
intentional TP-rebaselines: `functions/params.rs`, `functions/stress_advanced.rs`
(both: op_new_array ckpt-5 SURFACE-pinned → correct construction values),
`window_functions/basic.rs` (map ckpt-2 SURFACE-pinned → cleaner select String-carrier
surface). type_inference, generics, closures_hof, borrow_refs, numeric_conversions test
files were NOT touched.

| Suite | Result | Status |
|---|---|---|
| borrow_refs (sf-NEW Seam #2) | 215/4 | baseline UNCHANGED |
| type_inference (sf spot) | 265/30 | strict-flip FP baseline (inference-loss); untouched by V3-S5 |
| generics (sf spot) | 25/0 | clean |
| shape-vm `--lib` (`--test-threads=1`) | 1853/258 | 258 = pre-existing phase-2c host-tier eval `todo!` class (bundle_compiler stdlib-import / comptime / datetime-temporal); stable baseline |

**TP-rebaseline count:** 4 surface-PINNED tests rebaselined-as-TP by the V3-S5 Cascade
landing (`window_over_partition_by`, `multi_return_array`, `test_fn_returns_array`,
`test_fn_returns_array_access`) — all now run-verified against correct values rather than
the SURFACE string.

---

## §5 HARD BINDER compliance

- Per-T `TypedArray<*const TraitObjectStorage>` monomorphization. NO
  Arc<TypedArrayData> / TypedBuffer<T> / "TypedArrayKind/Carrier" resurrection
  (check-no-dynamic + no_dynamic sentinel green).
- 4-table HeapKind lockstep: verify-merge CHECK 5/6 clean for the new W16.2-B opcode.
- surface-and-stop discipline: no Bool-default (heterogeneous-element arrays still
  surface honestly; element kind stamped from the `Array<dyn T>` annotation at compile time).
- §2.7.24 Q25.C (TraitObject all-traits-dyn-able) cited at the producer site.
- refcount discipline: push-transfer + mem::forget; `data.len()==kinds.len()` parallel-track.

---

## §6 Disposition

- V3-S5 construction/method corpus: PASS (post-landing counts match Cascade run-verified
  deltas exactly; borrow_refs 215/4 held).
- s5 close-gate: CONVERGED VM==JIT (`x`); W16.2-B trait carrier exercised both modes.
  **3-INDEPENDENT-AGENT confirmation OWED (1 of 3 done).**
- All gates green. sf-NEW not regressed. 4 TP-rebaselines.
