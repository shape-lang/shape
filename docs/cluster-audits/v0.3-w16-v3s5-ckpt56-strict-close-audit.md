## v0.3 W16.1 — V3-S5 ckpt-5/6 STRICT close audit-day

Phase 4b Round 3 sub-cluster `phase-4b-round3-w16-1-audit`. Audit-only
deliverable; zero source changes in this sub-cluster. The construction-
site rebuild (`op_new_array` for heterogeneous + heap-element arrays;
`arr[i]` for `Array<TypedObject>` / `Array<dyn T>`) lands across
subsequent W16.2-N closure waves.

---

## §0 Audit scope + binding authority

**HEAD commit:** `494a3c44` (Phase 4b Round 2 LANG-9
inline-array-map-sum-hang close; supervisor merge 2026-05-18).

**Authority:** user-authorized 2026-05-18 (disposition (i)) — folds
V3-S5 ckpt-5/ckpt-6 STRICT close into v0.3 Phase 4 scope so the
smoke s5 fixture can pass under the release-binary corrected harness
as a v0.3-gating signal. This is an architectural correctness
disposition (the construction-site rebuild was already authorized at
W12-typed-array-data-deletion audit §3.5; the v0.3 fold simply
elevates it from v0.4 candidate per `v0.3-close-summary.md` §5.8 to
v0.3-gating territory).

**Predecessor surfaces** the audit consumes (all grep-verified at
HEAD `494a3c44`):

- `crates/shape-value/src/heap_variants.rs:70-89` — `HeapKind::TypedArray
  = 8` VACATED ordinal annotation (V3-S5 ckpt-1..ckpt-4 deletion chain).
- `crates/shape-vm/src/executor/objects/object_creation.rs:104-124` —
  `ckpt5_surface(op, count)` builder; called at lines 373 + 410.
- `docs/v0.3-close-summary.md` §5.8 V3-S5 ckpt-5/ckpt-6 STRICT close
  residuals (currently classified v0.4-candidate; folded into v0.3
  per user 2026-05-18 (i)).
- `docs/v0.3-close-summary.md` §0 W3.2-A snippet matrix
  (`arr_spread{,_min}`, `arr_comp{,2}` blocked in VM mode by this
  territory).
- `docs/cluster-audits/v0.3-w11-fup-c-print-typed-array-close.md`
  §4 Class V3-S5 ckpt-6 STRICT close (pre-existing).
- `docs/cluster-audits/v0.3-w14-h1-exception-registry.md` Surface S1
  + S5B classification (15-16 annotation_targets tests reclassified
  to TRACKED-V0.4 `v0.4-v3-s5-ckpt-6-strict-close`).
- `docs/cluster-audits/w12-typed-array-data-deletion-audit.md` §2.2 +
  §3.5 + §4.3 Obstacle O-3 + §4.4 Obstacle O-3a.
- `docs/adr/006-value-and-memory-model.md` §2.7.5 producer-side
  stamp-at-compile-time + §2.7.24 Q25.A SUPERSEDED amendment.
- CLAUDE.md Known Constraints `v2-raw-heap-audit` UPDATED 2026-05-17.
- CLAUDE.md Known Constraints — 4 `#[ignore]`'d sim tests at
  `bin/shape-cli/tests/stdlib/simulation.rs`.

**Empirical smoke s5 at HEAD `494a3c44`** (release binary at
`/home/dev/dev/shape-lang/shape/target/release/shape`):

```
$ ./target/release/shape run --mode vm /tmp/smokes/s5.shape
Error: Runtime error: Not implemented: op_new_array(2): SURFACE —
V3-S5 ckpt-5 consumer-cascade tier 3 surface. … (line 4) — ec=1
$ ./target/release/shape run --mode jit /tmp/smokes/s5.shape
Error: Runtime error: Not implemented: op_new_array(2): SURFACE —
V3-S5 ckpt-5 consumer-cascade tier 3 surface. … (line 4) — ec=1
```

s5 fails BOTH VM and JIT at HEAD. The `v0.3-close-summary.md` §0
matrix line `s5 | x (0) | x (0) | YES` is harness-defective per the
W12 close §1.1 root cause (`2>/dev/null | tail -1` drops stderr +
masks exit code via `tail`'s exit). With the corrected harness from
W12 §"Smoke matrix shape" (`out=$(timeout 30 shape run ... 2>&1);
ec=$?`), s5 surfaces honestly as ec=1.

The s5 fixture is the canonical `dyn HasX` trait-object array
exercise at `/tmp/smokes/s5.shape`:

```
trait HasX { method x_str() -> string; }
type Bar { v: int }
impl HasX for Bar { method x_str() -> string { "x" } }
let arr: Array<dyn HasX> = [Bar { v: 1 }, Bar { v: 2 }]
print(arr[0].x_str())
```

Expected output: `x`. Three SURFACE territories are exercised
sequentially in this snippet:

1. `[Bar { v: 1 }, Bar { v: 2 }]` literal → `op_new_array(2)`
   SURFACE (the first SURFACE; the snippet halts here).
2. `arr[0]` index access for `Array<TraitObject>` — never reached
   at HEAD (gated by #1) but still requires a `TypedArrayGet*` arm
   for `*const TraitObjectStorage` element kind to ride the typed
   fast path post-#1.
3. `.x_str()` trait dispatch on the recovered TraitObjectStorage —
   already works at HEAD per Smoke s3 (`f.x_str()` ⇒ `x` on scalar
   binding).

§1 enumerates every grep-verifiable producer site; §3 proposes the
W16.2-N partition; §5 names the close-gate ratification path.

---

## §1 SURFACE site enumeration (grep-verified at HEAD `494a3c44`)

The audit-day's empirical discovery surfaces the following sites.
Each is grep-verifiable; no archaeology. The classification by which
W16.2-N closure-wave consumes each site lives in §3.

### §1.A Construction-site SURFACEs (VM-side `op_new_*`)

| # | Site (file:line) | Op | Element-kind territory | Currently SURFACEs via | Producer-site gap |
|---|---|---|---|---|---|
| 1.A.1 | `crates/shape-vm/src/executor/objects/object_creation.rs:353-374` | `op_new_array(count)` | Heterogeneous, OR homogeneous-heap-element (`Array<TypedObject>` / `Array<dyn T>` / `Array<DateTime>` / `Array<BigInt>` / spread fall-through / empty `[]` literal) | `Err(ckpt5_surface("op_new_array", count))` | Compiler emits `OpCode::NewArray` per `crates/shape-vm/src/compiler/expressions/collections.rs:343-347` (legacy fall-through arm) AND `crates/shape-vm/src/compiler/loops.rs:1126` (`compile_array_with_spread` initial empty container) AND `crates/shape-vm/src/compiler/expressions/mod.rs:{425, 473, 488, 664, 683}` (annotation harness empty-arg-array slots). Every emission leaves the VM with no consumer past `ckpt5_surface`. |
| 1.A.2 | `crates/shape-vm/src/executor/objects/object_creation.rs:393-411` | `op_new_typed_array(count)` | Pre-§2.7.24 carrier (`Arc<TypedArrayData::{I64,F64,Bool,String}>`); current sole emission site is `crates/shape-vm/src/compiler/expressions/collections.rs:340` for homogeneous numeric/bool literal arrays that fall out of the typed v2 fast path | `Err(ckpt5_surface("op_new_typed_array", count))` | Same legacy emission gap. Distinct from the V3-S5 ckpt-1..ckpt-4 monomorphizations (`NewTypedArrayI64` / `NewTypedArrayF64` / etc.) which ARE wired through `crates/shape-vm/src/executor/v2_handlers/typed_array_elem.rs` end-to-end. |
| 1.A.3 | `crates/shape-vm/src/executor/objects/object_creation.rs:228-258` | `op_new_object(count)` | Ad-hoc `{ k: v }` object literals (was `Arc<TypedObjectStorage>` from key/value pairs) | `Err(VMError::NotImplemented("op_new_object: ad-hoc TypedObject construction depends on `create_typed_object_from_pairs` … phase-2c, see ADR-006 §2.7.4"))` | NO live compiler emission site at HEAD (`grep -rn 'OpCode::NewObject\\b' crates/shape-vm/src/` returns only `executor/tests/mod.rs:1007`). Every production compile-path for `{ k: v }` literals routes through `compile_typed_object_literal` → `NewTypedObject` (typed schema). The `op_new_object` handler is dormant; the simulation.rs `find_collisions_*` ignore comments citing `op_new_object` are LIKELY conflated with `op_new_array` — they cite the `boxes[i]` array literal which is `Array<TypedObject>` (1.A.1 territory), not `op_new_object`. Disposition: 1.A.3 is dormant + harmless; W16.2-N closure-wave should verify and, if confirmed dormant, optionally delete the opcode entirely. SURFACE-CLASS-DEAD. |

### §1.B Method-dispatch SURFACEs gated by §1.A (consumer side)

These do NOT fire at HEAD because the array is never constructed
(1.A surfaces first). Each becomes reachable AFTER 1.A.1 lands.
Their producer-side bodies all live behind `ckpt5_surface` /
`ckpt3_surface` / `ckpt2_surface` helpers; the W16.1 audit scope is
the §1.A construction-site rebuild that unblocks them. Their own
rebuild is downstream work (NOT scope of this W16.1 audit but
recorded for §3 partition awareness).

| File | Surface count | Helper |
|---|---|---|
| `crates/shape-vm/src/executor/objects/array_basic.rs:123,132,141,150,159,168,177,186` | 8 | `ckpt5_surface` |
| `crates/shape-vm/src/executor/objects/array_aggregation.rs:151,160,169,178,187,196` | 6 | `ckpt2_surface` |
| `crates/shape-vm/src/executor/objects/array_query.rs` | (per grep) | `ckpt3_surface` |
| `crates/shape-vm/src/executor/objects/array_transform.rs` | (per grep) | `ckpt2_surface` |
| `crates/shape-vm/src/executor/objects/array_sets.rs` | (per grep) | `ckpt2_surface` |
| `crates/shape-vm/src/executor/objects/array_sort.rs` | (per grep) | `ckpt3_surface` |
| `crates/shape-vm/src/executor/objects/array_joins.rs` | (per grep) | `ckpt3_surface` |
| `crates/shape-vm/src/executor/objects/concat.rs` | (per grep) | `ckpt3_surface` |
| `crates/shape-vm/src/executor/objects/iterator_methods.rs:149..343` | 16 | `ckpt3_surface` |
| `crates/shape-vm/src/executor/objects/typed_array_methods.rs:139..547` | ~50 | `ckpt3_surface` |
| `crates/shape-vm/src/executor/objects/property_access.rs:68` | (per grep) | `ckpt3_surface` |
| `crates/shape-vm/src/executor/builtins/array_ops.rs:81` | (per grep) | `ckpt3_surface` |
| `crates/shape-vm/src/executor/loops/mod.rs:250,471` | 2 | `ckpt4_surface` |
| **Total per `grep -rn "Err(ckpt[0-9]_surface" crates/shape-vm/src/`** | **125** | — |

§1.B is the consumer-cascade tier the W12 audit §3.1–§3.5 sequenced
across S1 + S2-prime + S3 + S4. The W16.1 audit does NOT re-enumerate
the §3.1–§3.5 disposition (that audit's recommendations stand);
W16.1 names the §1.A construction-site SURFACE as the v0.3-gating
slice and partitions §1.A's three sites into closure-wave-shaped
sub-clusters in §3.

### §1.C JIT-side parallel SURFACEs

| # | Site (file:line) | Surfaces via | Trigger |
|---|---|---|---|
| 1.C.1 | `crates/shape-jit/src/mir_compiler/rvalues.rs:145-177` | `Err("Route A surface-and-stop: SURFACE — Rvalue::Aggregate reached the kind-blind fallback. … ADR-006 §2.7.14 / §2.7.5.")` | JIT-side `Rvalue::Aggregate` fallback. Triggers when destination `Place::Local` has no proven `ConcreteType::Array<scalar>` element kind. |
| 1.C.2 | `crates/shape-jit/src/mir_compiler/statements.rs:158-176` | `Err("Route A surface-and-stop: SURFACE — StatementKind::ArrayStore reached the kind-blind fallback. …")` | JIT-side `ArrayStore` fallback. Same precondition gap as 1.C.1. |
| 1.C.3 | `crates/shape-jit/src/mir_compiler/terminators.rs:1209-1240` | `Err("Route A surface-and-stop: SURFACE — enum constructor `{}` depends on a heterogeneous-element-array carrier …")` | JIT-side enum-constructor SURFACE for `Shape::Circle(...)` qualified-call shape that lowered (pre-V3-S5) via `jit_new_array` + `jit_array_push_elem`. Same heterogeneous-element-array dependency as 1.A.1. |

1.C.1 + 1.C.2 short-circuit through the `emit_v2_array_aggregate`
fast path at `statements.rs:24-35` when `v2_typed_array_elem_kind`
returns `Some(elem_kind)` AND `should_use_typed_array(elem_kind) ==
Some(_)` (the latter via `v2_typed_emission::should_use_typed_array`
at `crates/shape-vm/src/compiler/v2_typed_emission.rs:184-220`).
Today: scalar (`F64` / `I64` / `I32` / `Bool` / `I8` / `U8` / `I16`
/ `U16` / `U32` / `F32` / `Char`) + heap-scalar-element (`String` /
`Decimal` Wave 2 Agent A2 2026-05-14). NOT yet: `TypedObject` /
`TraitObject` / `BigInt` / `DateTime` / `Timespan` / `Duration` /
`Instant`.

The JIT v2 allocators at `crates/shape-jit/src/ffi/v2/mod.rs` cover
4 element kinds at HEAD (`jit_v2_array_new_f64` at :59,
`jit_v2_array_new_i64` at :789, `jit_v2_array_new_i32` at :828,
`jit_v2_array_new_bool` at :872). The 9 heap-element allocators
(`jit_v2_array_new_string` / `_decimal` / `_typed_object` /
`_trait_object` / `_temporal` / `_instant` / `_bigint` / etc.) ARE
NOT YET wired — the JIT-side parallel of §1.A.

### §1.D Comptime-target SURFACEs gated by §1.A

`crates/shape-vm/src/compiler/comptime_target.rs:249-291` —
`nb_string_array` + `nb_object_array` closures inside
`ComptimeTarget::to_nanboxed` panic with V3-S5 ckpt-5 SURFACE.
This is the V0.3 W14 H1 Surface S1 class B (15-16 annotation_targets
tests). The reclassification to TRACKED-V0.4
`v0.4-v3-s5-ckpt-6-strict-close` per `v0.3-w14-h1-exception-registry.md`
§S1 / §S5B is FOLDED INTO v0.3 by the user 2026-05-18 (i) authority.

Per `comptime_builtins.rs:407-419,792` the surviving `nb_object`
shape (single-TypedObject) uses `TypedObjectStorage::_new(...)` →
`TypedObjectPtr::new(ptr)` — the v2-raw carrier already wired. The
gap is the `Array<TypedObject>` / `Array<string>` carrier — same
gap as 1.A.1.

Per `comptime_builtins.rs:440-460` the W7 close (`a85a45b8`) flagged
this exact dependency: "Recursive `Array<FieldInfo>` carrying is a
W7-followup once Array<TypedObject> field-storage is wired post the
V3-S5 ckpt-5/ckpt-6 SURFACE classes (the `op_new_array` /
`op_new_object` / `arr[i]` for `Array<TypedObject>` cluster-2+
residuals; see CLAUDE.md "Known Constraints" v2-raw-heap-audit
entry)."

### §1.E `arr[i]` for `Array<TypedObject>` / `Array<dyn T>` (the third territory in the prompt)

The compile-time emission for `arr[i]` is at
`crates/shape-vm/src/compiler/expressions/property_access.rs:522-631`
(`compile_expr_index_access`). The fast-path arms cover:

- `try_resolve_typed_elem_get(object)` (`property_access.rs:667-683`)
  — yields `GetElemI64` / `GetElemF64` for `v2_typed_array_locals`
  entries matching `TypedArrayKind::{I64, F64}` only.
- `resolve_receiver_typed_array_kind(object)` (used at :564) — for
  any `TypedArrayKind` returns the matching `kind.get_opcode()`
  (`TypedArrayGet*`) at :621.
- Else legacy `OpCode::GetProp` at :624.

For `Array<TypedObject>` / `Array<dyn T>` the receiver kind
resolution returns `None` (no `TypedArrayKind::TypedObject` /
`TraitObject` variant exists — see
`v2_typed_emission.rs:34-85`). `arr[i]` therefore falls to legacy
`GetProp`. That handler chain ultimately dispatches via PHF method
registry — but at HEAD it never gets there: the array is never
constructed past 1.A.1 SURFACE.

**Producer-side gap for §1.E**: same as 1.A.1 — the precondition
is `TypedArrayKind::TypedObject` + `TypedArrayKind::TraitObject`
variants that don't yet exist, paired with the matching
`NewTypedArrayTypedObject` / `NewTypedArrayTraitObject` opcodes +
JIT FFI `jit_v2_array_new_typed_object` / `_trait_object` allocators.

### §1 site count summary

| Class | Sites |
|---|---|
| §1.A.1 `op_new_array` (heterogeneous + heap-element-array) | 1 (VM-side handler) + 5 emission sites in the bytecode compiler |
| §1.A.2 `op_new_typed_array` (legacy generic Vec<int>/Vec<number>/Vec<bool>) | 1 (VM-side handler) + 1 emission site |
| §1.A.3 `op_new_object` (dormant, no live compiler emission) | 1 (VM-side handler) + 0 live emission sites |
| §1.B `ckpt[2-5]_surface` consumer-cascade | 125 across 14 files |
| §1.C JIT-side parallel SURFACEs | 3 (rvalues.rs + statements.rs + terminators.rs) |
| §1.D Comptime-target | 2 (`nb_string_array` + `nb_object_array`) |
| §1.E `arr[i]` for `Array<TypedObject>` / `Array<dyn T>` | 1 emission gap (no `TypedArrayKind::{TypedObject, TraitObject}` variants) |

**Total v0.3-gating SURFACE sites named by the W16.1 audit**: 3
distinct construction-site SURFACEs (§1.A.1 + §1.A.2 + §1.A.3) +
1 producer-emission gap (§1.E) for the new `TypedArrayKind`
variants. Everything else (§1.B + §1.C + §1.D) is downstream and
becomes reachable / fixable after §1.A.1 lands.

---

## §2 Per-SURFACE producer-site analysis

### §2.1 §1.A.1 `op_new_array(count)` — heterogeneous + heap-element arrays

**Kind-stamp gap**: the bytecode compiler at
`crates/shape-vm/src/compiler/expressions/collections.rs:139-388`
proves the element type for the v2 typed-fast-path branch
(`typed_kind: Option<TypedArrayKind>` at :190-214) and emits the
matching `NewTypedArrayKind`. When the element type does NOT match
a `TypedArrayKind` variant — currently any
`ConcreteType::{TypedObject, TraitObject, DateTime, Timespan,
Duration, Instant, BigInt}` or any heterogeneous-element shape OR
any spread-bearing literal — it falls to `OpCode::NewArray` at :345
which produces no v2-raw carrier.

**§2.7.5 producer-side discipline path**: extend the
producer-side proof per W12 audit §2.2 + §3.2 (S2-prime) +
§3.3 (S3):

1. Add `TypedArrayKind::TypedObject` + `TypedArrayKind::TraitObject`
   variants to `crates/shape-vm/src/compiler/v2_typed_emission.rs:34-85`
   (Wave 2 Agent A2 precedent shape; per ADR-006 §2.7.24 Q25.A
   SUPERSEDED the carrier target is `TypedArray<*const
   TypedObjectStorage>` / `TypedArray<*const TraitObjectStorage>`).
2. Add matching opcodes `NewTypedArrayTypedObject` /
   `NewTypedArrayTraitObject` + `TypedArrayPushTypedObject` /
   `TypedArrayPushTraitObject` + `TypedArrayGetTypedObject` /
   `TypedArrayGetTraitObject` + `TypedArraySetTypedObject` /
   `TypedArraySetTraitObject`. Mirror the Wave 2 Agent A2 String /
   Decimal opcodes.
3. Add `should_use_typed_array(ConcreteType::Struct(_)) =>
   Some(TypedArrayKind::TypedObject)` arm + `(ConcreteType::TraitObject(_))
   => Some(TypedArrayKind::TraitObject)` arm at
   `v2_typed_emission.rs:184-220`.
4. Wire the v2-raw `TypedArray<*const TypedObjectStorage>` carrier
   per the existing `HeapElement` impl at
   `crates/shape-value/src/heap_value.rs:4058` (TypedObjectStorage)
   + `:3092` (TraitObjectStorage) — both already implement
   `HeapElement` (Obstacle O-3 / O-3a from W12 audit §4.3 + §4.4
   RESOLVED at HEAD per `_new()` returning `*mut Self` with
   `HeapHeader` + auto-derived `_drop` retiring per-slot heap shares).
5. Heterogeneous-element shape ALONE (no homogeneous element type)
   remains a separate SURFACE — the heterogeneous case is genuinely
   "no proof of element kind" and must surface-and-stop honestly per
   ADR-006 §2.7.14 Forbidden patterns ("Bool-default fallback for
   unknown element kinds"). The user-facing shape is to require an
   explicit `Array<dyn Any>` annotation (which DOES select a typed
   kind via `TraitObject`) for genuinely heterogeneous literals.

**NO runtime fabrication; NO §2.7.5 boundary translation**. The
producer-side proof is the element type information already
available at `compile_expr_array` time (`literal_numeric` +
`infer_array_element_type` + `pending_variable_typed_array_kind` +
the `concrete_type_for_expr` lookup against AST spans). Every fix
emits a typed `NewTypedArray*` opcode at the producer site so the
runtime kind is stamped, never decoded.

**Empty-literal sub-case** (`OpCode::NewArray` at compiler
`expressions/mod.rs:425,473,488,664,683` + `loops.rs:1126`): every
empty-`[]` site has a downstream `push` that proves the element type.
The fix shape is to defer the allocator emission to the first push
site (per
`compile_array_with_spread` shape) — OR to thread the destination
slot's `ConcreteType::Array<T>` through `Operand::TypedObjectAlloc`
analogue (new `Operand::TypedArrayAlloc { elem_kind, capacity }`).
Same §2.7.5 producer-side stamp shape.

### §2.2 §1.A.2 `op_new_typed_array(count)` — legacy generic Vec<int> / Vec<number> / Vec<bool>

**Kind-stamp gap**: the sole emission site is
`crates/shape-vm/src/compiler/expressions/collections.rs:340` with
operand `Count(elements.len())` — proves the element type to be
homogeneous-numeric / bool (`literal_numeric` matches `Some(Int |
Number)` OR `is_bool`) but emits a kind-erased `OpCode::NewTypedArray`
that ckpt-5 cannot consume.

**§2.7.5 producer-side discipline path**: this emission is dead
code at HEAD — every actually-numeric homogeneous literal is already
caught by the typed-kind branch above (`v2_typed_emission`'s typed
fast path at collections.rs :190-312). The remaining trigger is the
nested-array fall-through (`has_nested_array_elem` at :186-188
forces `typed_kind = None`) — but that path produces an
`Array<Array<T>>` outer literal which is heap-element-array
territory (§2.1, recipe identical to 1.A.1).

Disposition: delete `OpCode::NewTypedArray` (the kind-erased
generic variant) + the emission at :338-342. Replace with the
nested-array path emitting `NewTypedArrayTypedObject` (treating
inner arrays as `*const TypedArray<scalar>` per
`runtime-v2-spec.md:111-115` `Array<Array<number>> →
TypedArray<*const TypedArray<f64>>`). NO carrier-unification under
a new name.

### §2.3 §1.A.3 `op_new_object(count)` — dormant

**Kind-stamp gap**: NONE. No live compiler emission site at HEAD.
Grep at `crates/shape-vm/src/` (excluding `executor/tests/mod.rs`
test scaffold + `bytecode/opcode_defs.rs:230` definition + `executor/
dispatch.rs:718` dispatch arm + `executor/objects/mod.rs:211`
handler hook + the file-doc comments) returns ZERO live emission
sites.

**§2.7.5 producer-side discipline path**: confirm dormancy + delete.
Specifically:

1. Confirm no W14 H1 Surface S1 test routes through `op_new_object`
   (per `v0.3-w14-h1-exception-registry.md` the V3-S5 panic shape
   is `nb_object_array` from `comptime_target.rs`, NOT
   `op_new_object`).
2. Confirm the 4 ignored sim tests at `bin/shape-cli/tests/stdlib/
   simulation.rs` cite `op_new_object` only as conflated narrative
   — the actual SURFACE is `op_new_array(N)` at
   `object_creation.rs:353`. (Verified empirically at the W16.1
   audit-day: `boxes = [aabb(...), aabb(...), ...]` literal fails
   at 1.A.1 before `boxes[i]` runs.)
3. Delete the `OpCode::NewObject` definition + dispatch arm +
   handler + test (or rebuild the test to use `NewTypedObject`).
   Pure subtractive close; trivially gated by the dormancy
   confirmation.

Disposition: **SURFACE-CLASS-DEAD**. W16.2-N closure-wave can
either delete the dead opcode in lockstep with the §1.A.1 fix OR
defer to a downstream sub-cluster after §1.A.1 lands and the
ckpt-6 STRICT close discipline runs.

### §2.4 §1.E `arr[i]` for `Array<TypedObject>` / `Array<dyn T>`

**Kind-stamp gap**: same as §2.1 — `resolve_receiver_typed_array_kind`
at `property_access.rs:564` returns `None` because no
`TypedArrayKind::TypedObject` / `TraitObject` variant exists.

**§2.7.5 producer-side discipline path**: gated entirely by §2.1
step (1) — the new `TypedArrayKind::TypedObject` / `TraitObject`
variants make `arr[i]` fast-path-eligible. Once §2.1 lands, the
existing `get_opcode()` dispatch at `property_access.rs:621` picks
up the new `TypedArrayGetTypedObject` / `TypedArrayGetTraitObject`
opcodes automatically. No separate emission-site change needed.

The runtime-tier read also stamps the producer-side proof: the
element-read pushes `NativeKind::Ptr(HeapKind::TypedObject)` /
`Ptr(HeapKind::TraitObject)` (NOT a Bool-default; the kind is
known from the `TypedArrayKind` variant at compile time). The
downstream `arr[0].x_str()` dispatch then routes through the
existing `as_heap_value()` + `HeapValue::TraitObject(_)` arm at
the trait-dispatch site (which already works at HEAD per Smoke s3).

---

## §3 Per-territory closure-wave partition recommendation

The W16.2-N partition is shaped by territory non-overlap discipline
+ minimal-cost-to-s5-PASS sequencing.

### §3.A Recommended slate: **3 sub-clusters** (one optional)

| Sub-cluster | Territory | Scope | Site count | Close-gate signal |
|---|---|---|---|---|
| **W16.2-A op_new_array-typed-object-element** | `compiler/v2_typed_emission.rs` (add `TypedArrayKind::TypedObject`) + `compiler/expressions/collections.rs` typed-fast-path branch (already exercises the new variant via `should_use_typed_array`) + new opcodes (`NewTypedArrayTypedObject`, `TypedArrayPushTypedObject`, `TypedArrayGetTypedObject`, `TypedArraySetTypedObject`) + matching runtime handlers in `executor/v2_handlers/` + JIT FFI `ffi/v2/mod.rs::jit_v2_array_new_typed_object` + `HeapElement` dispatch wiring (`TypedArray<*const TypedObjectStorage>::drop_array_heap` already provided by the generic `impl<T: HeapElement> TypedArray<*const T>` at `crates/shape-value/src/v2/typed_array.rs:296`) | medium | 1 producer-side `TypedArrayKind` variant + 4 opcodes (Push / Get / Set / New) + 4 runtime handlers + 1 JIT FFI allocator + `should_use_typed_array` arm + W12 audit §2.2 Obstacle O-3 RESOLVED at HEAD verify | s5 fixture `let arr: Array<TypedObject> = [obj1, obj2]; print(arr[0].field)` shape PASSes VM == JIT in release-binary corrected harness |
| **W16.2-B op_new_array-trait-object-element** | Mirror of W16.2-A for `TraitObject` element kind. Independent territory (different opcode names + JIT FFI symbol); can land in parallel with W16.2-A. | medium | Same shape as W16.2-A, swapping `TypedObjectStorage` → `TraitObjectStorage` and `HeapKind::TypedObject` → `HeapKind::TraitObject` | smoke s5 `let arr: Array<dyn HasX> = [Bar { v: 1 }, Bar { v: 2 }]; print(arr[0].x_str())` PASSes VM == JIT |
| **W16.2-C op_new_array-empty-literal-deferred-alloc** (OPTIONAL — depends on whether the empty-literal codegen at `compile_array_with_spread` is sufficient for `arr_spread{,_min}` + `arr_comp{,2}`) | `compiler/loops.rs:1126` + `compiler/expressions/mod.rs:{425, 473, 488, 664, 683}` empty-array emission sites: defer the allocator emission to the first push site, OR thread destination slot's `ConcreteType::Array<T>` through a new `Operand::TypedArrayAlloc { elem_kind, capacity }` | small | 6 emission sites + 1 new operand variant + handler dispatch | `arr_spread{,_min}` + `arr_comp{,2}` from W3.2-A snippet matrix PASS VM == JIT |

### §3.B Sub-clusters NOT in v0.3 scope (defer to v0.4)

- **W16.2-D op_new_array-temporal-element / W16.2-E
  op_new_array-bigint-element / W16.2-F op_new_array-instant-element**:
  per W12 audit §2.2 Obstacle O-1 (DateTime / Timespan / Duration
  share `TemporalData`) + Obstacle 3 R19 defer (BigInt). NOT
  v0.3-gating; the s5 smoke fixture does not exercise these
  element kinds. Track per W12 audit §3.2 (S2-prime / S2-prime²)
  cadence.
- **W16.2-G op_new_object-dormant-delete**: per §2.3 disposition
  (SURFACE-CLASS-DEAD); optional pure-subtractive close. NOT
  v0.3-gating.
- **W16.2-H §1.C JIT-side enum-constructor heterogeneous-element-array**:
  per §1.C.3 — the `Shape::Circle(a, b)` qualified-call lowering
  depends on a heterogeneous-element carrier that W16.2-A / -B do
  not yet supply. Track separately as a v0.4 follow-up unless a
  smoke fixture surfaces it as v0.3-gating.
- **W16.2-I §1.D comptime-target nb_*_array rebuild**: per §1.D
  (`comptime_target.rs:249-291`). Lands as a downstream close after
  W16.2-A + W16.2-B retire the construction-site SURFACE; W16.2-I
  consumes the new `TypedArray<*const TypedObjectStorage>` carrier
  to rebuild `nb_object_array`. This closes the W14 H1 Surface S1
  S5B class (15-16 annotation_targets tests) per user 2026-05-18 (i)
  authority.
- **W16.2-J §1.B ckpt-N consumer-cascade closures**: 125-site
  cascade. Per W12 audit §3.2–§3.5 sequencing. NOT in scope of
  the W16.1 audit-day partition; track per existing W12-audit-N
  closure-wave cadence.

### §3.C Recommended landing order

1. **W16.2-A + W16.2-B in parallel** (independent territory; no
   ownership conflict). Either order closes s5 partially: -A makes
   `Array<TypedObject>` literals constructible, -B makes
   `Array<dyn T>` literals constructible (the s5 fixture proper).
2. **W16.2-C optional**, lands after -A + -B if `arr_spread*` /
   `arr_comp*` snippets remain blocked (verify empirically at the
   -A+-B close per W3.2-A 27-snippet matrix re-run).
3. **W16.2-J §1.B ckpt-N consumer cascade** runs concurrently per
   the W12 audit's existing S1/S2/S3 cadence; the W16.1 audit
   names it as separate territory + does not partition it.
4. Then ratify: smoke s5 PASS via release-binary corrected harness
   = v0.3 close-gate signal (per `v0.3-close-summary.md` §0
   smoke matrix preservation criterion).

---

## §4 Cross-reference matrix

### §4.A The 4 `#[ignore]`'d sim tests (per CLAUDE.md Known Constraints)

All at `bin/shape-cli/tests/stdlib/simulation.rs`. Each ignore
comment explicitly cites `V3-S5 ckpt-5/ckpt-6 SURFACE class:
op_new_array trips the consumer-cascade tier 3 SURFACE-and-stop at
object_creation.rs:353` (the §1.A.1 territory):

| Test | Line | Element-kind territory | Closure-wave |
|---|---|---|---|
| `test_harmonic_oscillator_rk4_system` | `:104-125` | `rk4_system` returns `Array<{t, y, ...}>` (TypedObject elements) | W16.2-A |
| `test_rk45_system_harmonic_oscillator` | `:183-209` | `rk45_system` returns same | W16.2-A |
| `test_find_collisions_brute` | `:459-490` | `let boxes = [aabb(...), aabb(...), ...]` → `Array<TypedObject>` literal | W16.2-A |
| `test_find_collisions_sweep` | `:492-520` | Same | W16.2-A |

All 4 close at W16.2-A landing (all are TypedObject-element shapes;
none require `TraitObject`). Disposition: at W16.2-A close, the 4
`#[ignore]` annotations are removed in lockstep + the tests run as
part of the standard suite. Per `v0.3-w14-h1-exception-registry.md`
Surface S1 disposition, the 15-16 annotation_targets V3-S5 tests
under Class 5B route to W16.2-A close in the same way.

### §4.B smoke s5 (canonical fixture; current FAIL at HEAD)

Reproducer at `/tmp/smokes/s5.shape`:

```
trait HasX { method x_str() -> string; }
type Bar { v: int }
impl HasX for Bar { method x_str() -> string { "x" } }
let arr: Array<dyn HasX> = [Bar { v: 1 }, Bar { v: 2 }]
print(arr[0].x_str())
```

Expected: `x`. Empirical at HEAD `494a3c44` (release binary
corrected harness): VM ec=1, JIT ec=1, both with `op_new_array(2):
SURFACE`. Closes at **W16.2-B** (the `TraitObject`-element path;
the `Bar { v: 1 }` constructors lower to `TypedObject` per the
existing scalar-binding shape, then the `Array<dyn HasX>`
annotation drives `pending_variable_typed_array_kind =
Some(TypedArrayKind::TraitObject)` post-W16.2-B).

### §4.C Fuzz-corpus surfaces in `tools/shape-fuzz/tests/corpus/`

Grep of corpus seeds tagged with V3-S5 territory class header:

| Seed | Class header | Empirical VM | Empirical JIT | Closure-wave |
|---|---|---|---|---|
| `tools/shape-fuzz/tests/corpus/collections/c09_spread_expand.shape` | `CLASS: W11-followup-jit-spread-expand` | `op_new_array(0)` SURFACE (line 6) | ec=0 produces `3` (JIT native fast-path) | W16.2-C (VM empty-literal) + existing W11-followup-jit-spread-expand (orthogonal; semantically-wrong JIT output is a separate bug per W11-fup-C close §4) |

No fuzz-corpus seed at HEAD directly exercises `Array<TypedObject>`
/ `Array<dyn T>` construction. Recommendation: W16.2-A + W16.2-B
closures-waves SHOULD add corpus seeds:

- `collections/c11_array_typed_object.shape` (W16.2-A): `type B {v:
  int}; let arr = [B{v:1}, B{v:2}]; print(arr[1].v)` — expects `2`.
- `collections/c12_array_trait_object.shape` (W16.2-B): smoke s5
  shape — expects `x`.

The W13 differential-fuzz audit (`v0.3-w13-differential-fuzz-audit.md`)
named this territory as a future negative-corpus expansion in
§4.1; W16.2-A + W16.2-B closure-waves are the natural addition site.

### §4.D snippet matrix interaction (`v0.3-close-summary.md` §0 W3.2-A)

W3.2-A 27-snippet matrix at HEAD per W11-fup-C close §3:
- VM: 21/27
- JIT: 24/27

The 6-snippet VM gap includes `arr_spread{,_min}` + `arr_comp{,2}`
+ at least 2 V3-S5-territory shapes per close §0 line 60. W16.2-A
+ W16.2-B + (optional) W16.2-C will lift the VM column by an
estimated 4-6 snippets; the JIT column is gated separately on
W11-followup-jit-spread-expand (orthogonal).

---

## §5 Close-gate ratification path

### §5.A v0.3-gating signal: smoke s5 PASS VM == JIT via release-binary corrected harness

```bash
$ for s in s1 s2 s3 s4 s5; do
    out_vm=$(timeout 30 ./target/release/shape run --mode vm /tmp/smokes/$s.shape 2>&1); ec_vm=$?
    out_jit=$(timeout 30 ./target/release/shape run --mode jit /tmp/smokes/$s.shape 2>&1); ec_jit=$?
    tail_vm=$(echo "$out_vm" | tail -1)
    tail_jit=$(echo "$out_jit" | tail -1)
    echo "$s VM='$tail_vm' (ec=$ec_vm) JIT='$tail_jit' (ec=$ec_jit)"
  done
```

Acceptance criterion (per `v0.3-close-summary.md` §0 smoke-matrix
preservation gate, post-W12 corrected-harness binding):

| Smoke | VM expected | JIT expected | VM == JIT |
|---|---|---|---|
| s1 | `4950` (ec=0) | `4950` (ec=0) | YES |
| s2 | `30` (ec=0) | `30` (ec=0) | YES |
| s3 | `x` (ec=0) | `x` (ec=0) | YES |
| s4 | `2` (ec=0) | `2` (ec=0) | YES |
| **s5** | **`x` (ec=0)** | **`x` (ec=0)** | **YES** |

### §5.B Sequence of closes

1. **W16.2-A** lands → 4 sim tests unignore-and-pass; `arr =
   [TypedObject(...), TypedObject(...)]` shapes compile + execute.
   Smoke s5 STILL FAILS (the s5 fixture is trait-object territory).
2. **W16.2-B** lands → smoke s5 PASS VM == JIT (release-binary
   corrected harness). v0.3-gating signal MET.
3. **W16.2-C** lands (OPTIONAL) → `arr_spread{,_min}` +
   `arr_comp{,2}` VM-mode SURFACEs retired; W3.2-A snippet matrix
   VM column +4-6.
4. **Supervisor ratify** post-W16.2-B: confirm smoke s5 PASS via
   release-binary corrected harness at three independent agents (the
   harness-defect lesson from W12 close §1.1: a single agent's
   corrected-harness run is insufficient evidence; per supervisor
   2026-05-18 binding 3-agent independent confirmation is the
   ratify shape).

### §5.C Per-territory ratify checklist (W16.2-A / W16.2-B)

Each closure-wave's close gate per ADR-006 §2.7.5 + §2.7.24
discipline:

1. `just check-clean` exit 0 (workspace builds).
2. `bash scripts/verify-merge.sh` 11/13 PASS (or higher) — the
   2 pre-existing `cargo check`-via-`cc`-linker failures stay (per
   W11-fup-C close §3.2 baseline).
3. `bash scripts/check-no-dynamic.sh` exit 0 (no
   bridge/probe/helper/hop/translator/adapter/shim descriptors).
4. New corpus seed `collections/c11_*` (W16.2-A) /
   `collections/c12_*` (W16.2-B) PASSes VM == JIT.
5. Sim tests unignore-and-pass (W16.2-A: 4 tests; W16.2-B: 0 tests
   directly but smoke s5 PASS).
6. Smoke matrix s1-s5 5/5 VM == JIT preserved via release-binary
   corrected harness (W16.2-B is the s5-closing wave).
7. Class-shift audit: per surface-and-stop discipline + the W11-fup-C
   close §6 imprecision #97 lesson, every NEW SURFACE the close
   uncovers (by retiring the named-territory surface that masked
   it) gets named + classified + tracked. Expected class-shifts at
   W16.2-A close: §1.C JIT-side enum-constructor heterogeneous-element-
   array (NEW class W16.2-H) becomes potentially-reachable in
   programs that lower enum-constructor payloads through
   `Array<TypedObject>` shapes.

---

## §6 SURFACE-AND-STOP items

### §6.A Items requiring NO escalation

- §2.1 producer-side proof extension follows existing W12 audit
  §2.2 + §3.2 + §3.3 + §4.1.B disposition. No ADR amendment needed;
  W12 audit §4.1.C + §4.1.D already drafted the
  Q25.A SUPERSEDED amendment text (LANDED per Round 20 S2-prime
  audit-first deliverable (c), 2026-05-14).
- §2.3 op_new_object dormancy delete: pure subtractive close;
  follows existing W12 audit §3.5 S5 deletion cadence.
- §2.4 `arr[i]` for typed-object/trait-object element: closes
  automatically once §2.1 lands; no separate work.

### §6.B Items requiring supervisor architectural ratify

- **Item 6.B.1 — Empty-literal codegen disposition (W16.2-C
  alternatives)**: defer-allocator-to-first-push vs new
  `Operand::TypedArrayAlloc { elem_kind, capacity }` operand. Both
  are §2.7.5-compliant; trade-off is bytecode-density vs runtime-
  allocator-discipline. Supervisor disposition needed at W16.2-C
  dispatch time.
- **Item 6.B.2 — Heterogeneous-element-array surface stance**: per
  §2.1 step (5), genuinely heterogeneous literals (`[1, "hello",
  true]`) surface-and-stop with no fast-path emission. The
  user-facing shape is to either (a) require explicit `Array<dyn
  Any>` annotation, OR (b) recognize the heterogeneity and emit a
  structured compile error. Supervisor disposition needed: which
  shape preserves the §2.7.14 forbidden-list ("Bool-default
  fallback for unknown element kinds") best?
- **Item 6.B.3 — Class-shift discipline at W16.2-A close**: per
  §5.C item 7, the W11-fup-C close §6 imprecision #97 lesson is
  binding here. Supervisor ratify gate at W16.2-A close MUST
  empirically re-classify any newly-reachable SURFACE in the §1.B /
  §1.C cascade — pre-emptively naming W16.2-H (JIT-side enum-
  constructor heterogeneous-element-array) as a known class-shift
  candidate.

### §6.C Items NOT requiring escalation per binding precedent

- ADR-006 §2.7.5 producer-side stamp-at-compile-time: BINDING at
  HEAD. Every recommendation in §2 + §3 + §5 follows this
  discipline.
- ADR-006 §2.7.24 Q25.A SUPERSEDED: amendment text LANDED at Round
  20 S2-prime (2026-05-14). No further amendment needed for the
  W16.1 + W16.2-N territory.
- ADR-005 §1 single-discriminator: PRESERVED. `TypedObjectStorage`
  + `TraitObjectStorage` already implement `HeapElement` (verified
  at HEAD); the new `TypedArray<*const T>` carriers dispatch via
  the existing `T: HeapElement` machinery. NO parallel
  discriminator introduced.
- §Renames-to-refuse-on-sight: the audit doc text + the
  recommended fixes use only purpose-named descriptors. NO
  bridge/probe/helper/hop/translator/adapter/shim framings.

### §6.D Items NOT requiring user authority

- CLAUDE.md modifications: NONE required for this audit. The W5
  close 2026-05-17 update to the `v2-raw-heap-audit` Known
  Constraint already cites the V3-S5 ckpt-5/ckpt-6 territory as
  "distinct root cause families, tracked per their own follow-up
  cadence"; the user 2026-05-18 (i) authorization to fold this
  into v0.3 does not change the CLAUDE.md text (the entry remains
  accurate). At W16.2-A / W16.2-B close, CLAUDE.md may need a
  one-line update to remove the "4 #[ignore]'d sim tests" + "V3-S5
  ckpt-5/ckpt-6 SURFACE territory" residual line — that update IS
  user-authorization-required.
- New AGENTS.md rows for each W16.2-N closure-wave (NOT this
  audit-day; this audit lands one AGENTS.md row per §7 below).

---

## §7 AGENTS.md row (appended at audit close)

| Branch | Worktree | Role | Active cluster | Files owned | Status | Last update |
|---|---|---|---|---|---|---|
| `phase-4b-round3-w16-1-audit` | `/home/dev/dev/shape-lang/shape-r3-w16-1-audit` | Phase 4b Round 3 W16.1 V3-S5 ckpt-5/6 STRICT close audit-day | — | `docs/cluster-audits/v0.3-w16-v3s5-ckpt56-strict-close-audit.md` + `AGENTS.md` (audit-only; zero source changes) | **CLOSED 2026-05-18** — Phase 4b Round 3 W16.1 V3-S5 ckpt-5/ckpt-6 STRICT close audit-day. **Authority**: user 2026-05-18 (i) — folds V3-S5 ckpt-5/ckpt-6 STRICT close from v0.4-candidate into v0.3 Phase 4 scope for s5 smoke-fixture v0.3-gating. **Audit deliverable**: `docs/cluster-audits/v0.3-w16-v3s5-ckpt56-strict-close-audit.md` with §0-§7. **Site enumeration (§1)**: 3 construction-site SURFACEs (§1.A.1 `op_new_array` heterogeneous + heap-element; §1.A.2 `op_new_typed_array` legacy generic; §1.A.3 `op_new_object` dormant) + 1 producer-emission gap (§1.E `arr[i]` for `Array<TypedObject>` / `Array<dyn T>`); 125 downstream §1.B `ckpt[2-5]_surface` cascade sites tracked separately per W12 audit cadence; 3 JIT-side parallel SURFACEs (§1.C) + 2 comptime-target SURFACEs (§1.D). **Smoke s5 empirical at HEAD** `494a3c44`: VM ec=1 + JIT ec=1, both `op_new_array(2): SURFACE` (release-binary corrected harness); §0 close-summary matrix line `s5 \| x (0) \| x (0)` confirmed harness-defective per W12 close §1.1 (`2>/dev/null \| tail -1` drops stderr + masks ec via `tail`). **Per-territory partition (§3)**: 3-sub-cluster slate W16.2-A (`op_new_array-typed-object-element` — closes 4 sim tests + W14 H1 Surface S1 class B 15-16 annotation_targets tests via W16.2-I downstream); W16.2-B (`op_new_array-trait-object-element` — closes smoke s5); W16.2-C (OPTIONAL `op_new_array-empty-literal-deferred-alloc` — closes `arr_spread{,_min}` + `arr_comp{,2}`). Downstream v0.4 candidates W16.2-D/E/F (Temporal/BigInt/Instant) + W16.2-G (op_new_object delete) + W16.2-H (JIT enum-constructor heterogeneous-element) + W16.2-I (comptime nb_object_array rebuild) + W16.2-J (§1.B 125-site cascade per W12 audit S1/S2/S3 cadence). **Discipline**: ADR-006 §2.7.5 producer-side stamp; W12 audit §2.2 Obstacles O-3 (TypedObject) + O-3a (TraitObject) RESOLVED at HEAD per `TypedObjectStorage::_new` + `TraitObjectStorage::_new` returning `*mut Self` with `HeapHeader` + `HeapElement` impls already wired (`crates/shape-value/src/heap_value.rs:4058` + `:3092`). NO ADR amendment required; Q25.A SUPERSEDED LANDED at Round 20 S2-prime (2026-05-14). **SURFACE-AND-STOP items (§6)**: 3 supervisor-architectural-ratify items (6.B.1 W16.2-C empty-literal codegen alternatives; 6.B.2 heterogeneous-element surface stance; 6.B.3 class-shift discipline at W16.2-A close); 0 ADR-006 amendments; 0 user-authorization items at audit close (CLAUDE.md update deferred to W16.2-A/B close gate). **Close gates**: `bash scripts/verify-merge.sh` 11/13 PASS (2 pre-existing `cargo check`-via-`cc`-linker failures, same as W11-fup-C / W11-fup-A / W11-fup-B baseline); `bash scripts/check-no-dynamic.sh` exit 0 (broader-family regex clean in audit doc text); audit-only = no test runs, no source changes, smoke matrix s1+s2+s3+s4 trivially preserved at HEAD `494a3c44`; s5 stays FAILing (the target the W16.2-N closure-wave slate retires). Close commit `<HASH>`. | 2026-05-18 |

---

*W16.1 V3-S5 ckpt-5/ckpt-6 STRICT close audit-day closes; W16.2-A
+ W16.2-B sub-cluster dispatch is the natural next step per §5
ratification path. Per CLAUDE.md §Forbidden Patterns + the W11-fup-C
close §5 + §6 imprecision discipline, every closure-wave's close
gate MUST empirically re-verify smoke s5 via release-binary
corrected harness (NOT the `2>/dev/null | tail -1` shape that
masked the s5 FAIL at the v0.3-close-summary §0 matrix landing).*
