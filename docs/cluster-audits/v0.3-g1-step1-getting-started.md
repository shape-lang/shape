# v0.3 G.1 Step-1 Slice-A Audit — getting-started + examples

**Branch:** `r8w5-g1-getting-started`
**Parent HEAD:** `605ad98e` (`infra(v0.3): R8 W4 close-followup — git-stash pre-commit hook + §5 v0.4 inventory expansion + handover refresh`).
**Dispatch:** v0.3 G.1 doc-truth Step 1 slice-A — per `docs/v0.3-close-summary.md` §0.A G.1 (audit-only; doc-aspirational reclassification + shape-web in-place fixes for (a)-class).
**Scope:** 9 .mdx files (6 getting-started + 3 examples). Audit-only in `shape/` worktree (no source changes); shape-web book .mdx edits in-place where (a)-class fix needed (left UNCOMMITTED for team-lead batch).

---

## §0 Slice scope + binding + HEAD anchor

**HEAD anchor:** `605ad98e` (shape) — same as main; verified via `git -C /home/dev/dev/shape-lang/shape-r8w5-g1-getting-started rev-parse HEAD`.

**Binding:**
- Per-snippet protocol (canonical (ii) F' harness): per dispatch prompt; both modes verified.
- Classification rule: (a) doc wrong / code right → in-place .mdx edit; (b) doc was a contract / code wrong → NEW v0.3-gating; (c) doc describes v0.4 feature → annotate + add to inventory.
- Forbidden patterns N/A (pure doc-truth — no Rust source changes in shape/ worktree).
- No `git stash` (pre-commit hook enforces).

**In-slice files (9):**

| # | File | Snippets (runnable=true) | Snippets (runnable=false / other) | Notes |
|---|------|--------------------------|-----------------------------------|-------|
| 1 | `getting-started/installation.mdx` | 2 | 0 | `shape` fences at L88, L110; rest are bash/text |
| 2 | `getting-started/basic-concepts.mdx` | 6 | 4 (runnable=false; 1 unmarked Option/Result) | L14, L50, L69, L80, L93, L133 runnable; L26, L39, L109, L149, L168 non-runnable |
| 3 | `getting-started/first-query.mdx` | 7 | 1 (runnable=false typed Record at L91) | L12, L20, L29, L41, L52, L73, L108 runnable |
| 4 | `getting-started/repl.mdx` | 0 | 3 (runnable=false REPL transcript bodies) | All non-runnable per W3.2-C close (REPL is TUI, snippets are illustrative) |
| 5 | `getting-started/editor-setup-vscode.mdx` | 0 | 0 | bash/no shape fences |
| 6 | `getting-started/editor-setup-neovim.mdx` | 0 | 0 | bash/vim/lua — no shape fences |
| 7 | `examples/hello-world.mdx` | 1 | 0 | Full tour at L12 |
| 8 | `examples/web-request.mdx` | 0 | 1 (runnable=false; SURFACE callout per W17-marshal-return-arms) | Per-prompt classification rule: (c)/(b)-class deferred — already annotated in book |
| 9 | `examples/comptime-codegen.mdx` | 0 | 1 (runnable=false; SURFACE callout per V3-S5 ckpt-6) | Already annotated in book; matches (c) class |

**Total labeled-runnable in scope:** 16 (per dispatch prompt's per-snippet protocol — only `runnable=true` fences are verified).

---

## §1 Per-snippet results table

Methodology: each snippet extracted to `/tmp/g1-slice-a/<id>.shape`; run via worktree release binary (`/home/dev/dev/shape-lang/shape-r8w5-g1-getting-started/target/release/shape`) under canonical (ii) F' harness:

```bash
out=$(timeout 30 "$SHAPE" run --mode $mode $file 2>&1); ec=$?; last=$(echo "$out" | tail -1)
```

| File:snippet | Classification | Doc-stated expected (last line) | Actual VM (last line, ec) | Actual JIT (last line, ec) | VM == JIT | Status |
|---|---|---|---|---|---|---|
| `installation.mdx:88` install-s1 (fn main hello) | pass-as-is | "hello from shape" | `hello from shape` (0) | `hello from shape` (0) | YES | PASS |
| `installation.mdx:110` install-s2 (let x/y) | pass-as-is | `// 30` inline | `30` (0) | `30` (0) | YES | PASS |
| `basic-concepts.mdx:14` basic-s1 (if-else expr) | pass-as-is | (no stated; expects "B" — score=84 → ≥80 branch) | `B` (0) | `B` (0) | YES | PASS |
| `basic-concepts.mdx:50` basic-s2 (typed Point) | pass-as-is | (no stated; expects 35 — 10+25) | `35` (0) | `35` (0) | YES | PASS |
| `basic-concepts.mdx:69` basic-s3 (distance2 fn) | pass-as-is | (no stated; expects 25 — 3²+4²) | `25` (0) | `25` (0) | YES | PASS |
| `basic-concepts.mdx:80` basic-s4 (closure filter) | pass-as-is | (no stated; expects `[2, 4]`) | `[2, 4]` (0) | `[2, 4]` (0) | YES | PASS |
| `basic-concepts.mdx:93` basic-s5 (match union) | pass-as-is | (no stated; expects "int(7)" + "string(hi)") | `string(hi)` (0); full = `int(7)\nstring(hi)` | `string(hi)` (0); full = `int(7)\nstring(hi)` | YES | PASS — W15 reaudit JIT-SIGSEGV (FIX-LANGUAGE) **RESOLVED at HEAD** |
| `basic-concepts.mdx:133` basic-s6 (sqrt named-import) | pass-as-is | (no stated; expects ~5.6418...) | `5.641898218231844` (0) | `5.641898218231844` (0) | YES | PASS — W15 reaudit JIT-NaN bug (FIX-LANGUAGE) **RESOLVED at HEAD** |
| `first-query.mdx:12` firstq-s1 (Hello, Shape) | pass-as-is | (no stated) | `Hello, Shape` (0) | `Hello, Shape` (0) | YES | PASS |
| `first-query.mdx:20` firstq-s2 (indexing) | pass-as-is | inline `// first`, `// last` — expects 20, 22 | `22` (0); full = `20\n22` | `22` (0); full = `20\n22` | YES | PASS |
| `first-query.mdx:29` firstq-s3 (sum/len/avg floats) | pass-as-is | (no stated; expects `count=6, total=72.0, avg=12.0`) | `count=6, total=72.0, avg=12.0` (0) | `count=6, total=72.0, avg=12.0` (0) | YES | PASS |
| `first-query.mdx:41` firstq-s4 (filter readings) | pass-as-is | inline `// [27, 30, 25]` | `[27, 30, 25]` (0) | `[27, 30, 25]` (0) | YES | PASS — W15 reaudit JIT-filter bug (FIX-LANGUAGE HIGH) **RESOLVED at HEAD** |
| `first-query.mdx:52` firstq-s5 (is_anomaly fn) | pass-as-is | (no stated; expects "anomaly detected") | `anomaly detected` (0) | `anomaly detected` (0) | YES | PASS |
| `first-query.mdx:73` firstq-s6 (match bucket) | pass-as-is | (no stated; expects "negative\nzero\npositive") | `positive` (0); full 3 lines | `positive` (0); full 3 lines | YES | PASS |
| `first-query.mdx:108` firstq-s7 (summarize fn main) | pass-as-is **with stderr-noise (b'-class)** | `count=5, total=20.5, avg=4.1` | `count=5, total=20.5, avg=4.1` (0) — preceded by 6 V2-verifier diagnostic lines on stderr | same + `[jit-fallback] function main failed JIT compile` — interpreter completes correctly | YES (stdout convergent) | PASS-on-stdout — see §3 G1-B-FQ7-V2VERIFIER |
| `hello-world.mdx:12` hello-s1 (full tour) | pass-as-is | Full 8-line "Expected output" block | matches all 8 lines exactly; last = `valid age: 30` (0) | matches all 8 lines exactly; last = `valid age: 30` (0) | YES | PASS — W15 reaudit cmp-on-Int64 (FIX-LANGUAGE) was W3.2-C fix; HEAD STABLE |

**Summary:**

| Category | Count |
|----------|------:|
| pass-as-is | 15 |
| pass-on-stdout, stderr-noise (b'-class for team-lead) | 1 (firstq-s7) |
| (a)-class shape-web in-place fix | 0 |
| (b)-class NEW v0.3-gating | 0 (the stderr-noise observation is logged in §3 but is non-blocking for G.1 — last-line + ec=0 + VM == JIT all green) |
| (c)-class v0.4 inventory | 0 (web-request + comptime-codegen are runnable=false with existing SURFACE callouts; already correctly classified pre-G.1) |

**Pre-existing W3.2-C / W15-reaudit dispositions verified at HEAD `605ad98e`:**
- W3.2-C's 7-commit slice-C systematic rewrite (per `docs/cluster-audits/v0.3-w3-2-c-close.md`) holds.
- W15 re-audit FIX-LANGUAGE bugs (basic-concepts.mdx:93 union-match JIT SIGSEGV; basic-concepts.mdx:133 JIT math NaN; first-query.mdx:41 JIT filter) — **all 3 resolved at HEAD**.
- web-request.mdx SURFACE callout (W17-marshal-return-arms) — W17 merged `b4d76858` per close-summary §0.A row C; callout text could be retired in future fix-wave but body is runnable=false so does not block G.1.
- comptime-codegen.mdx SURFACE callout (V3-S5 ckpt-6) — still valid per W15-V3S5 strict-close audit territory; body is runnable=false; no action required for G.1.

---

## §2 (a)-class in-place fixes summary

**ZERO (a)-class fixes required.** All 16 labeled-runnable snippets in slice-A:
- Run cleanly under both VM and JIT at HEAD `605ad98e`.
- Produce output matching doc-stated expected (where stated; semantically correct where not).
- Exhibit VM == JIT convergence on stdout.

**shape-web files modified:** NONE.

This is a confirmation-of-stability outcome: the W3.2-C systematic rewrite (`a8853958` per close-summary §0.B) + the intervening language-fix waves (multiple commits per W15-reaudit follow-ups) have closed the slice-A bug class. No further in-place doc edits are needed at the labeled-runnable level.

---

## §3 (b)-class v0.3-gating list

**ZERO blocking (b)-class items.** All snippets produce correct stdout + ec=0 + VM == JIT.

**ONE (b')-class observation** (non-blocking; logged for team-lead triage at G.1 close):

### G1-B-FQ7-V2VERIFIER — V2 bytecode verification diagnostics on stderr for TypedArray<f64>-inside-fn-main

| Field | Value |
|---|---|
| **Snippet** | `first-query.mdx:108` firstq-s7 (the `fn main() { let values = [3.2, 4.1, 3.8, 5.0, 4.4]; print(summarize(values)) }` save-and-run example) |
| **Stdout** | CORRECT (`count=5, total=20.5, avg=4.1`); ec=0; VM == JIT |
| **Stderr (VM)** | 7 lines: `V2 bytecode verification failed: 6 violation(s)` + 6 `V2 typed opcode {NewTypedArrayF64,TypedArrayPushF64} at offset N in function 'main' has no FrameDescriptor` |
| **Stderr (JIT)** | Same 7 lines emitted TWICE (T1 verify + T2 verify) + `[jit-fallback] function main failed JIT compile: Runtime error: JIT execution error (code: -1); running under interpreter` — interpreter fall-through completes correctly per W12 mode-binding |
| **Trigger** | `fn main() { ... [floats] ... }` — `NewTypedArrayF64` + `TypedArrayPushF64` emitted inside a user function body without `FrameDescriptor` attachment. Top-level `let values = [...floats...]` (firstq-s3, firstq-s5) does NOT trigger. |
| **Severity** | Non-blocking for G.1 (stdout correct; ec=0; doc's stated expected matches). Cosmetic / noise-on-stderr. JIT fallback is a JIT-defection that interpreter saves. |
| **Classification rationale** | Strict reading of G.1 "labeled-runnable snippet runs cleanly VM == JIT" — stdout converges, exit code 0, last-line matches. Stderr-noise is NOT in the G.1 close gate language. Therefore (b')-class follow-up, NOT (b)-class gating. |
| **Suggested follow-up territory** | V2-verifier `FrameDescriptor` attachment on `NewTypedArrayF64` / `TypedArrayPushF64` emit sites when inside `fn main` user-function body. Likely 1 file in `crates/shape-vm/src/compiler/` (typed-array-emit path) + 1 site missing the descriptor wire-up. Team-lead can dispatch as a one-shot fix in a post-G.1 hardening wave, OR record as Known Constraint and defer if scope-bounded. |

No other items. The dispatch prompt's slice-A snippet corpus is GREEN.

---

## §4 (c)-class v0.4 inventory

**ZERO new (c)-class deferrals from labeled-runnable snippets.**

Pre-existing (c)-class items already correctly annotated in book at HEAD (not introduced by G.1; verified in-place):

| Item | Page | Status at HEAD |
|---|---|---|
| HTTP `get` / `post_json` + JSON `parse` / `stringify` end-to-end execution gated on `W17-marshal-return-arms` | `examples/web-request.mdx:12-21` runnable=false SURFACE callout | W17-marshal-return-arms MERGED `b4d76858` per close-summary §0.A row C — callout text COULD be retired in a future shape-web fix-wave (one-line callout removal + flip runnable=false → runnable=true after re-verification). NOT a G.1 (a)-class fix because runnable=false is intentional doc-level marking, not a wrong-output bug. Team-lead may dispatch a separate web-request re-verify wave. |
| Comptime annotation + `extend` + `set return` codepath gated on V3-S5 ckpt-6 STRICT close | `examples/comptime-codegen.mdx:12-20` runnable=false SURFACE callout | V3-S5 ckpt-6 STRICT close still in progress per `v0.3-w16-v3s5-ckpt56-strict-close-audit.md` — callout body remains accurate. Genuine v0.4 deferral candidate per W3.2-C close §3 defer-with-cite disposition. No action this audit. |

---

## §5 Smoke 5/5 + close-gate evidence

**Smoke matrix at HEAD `605ad98e` (canonical (ii) F' harness, worktree binary):**

```
s1 vm ec=0 last=4950
s1 jit ec=0 last=4950
s2 vm ec=0 last=30
s2 jit ec=0 last=30
s3 vm ec=0 last=x
s3 jit ec=0 last=x
s4 vm ec=0 last=2
s4 jit ec=0 last=2
s5 vm ec=0 last=x
s5 jit ec=0 last=x
```

**5/5 VM == JIT** matching dispatch prompt's expected values (s1=4950, s2=30, s3=x, s4=2, s5=x; all ec=0).

**Close gates (post-commit):**
- `direnv exec /home/dev/dev/shape-lang just check-clean` — pending verification post-commit (§5 finalization).
- `bash scripts/verify-merge.sh` — pending verification post-commit.
- `bash scripts/check-no-dynamic.sh` — pending verification post-commit.
- Source changes in `shape/` workspace: NONE (audit-only; per dispatch discipline).
- shape-web book .mdx edits: NONE (zero (a)-class).
- `Co-Authored-By: Claude` trailer: NONE.
- No `--no-verify`.

---

## §6 Defection-attractor framings refused

NONE encountered. This is a pure doc-truth audit; no Rust source touched; no ValueWord / tagged-dispatch / kind-blind ABI / broader-family regex patterns. Pre-existing SURFACE callouts in `web-request.mdx` and `comptime-codegen.mdx` were preserved as-is (cite-with-defer is canonical W15-reaudit annotation shape, NOT a defection).

---

## §7 Honest scope-completion note

**9/9 slice-A files visited and labeled-runnable snippet corpus fully audited.**

- 16/16 runnable=true snippets extracted, verified VM + JIT, classified.
- runnable=false snippets (REPL transcript bodies, illustrative Option/Result without runnable label, Data Tables non-runnable shell, web-request body, comptime-codegen body) are out-of-scope-per-protocol — the dispatch prompt instructs "for each labeled-runnable snippet" — but were visually inspected for stale-API claims; all match prior W3.2-C close + W15 reaudit shape.
- Editor-setup files (vscode + neovim): bash/vim/lua only — no shape fences to verify.

**Confidence:** HIGH. Slice-A getting-started + examples is in a stable, doc-truth-correct state at HEAD `605ad98e`. The single (b')-class observation (G1-B-FQ7-V2VERIFIER stderr-noise) is non-blocking per strict G.1 close-gate reading.

**Recommendation to team-lead:** close G.1 slice-A as PASS; optionally dispatch a one-shot post-G.1 hardening wave for G1-B-FQ7-V2VERIFIER (FrameDescriptor attachment on NewTypedArrayF64 / TypedArrayPushF64 emit inside `fn main` body); optionally dispatch a one-shot shape-web fix-wave to retire the web-request.mdx SURFACE callout now that W17-marshal-return-arms is merged.
