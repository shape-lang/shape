from std::core::intrinsics use { Result, Option, AnyError, Ok, Err }

fn read_file() -> Option<int> {
    return None;
}

fn load_config() -> Result<int> {
    let v = (read_file() !! "high level context")?;
    return Ok(v);
}

print("before");
let cfg = load_config()?;
print(f"after {cfg}");
