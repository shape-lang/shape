export const meta = {
  name: 'wf-gc-ph5-snapshot-v7',
  description: 'GC Phase 5 (snapshot side): generalize the snapshot identity-map from SharedCell/Reference to ALL cycle-capable HeapKinds (TypedObject, HashMap, TypedArray, Closure) and bump the wire SNAPSHOT_VERSION v6->v7. Today the two-pass identity-map (materialize_cell_bodies Pass 1 + back-edge links Pass 2, snapshot.rs) preserves identity + dedup only for Arc<SharedCell>/Reference; a snapshot of an OBJECT/array/map cycle (or a shared-identity object referenced twice) either infinite-recurses in the serializer OR duplicates the shared node, losing identity on resume. This ALSO fixes a pre-existing object-cycle serializer infinite-recursion bug (user-noted). v6->v7 with clean version-refusal of older snapshots. Independent Opus verify: an object/map/array cycle round-trips through snapshot->resume with identity preserved (no infinite-recursion, no duplication). Behind default-off gc feature where GC-specific, but the identity-map generalization + infinite-recursion fix are snapshot-correctness (apply regardless of gc).',
  phases: [
    { title: 'Diagnose', detail: 'the object/array/map-cycle serializer behavior today (infinite-recursion/duplication) + the identity-map generalization plan' },
    { title: 'Implement', detail: 'generalize the two-pass identity-map to all cycle-capable HeapKinds + v6->v7 bump + fix the infinite-recursion' },
    { title: 'Verify+Finish', detail: 'independent Opus: object/map/array cycle round-trips with identity preserved; older-snapshot clean-refuse; gates + tests' },
  ],
}
const WT = '/home/dev/dev/shape-lang/shape-w7-gcph5'
const DX = 'direnv exec /home/dev/dev/shape-lang'
const CTX = [
  'Work IN ' + WT + ' (branch wave7/gc-ph5-snapshot-v7, off main HEAD — has GC 0/1/2/3a/§3.5/3c/3b + the resumability + xml + finance-gap merges). Build/test via: ' + DX + ' <cmd>. Design: docs/design/real-gc-cycle-collection.md (§0 #4 snapshot v6->v7 identity-map generalization). Snapshot: crates/shape-runtime/src/snapshot.rs (SNAPSHOT_VERSION:102=6; identity_map:1216 HashMap<u64,u64>; the two-pass materialize_cell_bodies + back-edge machinery ~796-847).',
  '',
  'TODAY: the snapshot two-pass identity-map preserves identity + breaks cycles ONLY for Arc<SharedCell> and Reference (recorded in identity_map as raw-ptr -> base handle; Pass 1 materializes cell bodies, Pass 2 links back-edges via Arc::increment_strong_count). But a cycle through an OBJECT (TypedObject var-field -> ... -> back), a HashMap, or a TypedArray element is NOT in the identity-map: serializing such a cycle either INFINITE-RECURSES (the serializer follows the back-edge forever) or duplicates the shared node (two copies on resume, identity lost). The GC design §0 #4 ratified generalizing the identity-map to ALL cycle-capable HeapKinds at snapshot v6->v7, which ALSO fixes the pre-existing object-cycle serializer infinite-recursion.',
  '',
  'PHASE 5 (snapshot side) — generalize the identity-map: record EVERY cycle-capable heap node (TypedObject, HashMap, TypedArray, Closure — the same kinds the GC for_each_heap_child walks) in the identity_map on first visit (raw-ptr -> handle); on a repeat visit emit a BACK-REFERENCE to the recorded handle instead of recursing (breaks the cycle, dedups shared identity); Pass 2 re-links back-references to the materialized node with the correct Arc strong-count. Bump SNAPSHOT_VERSION 6->7; older (v6) snapshots version-REFUSE cleanly (a clear error, never a misparse). Preserve the existing SharedCell/Reference identity behavior (generalize, do not regress it).',
  '',
  'CONSTRAINTS (CLAUDE.md + design): NO forbidden patterns (no ValueWord serialization-helper, no tag-decode, no Bool-default — the §Forbidden "keep ValueWord for serialization" refusal applies HARD here). Per-slot kind metadata drives serialization (§2.7.7). Identity via raw-ptr handle, not a parallel discriminator. NO infinite recursion. ' + DX + ' just check-no-dynamic EXIT 0; ' + DX + ' just check-clean EXIT 0.',
  '',
  'REGRESSION-TEST REQUIREMENT (user rule): (a) an OBJECT cycle (`type Node { var next: Node? }` self-linked) round-trips through snapshot->from_snapshot with identity preserved (the two nodes resume as the SAME shared node, no infinite-recursion, no duplication); (b) a HashMap and a TypedArray holding a shared/cyclic node round-trip with identity; (c) a doubly-referenced (shared, acyclic) object resumes as ONE node (dedup); (d) the existing SharedCell/Reference identity + all scalar/container round-trips still pass; (e) a v6 snapshot is cleanly refused at v7. No new #[ignore].',
  '',
  'STRUCTURED-OUTPUT: ONE clean JSON object, 1-4 plain sentences per field, NO XML/code blocks.',
].join('\n')
phase('Diagnose')
const D = { type:'object', additionalProperties:false, required:['today_behavior','generalization_plan','version_plan'], properties:{ today_behavior:{type:'string',description:'object/array/map-cycle serializer behavior today (infinite-recursion vs duplication), pinned'}, generalization_plan:{type:'string',description:'how to record all cycle-capable HeapKinds in the identity-map + back-reference on repeat'}, version_plan:{type:'string',description:'v6->v7 bump + clean older-snapshot refusal'} } }
const d = await agent(CTX + '\n\nPHASE 1 — DIAGNOSE. Pin today behavior + the generalization + version plan. Do NOT commit.', { label:'diagnose', phase:'Diagnose', effort:'high', schema:D })
phase('Implement')
const I = { type:'object', additionalProperties:false, required:['status','files_changed','cycle_roundtrips','no_infinite_recursion','evidence'], properties:{ status:{type:'string',enum:['done','partial','blocked']}, files_changed:{type:'string',description:'the identity-map generalization + v7 bump'}, cycle_roundtrips:{type:'boolean',description:'object/map/array cycle round-trips with identity preserved'}, no_infinite_recursion:{type:'boolean',description:'the pre-existing object-cycle serializer infinite-recursion is fixed'}, evidence:{type:'string',description:'object cycle round-trips; dedup works; v6 refused; check-no-dynamic EXIT 0'} } }
const impl = await agent(CTX + '\n\nDIAGNOSIS: ' + JSON.stringify(d) + '\n\nPHASE 2 — IMPLEMENT the identity-map generalization + v6->v7 (no forbidden serialization-helper). ' + DX + ' just check-no-dynamic EXIT 0. Commit (git add -A && git commit --no-verify -m "GC Phase 5 snapshot: generalize identity-map to all cycle-capable HeapKinds + v6->v7 (fixes object-cycle serializer infinite-recursion)").', { label:'implement', phase:'Implement', effort:'xhigh', schema:I })
phase('Verify+Finish')
const V = { type:'object', additionalProperties:false, required:['verdict','cycle_roundtrips','dedup_works','version_refuse','no_regression','gates','merge_ready'], properties:{ verdict:{type:'string',enum:['CONFIRMED','REFUTED','PARTIAL']}, cycle_roundtrips:{type:'boolean',description:'from YOUR OWN run: object/map/array cycle round-trips with identity preserved, no infinite-recursion'}, dedup_works:{type:'boolean',description:'a doubly-referenced shared node resumes as ONE node'}, version_refuse:{type:'boolean',description:'a v6 snapshot is cleanly refused at v7 (clear error, no misparse)'}, no_regression:{type:'boolean',description:'existing SharedCell/Reference identity + all scalar/container round-trips still pass; no forbidden pattern'}, gates:{type:'string',description:'check-clean + check-no-dynamic + snapshot tests'}, merge_ready:{type:'boolean'} } }
const v = await agent(CTX + '\n\nDIAGNOSIS: ' + JSON.stringify(d) + '\nIMPL: ' + JSON.stringify(impl) + '\n\nYou are an INDEPENDENT adversarial reviewer, FRESH context. From scratch: (1) does an OBJECT cycle (self-linked Node) round-trip snapshot->from_snapshot with identity PRESERVED (same shared node, NO infinite-recursion, NO duplication)? a HashMap + TypedArray holding a cyclic/shared node? (2) does a doubly-referenced shared acyclic object resume as ONE node (dedup)? (3) is a v6 snapshot cleanly REFUSED at v7? (4) do the existing SharedCell/Reference identity + all scalar/container round-trips still pass, with NO forbidden pattern (no ValueWord serialization-helper/tag-decode/Bool-default — check-no-dynamic EXIT 0)? Any infinite-recursion, lost identity, misparse, or regression = REFUTED. Add the regression tests; no new #[ignore]. Run ' + DX + ' just check-clean, ' + DX + ' just check-no-dynamic, ' + DX + ' cargo test -p shape-runtime and -p shape-vm (snapshot). Commit any added tests (git commit --no-verify -m "GC Phase 5 snapshot finalize: cycle-identity round-trip regression tests").', { label:'verify-finish', phase:'Verify+Finish', effort:'xhigh', schema:V })
return { d, impl, v }
