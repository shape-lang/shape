# Fable independent verification — complex features (2026-07-06, main a58a7324)

Independent Fable-model adversarial verification (READ-ONLY). Workflow `docs/cluster-audits/fable-verify-complex.js`.

## Verdicts

- **polyglot**: SOUND (forbidden_clean=true)
- **distributed-transfer**: DEFECTS (forbidden_clean=true)
- **polyglot-distributed**: DEFECTS (forbidden_clean=true)
- **correctness-core**: SOUND (forbidden_clean=true)

## Overall
Not done right as a whole. Two of the four pillars survive adversarial verification cleanly: correctness-core (all six audit-critical fixes independently reproduced under vm AND jit, 466/467 vm-jit parity with zero unexpected divergences, real server-side sandbox differential, strict typing enforced at compile time) and local polyglot (real CPython/deno execution proven by extension-removal probes, extern C with out-params, Q13 Result enforcement, identical vm/jit output). Plain distributed transfer is also genuine â code demonstrably executes on the serve node (blobs=N in server logs), and there is no silent local fallback anywhere. But the COMPOSED distributed surface â precisely the user's 2026-07-05 priority spine (polyglot x distributed must compose) â is refuted: every @remote transfer of a foreign-bearing function (python/typescript/C) fails at the receiver on merged main; remote bodies that call stdlib module functions (env/file/http) fail with an internal Null-callee error; remote::call's documented Result<R, RemoteError> contract does not exist as user surface (the documented match pattern crashes at runtime); the receiver-owned permission-over-wire load refusal never fires for real compiled programs because transferred blobs carry empty required_permissions; and the book plus WF-2F close matrix claim a 9-cell matrix green with "captured outputs" of which 6 cells do not reproduce. The redeeming property is failure honesty: every broken path fails loudly with exit!=0 â no silent-wrong output, no silent local execution, no sandbox escape observed. Verdict: foundation sound, composition layer broken, documentation ahead of reality.

## Confirmed sound
CONFIRMED GENUINE (strongest evidence each):
1. Local polyglot execution (python/typescript/C), vm+jit â decisive probe: with ~/.shape/extensions/libshape_ext_python.so moved aside, the same python program fails cleanly ("no extension provides language 'python'") instead of silently succeeding; restored, py_add(19,23)=42 and sys.version_infoâ"py3.13" prove real CPython (ldd shows libpython3.13.so.1.0).
2. extern C FFI incl. out-params â custom gcc-built libprobe.so: add3(10,20,12)=42, dscale(4.0)=10.0, out-param write_out returned 42 via the generated cell alloc/read/free stub, identical vm/jit; missing symbol gives structured RuntimeError naming function+library+symbol+line, exit 1.
3. Q13 foreign-return conformance â fn python returning string against Result<int> yields catchable Err "TypeConformanceError: expected int, got string"; python raise / ts throw both catchable; program continues; vm output diff-identical to jit.
4. Plain @remote transfer is genuinely remote â server log "[serve] inbound Call fn=\"add___impl\" blobs=1" proves body transfer + server execution; with server down both modes hard-error "Connection refused" exit 1 (never local-executes); transitive user-fn dependency closure works (blobs=2, DEP_RESULT=25); heap returns (Array, TypedObject) come back intact.
5. Snapshotâresume across foreign frames, all 3 languages â decisive probe: resuming hash 2f8ca4f6 with extensions moved aside restored state then failed the post-resume foreign call with a language-named error, proving the successful resume genuinely re-linked and re-executed python (restored_before=105 without re-printing before=105 = true re-entry).
6. TLS server termination â openssl s_client to :9742 completes real TLSv1.3 (TLS_AES_256_GCM_SHA384); plaintext client rejected server-side; 0.0.0.0 without cert/auth-token honestly refused at startup.
7. Correctness-core, all six audit items: (a) JIT double-execution gone â print called 150x prints exactly 150 X's in both modes; parity harness MATCH=466 unexpected=0; (b) HashMap arity-2 filter/map identical vm/jit and stable across runs; (c) i64 overflow traps natively under JIT (binop_int_checked at shape-jit/src/mir_compiler/rvalues.rs:1553), exit 1, no silent wrap; (d) Drop-at-escape correct (returned/captured Drop-bearing values finalize after last use); (e) security differential real â strict serve blocks file::write_text server-side (file NOT created), sandbox none writes it; (f) strict typing enforced â stringâint, 3+"x", if 5, intânumber without `as`, bare Option all compile-error, no ReliableOnly bypass.
8. Forbidden-pattern hygiene â `just check-no-dynamic` exit 0 in all four domains; greps of remote.rs, call_convention.rs, serve_cmd.rs, shape-wire find no live ValueWord/is_tagged/tag-decode/synthesize_value_word (one Bool-default-family exception flagged as part of defect 1 below).

## Defects (ranked, all reproduced on merged main)
Ranked, all reproduced hands-on on merged main (HEAD a58a7324 = eacbed65 + docs-only commit), release binary, vm and jit:

1. [CRITICAL] @remote x foreign composition totally broken at the receiver â the user's #1 priority cell. Any @remote fn python/typescript/extern C fails: arity>=1 â "frame_descriptor has 0 slots but arity is 1" (crates/shape-vm/src/remote.rs:1139); arity 0 â server-side "call_value_immediate_nb: callee must be ... got Bool" (crates/shape-vm/src/executor/call_convention.rs:1150). Root cause: sender's build_minimal_blobs_by_hash (remote.rs:583-618) follows only static blob.dependencies edges, so the foreign stub (reached via LoadModuleBinding+CallValue) never travels (serve log: blobs=1 foreign_entries=1); receiver's initialize_foreign_stub_bindings (crates/shape-vm/src/executor/mod.rs:943-973) silently `continue`s at :957/:965, leaving a (0, NativeKind::Bool) sentinel to be consumed at dispatch â a Bool-default-family surface-and-stop violation (ADR-006 Â§2.7.8) and the one forbidden-pattern hit found live. Reproduced on strict, strict-empty, and sandbox-none nodes. Likely merge regression: WF-2F (cd9e41e2) fixed this on the blobs=0 full-program path; the merged minimal-blob path (blobs=1) has zero test coverage with foreign_functions non-empty.

2. [CRITICAL, documentation integrity] The book chapter (shape-web/book/book-site/src/content/docs/advanced/polyglot-distributed.mdx) and shape/docs/cluster-audits/wf2f-close-matrix.md claim the 9-cell matrix green with "real captured output" (CELL_PY_TRANSFER=105, CELL_C_TRANSFER=42, CELL_TS_TRANSFER=21, CELL_*_COMBINED). 6 of 9 cells (entire transfer + combined rows) are irreproducible â every one errors per defect 1. Only the snapshotâresume row (3 cells) reproduces. Distinct from the KNOWN in-flight WF-2G combined-persist gap: the combined cells never even reach the documented Barrier Err â they die on transfer first.

3. [CRITICAL] Realistic remote bodies fail on the receiver: any transferred function calling a stdlib MODULE-level fn (env::cwd(), file::write_text, http::...) dies with "call_value_immediate_nb: callee must be ... got Null". Receiver initializes only foreign-stub module bindings (remote.rs:1027 initialize_foreign_stub_bindings); native stdlib module bindings stay Null because per-function dispatch never runs top-level module init. Blocks the primary distributed use cases (I/O, env); only pure operator/method bodies and user-fn deps work.

4. [HIGH] remote::call's public Result<R, RemoteError> contract is fiction. Stdlib (crates/shape-runtime/stdlib-src/core/remote.shape) and design Â§4.1.1 promise Result; the compiler lowers to __call_raising typed at the BARE return type (crates/shape-vm/src/compiler/function_calls.rs:6027-6047). Repro: `let r = remote::call("127.0.0.1:9740", mul, 6, 7)` prints RAW=42 (bare int); the DOCUMENTED `match ... { Ok(v)=>, Err(e)=> }` type-checks then CRASHES at runtime ("No match arm matched the value"), vm and jit. The recoverable-error surface â the entire point of remote::call â does not exist.

5. [HIGH] Receiver-owned permission-over-wire load refusal is non-functional for real programs. A strict node (granted=[]) did NOT refuse a transferred fs.write function at load â no PermissionDenied; it proceeded to execution (and died on defect 3 instead). Cause: transferred per-function blobs carry empty required_permissions; record_blob_permissions fires only for NAMED imports into the top-level __main__ blob (crates/shape-vm/src/compiler/statements.rs:1939, compiler_impl_initialization.rs:324-332); namespace imports record nothing and the callee's own blob never gets its body's permissions. The Â§4.6 refusal path (remote.rs:977 load_linked_program_with_permissions) is unit-tested only with hand-stuffed blob.required_permissions (remote.rs:3614) â never fed real derived data. The "zero sender trust" load-refusal claim is refuted; runtime check_permission remains as backstop but the system currently fails-closed for the WRONG reason (defect 3) â fixing 3 alone may open this hole.

6. [HIGH] Receiver ffi_languages enforcement unreachable end-to-end. The clean refusal (crates/shape-vm/src/executor/control_flow/mod.rs:1174-1185) passes unit tests but: (a) via @remote a strict-empty node returns the defect-1 Bool error, indistinguishable from an opted-in node; (b) via remote::execute, even a node started `--sandbox none --ffi-languages python` (log-confirmed granted) errors "no extension provides language 'python'" â the Execute handler does not wire the serve node's loaded language runtimes into its executing engine, so the POSITIVE half fails too; (c) default strict + --ffi-languages python still refuses at load with "requires permissions not granted: ffi.call" â the opt-in flag does not grant the permission it gates on.

7. [MEDIUM] `use std::core::snapshot` + bare snapshot() fails locally with the same internal "got Bool" error (exit 1) even in a no-foreign program; only `from std::core::snapshot use { snapshot, ... }` works. Same uninitialized-module-binding class as defect 1(b).

8. [MEDIUM] Extension-version skew detection is design-only: ExtensionReq (polyglot-distributed-integration.md A2, ratified 2026-07-05) has zero code hits in crates/; ForeignFunctionEntry::compute_content_hash (crates/shape-vm/src/bytecode/core_types.rs:200-231) omits extension version. Foreign SOURCE hashing itself IS implemented and receiver-verified (remote.rs:938-952).

9. [LOW] Combined @remote+snapshot cell fails with the defect-1 error before reaching the WF-2G-documented Barrier Err â honest failure, wrong shape. This is the item overlapping the known in-flight WF-2G gap; items 1-8 are real defects independent of it.

## Residual doubt
1. TLS client transport: none exists (shape-wire tcp.rs is plaintext-only; only QUIC has a rustls ClientConfig), so end-to-end encrypted TRANSFER from a Shape client could not be exercised â TLS termination was proven only server-side via openssl s_client. "Transfer over the encrypted channel" remains unverified by construction, not by pass.
2. MissingModuleFunction retry-once resupply (remote.rs:2009 call_with_resupply) is unit-tested (remote.rs:3703+, 3935+) but could not be triggered live â the sender proactively ships the full transitive blob closure in every observed Call, so the live resupply path is unexercised.
3. Defect 1's "merge regression from WF-2F" attribution is inferred from the blobs=0-vs-blobs=1 path difference and cd9e41e2's fix scope, not proven by bisect.
4. No actual sandbox escape was demonstrated through the empty-required_permissions hole (defect 5) because defect 3 kills the payload first and runtime check_permission stands behind it â whether the runtime backstop alone fully covers all transferred-code I/O once defect 3 is fixed was not verified.
5. JIT performance claims for polyglot and print-bearing programs are unverifiable as "JIT": foreign-bearing functions never compile (shape-jit/src/compiler/accessors.rs:688, documented J1 deopt) and any main calling print deopts whole-program ([jit-fallback], ffi/control/mod.rs:704-715) â semantics verified identical, but most real programs run fully interpreted under --mode jit today.
6. One pinned known-red vm/jit divergence remains in the 467-program parity corpus (ACC_...unknown_hof_return_kind) â accepted as tracked, not re-diagnosed here.
7. Book-wide truth was not re-measured: per prior audit ~388 of 498 runnable=false fences fail; this verification only refutes the specific 9-cell matrix claims (defect 2), and the broader book-vs-binary gap remains unquantified at this HEAD.