enum Color { Red, Green, Blue }
fn g(v: int) -> Result<int, string> { Ok(v) }
fn use_it(v: int) -> int {
  match g(v) {
    Ok(Red) => 1,
    _ => 99,
  }
}
print(use_it(0))
print(use_it(5))
print(use_it(1))
