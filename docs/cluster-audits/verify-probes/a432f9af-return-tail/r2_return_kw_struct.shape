type Point { x: int, y: int }
fn g() -> Result<Point, string> { Ok(Point { x: 1, y: 2 }) }
fn sink(n: int) -> int { n + 1 }
fn use_it() -> int {
  return match g() {
    Ok(Some(n)) => sink(n),
    _ => 0,
  };
}
fn main() { print(use_it()) }
