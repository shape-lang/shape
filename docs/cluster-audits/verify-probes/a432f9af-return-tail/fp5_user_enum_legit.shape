enum Shape { Circle(int), Square(int) }
fn g() -> Result<Shape, string> { Ok(Shape::Circle(10)) }
fn use_it() -> int {
  match g() {
    Ok(Shape::Circle(r)) => r,
    Ok(Shape::Square(s)) => s * 2,
    Err(_) => -1,
  }
}
print(use_it())
