type Point { x: int, y: int }
fn g() -> Array<Result<Point, string>> { [Ok(Point { x: 1, y: 2 })] }
fn use_it() -> int {
  match g() {
    [Some(n), _] => 1,
    _ => 0,
  }
}
fn main() { print(use_it()) }
