enum Color { Red, Green, Blue }
type Point { x: int, y: int }
fn g() -> Result<Point, string> { Ok(Point { x: 11, y: 22 }) }
fn use_it() -> int {
  match g() {
    Ok(Red) => Red,
    _ => 99,
  }
}
print(use_it())
