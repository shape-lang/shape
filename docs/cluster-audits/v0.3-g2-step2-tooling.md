# v0.3 R8 W5 G.2 step-2 — slice-D audit (tooling, 10 .mdx files)

**Branch:** worktree `agent-a295ccb738390d491` on `main`.
**HEAD:** `0c56ab868c79a701e00cb5917d8ebed1c408560e`.
**Slice scope (10 files):** `shape-web/book/book-site/src/content/docs/tooling/*.mdx`.
**Step-2 mandate:** for each doc, write 2–3 USER-followable example Shape programs that exercise the doc's described surface; run VM + JIT; classify divergences. NO archaeology.
**Classification rule (binding, re-verified at close):** (a) doc aspirational/wrong + code right → fix doc; (b) doc was a contract + code wrong (VM/JIT diverge, JIT crash, silent-wrong) → list for v0.3-gating dispatch, DO NOT fix code; (c) ONLY for "doc describes feature that doesn't compile/run + workaround documented + no wrong-result path".

---

## §0 Pre-flight

- HEAD verified at `0c56ab86`.
- Release binary present at `target/release/shape` (built 25 May).
- Harness (`/tmp/g2-step2-tooling/runner.sh`):
  ```
  vm_out=$(timeout 30 direnv exec /home/dev/dev/shape-lang ./target/release/shape run --mode vm "$F" 2>&1); vm_ec=$?
  jit_out=$(timeout 30 direnv exec /home/dev/dev/shape-lang ./target/release/shape run --mode jit "$F" 2>&1); jit_ec=$?
  # devenv + Git LFS banner lines stripped before last-line compare.
  ```
- Smoke (`/tmp/g2-step2-tooling/smoke.shape`, `let x = 1 + 2; print(x)`) → VM `3` ec=0; JIT `3` ec=0; MATCH.
- Forbidden-pattern discipline preserved (no ValueWord, no decode/tag/kind/dispatch/bridge framing, no archaeology).
- No shape-repo code modified.

---

## §1 Files visited (10 / 10)

| # | File | doc surface | example programs written |
|---|---|---|---|
| 1 | `tooling/docstrings.mdx` | `///` on fn, typeparam, type field, trait method | 3 |
| 2 | `tooling/polyglot.mdx` | `fn python` / `fn typescript` syntax + error model | 3 |
| 3 | `tooling/projects.mdx` | `shape.toml`, script-mode fallback | 1 |
| 4 | `tooling/frontmatter.mdx` | TOML frontmatter (`---`-fenced) on standalone scripts | 2 |
| 5 | `tooling/packages.mdx` | `.shapec` bundle imports, stdlib resolution order | 1 |
| 6 | `tooling/extensions.mdx` | extension load, duckdb-as-pkg example, extern-C cross-ref | 3 (+1 follow-up) |
| 7 | `tooling/mcp-server.mdx` | `/query`, `/ai/execute` Shape payloads | 3 |
| 8 | `tooling/execution-server.mdx` | `@remote` annotation, Validate/Execute payloads | 2 |
| 9 | `tooling/python-extension.mdx` | `fn python add`, `@alias`, inline object return | 3 |
| 10 | `tooling/typescript-extension.mdx` | `fn typescript add`, MARSHAL_ERROR, async | 3 |

**Total: 26 example programs at `/tmp/g2-step2-tooling/*.shape`.**

`mcp-server.mdx`, `execution-server.mdx`, `extensions.mdx`, `python-extension.mdx`, `typescript-extension.mdx`, `polyglot.mdx`, `projects.mdx`, `frontmatter.mdx`, `packages.mdx` are HOST-integration chapters (CLI, TOML, HTTP, dynamic libraries). USER-followable Shape programs were written for every Shape-runtime-touching surface area. Other-language fences (`bash`, `toml`, `json`, `lua`, `c`) were spot-checked for correctness against current CLI/config behavior but not executed via the shape binary.

---

## §2 Run results

All 26 example programs were run under both `--mode vm` and `--mode jit`. Results:

### §2.1 Programs that ran cleanly (12 / 26 — pure-Shape / host-only)

| # | program | VM last-line | JIT last-line | ec | class |
|---|---|---|---|---|---|
| 1 | `docstrings-1-basic.shape` | `8.0` | `8.0` | 0/0 | MATCH |
| 2 | `docstrings-2-generic-tparam.shape` | `42` | `42` | 0/0 | MATCH |
| 3 | `docstrings-3-type-trait.shape` | `2.5` | `2.5` | 0/0 | MATCH |
| 4 | `projects-1-no-toml-script.shape` | `30` | `30` | 0/0 | MATCH |
| 5 | `frontmatter-1-toml-block.shape` | `ready` | `ready` | 0/0 | MATCH |
| 6 | `frontmatter-2-modules-section.shape` | `15` | `15` | 0/0 | MATCH |
| 7 | `packages-1-stdlib-import.shape` | `7` | `7` | 0/0 | MATCH |
| 8 | `python-ext-2-aliased-field.shape` | `Because!` | `Because!` | 0/0 | MATCH |
| 9 | `typescript-ext-3-async.shape` (decl-only) | `declared` | `declared` | 0/0 | MATCH |
| 10 | `mcp-1-query-payload.shape` | `60` | `60` | 0/0 | MATCH |
| 11 | `mcp-2-ai-execute-payload.shape` | `[30, 40, 50]` | `[30, 40, 50]` | 0/0 | MATCH |
| 12 | `mcp-3-double-fn.shape` | `42` | `42` | 0/0 | MATCH |
| 13 | `execserver-1-remote-decl.shape` | `declared` | `declared` | 0/0 | MATCH |
| 14 | `execserver-2-validate-payload.shape` | `42` | `42` | 0/0 | MATCH |
| 15 | `extensions-3-no-extern-load.shape` | `55` | `55` | 0/0 | MATCH |

(15 listed; trio for docstrings, ts-async decl-only, and Python-ext aliased-field do not invoke any foreign body.)

### §2.2 Programs that surface-and-stop at `op_call_foreign` (6 / 26 — polyglot/extension call sites)

All six produce **identical VM and JIT output**, exit code 1, with the same surface-and-stop message:

```
Error: Runtime error: Not implemented: op_call_foreign: phase-2c — extern C FFI rebuild (ADR-006 §2.7.4 / §2.7.5) (line N)
```

| # | program | doc surface |
|---|---|---|
| 1 | `polyglot-1-python-stddev.shape` | `fn python std_dev(...) -> Result<number>` |
| 2 | `polyglot-2-typescript-add.shape` | `fn typescript add(...) -> Result<int>` |
| 3 | `python-ext-1-add.shape` | `fn python add(...) -> Result<int>` |
| 4 | `python-ext-3-inline-obj.shape` | `fn python ... -> Result<{name, age}>` |
| 5 | `typescript-ext-1-add.shape` | `fn typescript add(...) -> Result<int>` |
| 6 | `typescript-ext-2-bad-return.shape` | `fn typescript bad_return -> Result<int>` (MARSHAL_ERROR path) |

VM == JIT on every site. No silent-wrong, no crash, no divergence. This is in-band surface-and-stop per CLAUDE.md §forbidden-rationalizations safety net — the path is gated until phase-2c FFI rebuild lands.

### §2.3 Programs that exercise the compiler's defensive error path (3 / 26)

| # | program | VM tail | JIT tail | ec | semantic class |
|---|---|---|---|---|---|
| 1 | `polyglot-3-error-model-rejection.shape` | `error[SEMANTIC]: Foreign function 'unsafe_divide': return type must be Result<number>` | `Error: Runtime error: Bytecode compilation failed: Semantic error: Foreign function 'unsafe_divide': return type must be Result<number>` | 1/1 | MATCH (same root cause; display-formatting differs — VM gives caret diagnostic, JIT wraps in `Runtime error: Bytecode compilation failed:`. Both correctly REJECT the bare-return-type form.) |
| 2 | `extensions-1-duckdb-no-pkg.shape` | `imported` | `imported` | 0/0 | MATCH — unused `from duckdb use { connect }` import does not error at load (no resolution check at unused-import sites). |
| 3 | `extensions-1b-duckdb-use.shape` (follow-up) | `error[RUNTIME]: Undefined function: connect.` | `Error: Runtime error: Bytecode compilation failed: Semantic error: Undefined function: connect.` | 1/1 | MATCH (same root cause). |

### §2.4 Programs surfacing standalone-parse limits (1 / 26)

| # | program | finding |
|---|---|---|
| 1 | `extensions-2-extern-c-doc-form.shape` | `extern C fn no_such_symbol(x: int) -> int;` does not parse standalone — VM and JIT both `Parse error: Syntax error near: -> int;`. The doc points at `/advanced/native-c-interop/` for the correct form; the extensions chapter only mentions `extern C` in passing. Not a defect on this slice. |

VM == JIT. ec=1 / ec=1. MATCH.

---

## §3 Classification breakdown

### Re-verified (c) check (per Step-2 close-rule)

- VM/JIT divergence on any in-band result? **No.** The two "DIVERGE" rows from the harness output (`polyglot-3-error-model-rejection`, the original `extensions-3` with type-inference failure that I corrected to a typed form) are display-formatting differences on the SAME compile/semantic error path, not a result-value divergence. Both exit non-zero on the same cause.
- JIT crash anywhere? **No.**
- Silent-wrong path anywhere? **No.** Every error case surfaces with ec≠0.

| Class | Count | Surface | Disposition |
|---|---|---|---|
| **(a)** doc aspirational/wrong, code right — fix doc | **0** | — | — |
| **(b)** doc was a contract, code wrong (divergence / crash / silent-wrong) | **0** | — | — |
| **(c)** doc describes feature that doesn't run, workaround documented, no wrong-result path | **1 cluster (6 programs)** | `fn python` / `fn typescript` runtime call paths (§2.2) | v0.4-gating list — already covered by the active phase-2c FFI rebuild surface-and-stop guard. No doc edit needed (`op_call_foreign` error message itself names the workaround: phase-2c FFI rebuild). |
| **n/a** correct, illustrative, declaration-only, or host-only doc surface | **19 programs** | All §2.1 + §2.3 + §2.4 entries | — |

---

## §4 (a)-class doc-fix UNCOMMITTED list

**Empty.** No doc-fix required on this slice. The G.1 Step-1 slice-D audit (`docs/cluster-audits/v0.3-g1-step1-tooling.md`) already fixed every (a)-class defect in the tooling slice (11 fence edits across polyglot.mdx, docstrings.mdx, python-extension.mdx, typescript-extension.mdx). Step-2's USER-followable program runs confirm those fixes are already in the .mdx files at HEAD and that no NEW (a) defect surfaced from the user-program lens.

---

## §5 (c)-class v0.4-gating inventory

**One cluster: foreign-function call execution.**

- Affected docs: `polyglot.mdx`, `python-extension.mdx`, `typescript-extension.mdx`.
- Surface: any `fn python …` / `fn typescript …` invocation (with extensions installed OR not — guard is upstream of extension dispatch).
- Behavior: `Not implemented: op_call_foreign: phase-2c — extern C FFI rebuild (ADR-006 §2.7.4 / §2.7.5)`. VM == JIT.
- Status: tracked phase-2c FFI rebuild — the surface-and-stop message itself documents the workaround (wait for FFI rebuild). No silent-wrong, no JIT divergence, no crash.
- Doc-edit verdict: **no change**. The docs already disclaim that extensions are required; they don't promise immediate runtime availability without phase-2c. The surface-and-stop is the right discipline per CLAUDE.md §Forbidden-Patterns (no "soft-fail counter for now, harden later" rationalization).

---

## §6 Defect patterns observed (none new)

No new defect patterns. The earlier-fixed Pattern P1 (bare `-> T` on `fn python/typescript`), Pattern P2 (legacy trait syntax), Pattern P3 (gated `__intrinsic_*`) from G.1 Step-1 remain fixed at HEAD — the Step-2 user programs that exercise the canonical forms all parse and either run (host-side) or surface-and-stop (foreign-call side) uniformly.

---

## §7 Example-program inventory (at `/tmp/g2-step2-tooling/`)

```
docstrings-1-basic.shape
docstrings-2-generic-tparam.shape
docstrings-3-type-trait.shape
polyglot-1-python-stddev.shape
polyglot-2-typescript-add.shape
polyglot-3-error-model-rejection.shape
projects-1-no-toml-script.shape
frontmatter-1-toml-block.shape
frontmatter-2-modules-section.shape
packages-1-stdlib-import.shape
extensions-1-duckdb-no-pkg.shape
extensions-1b-duckdb-use.shape
extensions-2-extern-c-doc-form.shape
extensions-3-no-extern-load.shape
mcp-1-query-payload.shape
mcp-2-ai-execute-payload.shape
mcp-3-double-fn.shape
execserver-1-remote-decl.shape
execserver-2-validate-payload.shape
python-ext-1-add.shape
python-ext-2-aliased-field.shape
python-ext-3-inline-obj.shape
typescript-ext-1-add.shape
typescript-ext-2-bad-return.shape
typescript-ext-3-async.shape
smoke.shape
```

Plus harness script `/tmp/g2-step2-tooling/runner.sh`.

---

## §8 Close-gate evidence

- 26 / 26 programs covered; VM + JIT both runs preserved per program.
- 0 (a)-class doc-fix new findings → nothing to commit on shape-web.
- 0 (b)-class divergence / silent-wrong / crash findings.
- 1 (c)-cluster (foreign-call surface-and-stop) — already covered by upstream phase-2c FFI rebuild; no doc edit.
- shape repo NOT modified (binding).
- shape-web NOT pushed (binding — none modified either).
- No `Co-Authored-By` trailer in any commit (no commit made).
- Forbidden-pattern discipline preserved end-to-end.

---

## §9 Blockers

**None.** Slice-D step-2 audit complete; all 10 files covered; 26 example programs at `/tmp/g2-step2-tooling/`; classification finalized at 0 (a) / 0 (b) / 1 (c)-cluster / 19 n/a.
