# Shape v0.3 Roadmap

**Generated:** 2026-05-17 post-v0.2.0 ship (main @ 5daea484).
**Updated:** 2026-05-18 — Phase 4 expansion per user acceptance criterion revision (test coverage ≥99% per-feature + book 100% correct vs HEAD + W13 differential fuzz; Q25.C.6 IC devirtualization deferred to v0.4).
**Status:** Phase 4 IN FLIGHT.

## v0.3 acceptance criteria (Phase 4 revised; binding)

- **Operator coverage full set** for user types: arithmetic (Add/Sub/Mul/Div/Mod/Neg) + compound-assign + comparison (Eq/Ord) + bitwise (BitAnd/BitOr/BitXor/Shl/Shr) + Not + Index/IndexMut, all VM == JIT smoke-tested (substantially MET at HEAD per W1.1–W1.11)
- **LSP feature-spec en-par with rust-analyzer** for core features: hover, completions, diagnostics, inlay hints, go-to-def, find-references, rename, code-actions, semantic tokens, document/workspace symbols (substantially MET at HEAD per W2.1 + W2.2/W2.3/W2.4/W2.5/W2.6/W2.7/W2.8/W2.9 closure-waves; cost-class inlay hints W2.10 carries documented exception gated on Q25.C.6 deferred to v0.4)
- **Book 100% correct vs HEAD** (W15): zero made-up content; every labeled-runnable snippet runs cleanly VM == JIT; CI book-truth-gate enforces at every commit
- **Test coverage ≥99% per-feature** (W14): cargo-tarpaulin line + branch + dead-code measurement; DOCUMENTED EXCEPTIONS ONLY WHEN VERY HARD TO TEST
- **W13 differential fuzz operational** for VM vs JIT divergence on input corpora
- **W12 fall-through + `[jit-fallback]` diagnostic** operational (MET; commit `4083eb55`)
- **v2-raw residuals resolved** (MET; W5 commit `35b948ed`)
- **Phase 4 imprecision 83 root cause fix** (MET; W6 commit `ac885519`)
- **Smoke matrix 5/5 VM == JIT preserved** at every close per release-binary corrected harness
- **0 bad-code merges into main** preserved per corrected harness

## Workstreams

### W1 — Operator coverage (full set)

Mirror Phase 4 Add/AddAssign pattern: trait declaration in `crates/shape-runtime/stdlib-src/core/` + prelude import + UFCS dispatch arm + smoke fixtures VM == JIT.

| Sub-cluster | Operator(s) |
|---|---|
| W1.1 | Sub + SubAssign |
| W1.2 | Mul + MulAssign |
| W1.3 | Div + DivAssign |
| W1.4 | Mod + ModAssign |
| W1.5 | Neg |
| W1.6 | Not |
| W1.7 | Eq (`==`, `!=`) |
| W1.8 | Ord / PartialOrd (`<`, `<=`, `>`, `>=`) |
| W1.9 | BitAnd + BitOr + BitXor + Assign variants |
| W1.10 | Shl + Shr + Assign variants |
| W1.11 | Index + IndexMut |

Dispatch: parallel sub-agents (territory non-overlap — each touches its own stdlib file + per-operator dispatch arm; Phase 4 imprecision 83 fix should land in W6 first to remove return-typeless prelude trait method monomorphization risk). Scope: ~150-300 LoC per sub-cluster.

### W2 — LSP feature-spec en-par with rust-analyzer

**W2.1 Audit-day:** single sub-agent produces `docs/cluster-audits/v0.3-lsp-feature-spec.md` mapping rust-analyzer feature coverage vs `shape-lsp` HEAD. Per-feature gap inventory + per-gap scope estimate + closure-wave partition recommendation.

**W2.2-N Closure-waves:** per audit's per-family partition. Expected families:
- Hover (type info + doc lookup)
- Completions (context-aware; trait methods; named exports; closure params)
- Diagnostics (semantic errors with quick-fix code actions)
- Inlay hints (type hints; param names; closure return types; binding-kind hints; Q25.C.7 cost-class hints folds here)
- Go-to-definition + find-references
- Rename
- Code actions (quick-fixes + refactors)
- Semantic tokens (syntax highlighting)
- Formatting / range formatting
- Document symbols / workspace symbols

Scope: 1 audit-day + 6-10 closure-waves; medium-large workstream.

### W3 — Shape book audit + divergence resolution

**W3.1 Audit-day (5 parallel slice agents; non-overlapping book pages):**

| Slice | Territory |
|---|---|
| A | Language reference (syntax + grammar + types + traits + generics + pattern matching + control flow) |
| B | Stdlib reference (collections + IO + concurrency + temporal + decimal + bigint + string) |
| C | Tutorial + getting-started + installation + first-program |
| D | Comptime + polyglot interop (Python/TypeScript/C) + annotations |
| E | JIT + perf + sandboxing + permissions + content-addressed bytecode |

Per-slice deliverable: `docs/cluster-audits/v0.3-book-slice-{A..E}-divergence.md` with per-page divergence table + per-divergence disposition (update book / add to language / mark deferred-with-cite).

**W3.2-N Per-slice fix waves:** parallel post-audit per slice's recommendation. Some fixes are doc-only (book update); some are language additions; some are deletion of stale book pages.

Scope: 5 parallel audit agents + 5 parallel fix waves; bounded per-slice.

### W4 — Q25.C.6 IC devirtualization (dyn dispatch perf) — DEFERRED TO v0.4

Per `docs/cluster-audits/cluster-2-inventory.md` §I + cluster-1.5 Q25.C close: dyn-dispatch fast-path via inline-cache devirtualization. ~500-1500 LoC LARGE scope; multi-session chain candidate per D-α. Deferred to v0.4 per user 2026-05-18 acceptance revision; v0.3 closes without W4. (W2.10 LSP cost-class inlay hints carry as documented exception in W2 since they depend on W4.)

### W5 — v2-raw residuals (consolidated)

`length_typed_object_empty` SIGABRT + `w17_comptime_*` SIGABRTs empirical isolation + fix. Single consolidated sub-cluster mirroring `cluster-1.5-v2-raw-empirical-isolation-and-fix` shape (gdb-backtrace empirical isolation; ReceiverGuard or share-accounting fix per root cause).

### W6 — Phase 4 imprecision 83 root cause

Compiler trait-def return-type propagation gap (return-typeless prelude trait method poisons `Vec.map<U>` monomorphization; Phase 4 applied workaround at `add_assign(other: Self): Self` — root cause untouched). Single bounded sub-cluster on compiler side.

## §Phase-4 expansion (user 2026-05-18 acceptance criterion revision)

Phase 4 expands v0.3 acceptance to require test coverage ≥99% per-feature (W14), book 100% correct vs HEAD (W15), and W13 differential fuzz operational. W11-fup-C closed the Phase 3d trajectory at `672bda24`; Phase 4 begins from that HEAD.

### W11-fup-C — `jit_print_typed_array` FFI body + JIT-allocator element-type stamping (MERGED)

Closed at commit `672bda24`. Phase 4 carries the W11-followup-jit-spread-expand surface as a NEW v0.4 class (per close §4 + close-summary §5.1 entry 10). Phase 4 entry verifies no W11-fup-C post-merge residuals via a status-check sub-agent in Phase 4a.

### W13 — Differential fuzz (VM vs JIT)

Goal: input-corpus differential fuzzing of `--mode vm` vs `--mode jit` execution. Output-divergence (stdout / exit-code / runtime error) is a P0 surface; corpus is hand-seeded + corpus-grown.

- **W13.1 audit-day** — scope cargo-fuzz vs custom harness per Shape's tier-up + JIT-fallback semantics; per-domain inventory (arithmetic / collections / closures / pattern-matching / async / generic-monomorphization); corpus seeding strategy. Deliverable: `docs/cluster-audits/v0.3-w13-differential-fuzz-audit.md`.
- **W13.2-N closure-waves** — wire harness + per-domain corpus; CI integration nightly-only (NOT per-commit per test execution policy).

Scope: 1 audit-day + 2-4 closure-waves; medium.

### W14 — Test coverage (cargo-tarpaulin) ≥99% per-feature

Goal: every shipped feature has tests covering happy path + edge cases + error path. DOCUMENTED EXCEPTIONS ONLY WHEN VERY HARD TO TEST (per-exception cite at audit deliverable + close doc + this roadmap).

- **W14.1 audit-day** — cargo-tarpaulin install + invocation pattern (line + branch + dead-code via `--ignore-tests` + `--skip-clean` variants); per-crate baseline measurement; per-feature coverage classification (happy-path / edge / error / exception-with-cite). Deliverable: `docs/cluster-audits/v0.3-w14-test-coverage-audit.md`.
- **W14.2-N closure-waves** — parallel sub-agent fix waves per W14.1 audit per-crate gaps; batched 5-10 per ceremony round; strict folder/file structure rules per audit; non-overlapping territory.
- **W14.3 coverage gate** — cargo-tarpaulin runs at Phase-4b-batch-merge gate + nightly post-v0.3 (NOT per-commit per test execution policy); per-feature ≥99% threshold OR documented exception.

Absorbs the prior "~48 pre-existing shape-test failures (10 classes)" v0.4 candidate entry — Phase 4 fixes these as part of test hardening.

Scope: 1 audit-day + 4-8 closure-waves; large (per-crate coverage gap surface).

### W15 — Book truth (zero made-up content; 100% correct vs HEAD)

Goal: every snippet in the book is either (a) labeled-runnable and runs cleanly VM == JIT, or (b) labeled non-runnable with explicit "pseudo-code" / "incomplete" / "deferred" cite. CI book-truth-gate enforces.

- **W15.1 re-audit-day** — every labeled-runnable snippet at HEAD against W3.2-A/B/C/D/E residuals; per-divergence disposition (update book / fix language / mark deferred-with-cite).
- **W15.2-N closure-waves** — parallel book fix waves per W15.1 residuals; smaller batches than W14 (book content is per-page non-overlapping by territory).
- **W15.3 book-truth-gate** — CI executes every labeled-runnable snippet under VM AND JIT; fails on output divergence OR runtime error. Lives in book repo CI (shape-web); fallback wire to shape monorepo as needed.

Cross-references: V3-S5 ckpt-5/ckpt-6 STRICT close residuals (close-summary §5.8) block a subset of snippets in `arr_spread{,_min}` / `arr_comp{,2}`; W15.1 audit identifies the subset + marks deferred-with-cite citing the v0.4 candidate.

Scope: 1 re-audit-day + 3-6 closure-waves; medium-large.

## Sequencing recommendation (pre-Phase-4 trajectory; preserved as completed Round 1-3)

**Round 1 (LANDED):** W6 + W3.1 5-slice audit + W5 — parallel territory non-overlap.

**Round 2 (LANDED):** W1.1-W1.11 + W3.2-N per-slice fix waves + W2.1 audit-day — parallel.

**Round 3 (LANDED, W4 DEFERRED):** W2.2-N LSP closure-waves (W2.2/W2.3/W2.4/W2.5/W2.6/W2.7/W2.8/W2.9 closed). W4 IC devirtualization NOT dispatched — deferred to v0.4.

**Phase 3d (LANDED):** W11 partial + W11-fup-A + W11-fup-B + W11-fup-C + W12 — final closure-wave through `672bda24`.

## Phase 4 sequencing (in flight per user 2026-05-18 acceptance revision)

**Phase 4a — parallel audit-day round (4 sub-agents in parallel):**
- W11-fup-C status check (residual surfaces post-merge audit)
- W13.1 differential fuzz audit-day
- W14.1 test coverage audit-day (cargo-tarpaulin install + per-crate baseline + per-feature classification)
- W15.1 book re-audit-day

**Phase 4b — parallel fix waves (batched 5-10 per ceremony round; strict folder/file structure rules per audit):**
- W14.2-N parallel test-coverage fix waves per W14.1 audit per-crate gaps
- W15.2-N parallel book fix waves per W15.1 residuals (smaller batches)
- W13.2-N differential-fuzz harness + per-domain corpus closure-waves

**Phase 4c — gates after batch merges:**
- W14.3 coverage gate (cargo-tarpaulin Phase-4b-batch-merge gate + nightly post-v0.3)
- W15.3 book-truth-gate (CI executes every labeled-runnable snippet under VM AND JIT; fails on output divergence OR runtime error)
- W13 differential-fuzz CI integration (nightly only)

**Round 4: v0.3 close attempt + tag** — after Phase 4c gates lock; smoke matrix 5/5 VM == JIT empirically re-verified; supervisor ratifies; user authorizes `v0.3.0` tag.

### Test execution policy (binding for Phase 4)

- Per-commit gate = FOCUSED tests relevant to the specific change (sub-agent determines scope per change; team-lead enforces at close-gate review)
- Full suite + fuzz = NIGHTLY ONLY (not per-commit; not per-merge-ceremony unless audit-day batch merge)
- Coverage tool runs at Phase-4b-batch-merge + nightly only

## v1 candidates (post-v0.3 — out of v0.3 scope)

- 22/35 remaining per-HeapKind kinded jit_print arms (formatting polish; correctness preserved)
- Q25.C.4 `#[static_only]` resolve UNCOVERED (binary: implement OR remove from ADR)
- AGENTS.md V3-S6 chain rows crate-prefix annotation (doc discipline)
- ~48 pre-existing shape-test failures triage per `wave-3-baseline-classification.md` 10 classes
- stdlib polish (TBD scope; per W3 book audit findings)
- Error message polish (TBD scope; surfaced via W2 LSP diagnostics audit)
- Broader user-facing docs beyond book (API docs / examples / cookbook)
- Stability commitments + deprecation policy + breaking-change policy

## v0.3 trajectory estimate (Phase 4 revised)

Phase 4 begins from main HEAD `672bda24` (post-W11-fup-C close-summary `ec27b1ef` + RFCs `a7e9fe07`); Rounds 1-3 + Phase 3d LANDED.

| Stage | Sessions |
|---|---|
| Phase 4a (W11-fup-C status + W13.1 + W14.1 + W15.1 parallel audit-day) | 1 |
| Phase 4b (W14.2-N + W15.2-N + W13.2-N parallel fix waves) | 3-4 |
| Phase 4c (W14.3 coverage gate + W15.3 book-truth-gate + W13 CI integration) | 1 |
| Round 4 (v0.3 close + tag) | 0.5 |
| **Phase 4 remaining** | **5.5-6.5** |
| **Total with pre-Phase-4 Rounds 1-3 + Phase 3d** | **8-10** |

## Discipline binding (carry forward from Phase 3)

All Phase 3 disciplines apply to v0.3 workstreams:
- Bulldozer cadence with parallel sub-agent dispatch where territory non-overlapping
- Recursive pre-flight check binding for ground-truthable claims at every layer
- Reading 3 cadence-tightening (max ~100 lines per supervisor relay; one refinement pass per dispatch prompt)
- Surface-and-stop discipline for intractable in-wave gaps
- Refuse #10 anti-deferral within wave scope
- 5-arm receiver-recovery + 4-table HeapKind lockstep + §2.7.5 stamp-at-compile-time + ADR-005 §1 single-discriminator
- CLAUDE.md modifications require explicit user authorization
- Cluster/release tag landings require explicit user authorization
- No `Co-Authored-By: Claude` trailer; own all code quality
