# Shape v0.3 close summary (Round-6+ CLOSED — expanded-scope work in flight)

**Trajectory state at:** HEAD `4ffde515` (post §2.7.13 amendment + round-6+ close-doc refresh).
**Predecessor:** v0.2.0 (`5daea484` post-Phase-3 strict-typing migration).
**Status:** Round-6+ CLOSED (15 workstreams merged + verified; round-6+ gating set
empty). v0.3 scope EXPANDED 2026-05-22 by user: tag now gated on (a) four
architectural workstreams + six Known Constraints landing pre-tag, then (b) a
doc-truth + book-driven testing round, then (c) close + tag. v0.3.0 tag held.

v0.3 is the first user-facing-polish cycle after the v0.2.0 strict-typing
architectural rework. Headline deliverables: operator-trait coverage for
user types (W1), LSP feature-spec parity buildout against rust-analyzer
(W2), Shape book divergence resolution against shipped language (W3 +
W15), `--mode jit` semantics binding with interpreter fall-through (W12),
v2-raw residuals resolution (W5), Phase-4 imprecision 83 root cause (W6),
JIT call-method user-trait fix (W10), comptime `type_info` redesign (W7).
Phase 4 acceptance criterion revision (2026-05-18) added book 100%
correct vs HEAD (W15) + test coverage ≥99% per-feature (W14) + W13
differential fuzz as v0.3 close gates. Round-6+ added 15 no-known-
incorrectness sub-clusters (WS-1…WS-12). The 2026-05-22 expanded-scope
re-acceptance adds W16.2-J PHF-retirement, W17.3-4 per-container
FieldType, phase-2c host-tier marshal/snapshot rebuild, the 6 Known
Constraints, and a 2-step doc-truth + book-driven testing round.

---

## §0 v0.3 acceptance criteria (2026-05-22 EXPANDED + same-day dialogue additions) + current state at HEAD 43d3f86c

Acceptance criteria per user 2026-05-22 expanded-scope dispatch (initial
4 criteria + the doc-truth round) **plus three same-day dialogue
additions** (W18 content-rendering rebuild as criterion E; Len trait +
membership-naming standardization as criterion F; KC #5 → DROP test;
KC #2 still pending; comptime trait primitive pending user yes/no).
The 2026-05-18 Phase 4 revision is preserved at §0.B for archival.

**§0.A 2026-05-22 EXPANDED gating set (current binding).** v0.3.0 tag
lands when ALL of the following hold:

| # | Criterion | Disposition at HEAD `43d3f86c` |
|---|---|---|
| A | W16.2-J PHF-retirement merged. Likely needs ADR-006 §2.7.24 amendment — text drafted at J.1 close + surfaces to supervisor. | **AUDIT-CLOSED 2026-05-22.** Partition: 5 sub-clusters (J.1–J.5); 24–37h. W16.2-J.1 dispatched first-wave 2026-05-22. |
| B | W17.3-4 per-container `FieldType` refinement merged. | **W17.3-4.1 MERGED 2026-05-22 at `4b6d6833`.** Audit-closed 2026-05-22. Partition: 3 sub-clusters (W17.3-4.1–.3); 18–24h total; .1 done (6–8h). **HeapKind::Set supervisor surface RETIRED — empirically stale:** `HeapKind::HashMap` (ord 17) + `HeapKind::HashSet` (ord 21) already exist at HEAD; 4-table lockstep intact. .2 (compiler integration, 7–10h) + .3 (runtime dispatch + snapshot/wire, 5–6h) remain queued. |
| C | Phase-2c host-tier marshal/snapshot rebuild merged (ADR-006 §2.7.4 host-tier). | **AUDIT-CLOSED 2026-05-22.** Partition: 8 sub-clusters (T1 + W17-snapshot-resume + W17-typed-module-exports + W17-foreign-ffi + V3-S5 + C1 + C2 + C3); 70–90h serial / 25–30h parallel. **4 supervisor architectural under-specs surfaced:** snapshot wire format / module-export typed-Arc protocol / NativeScalar carrier design / FFI version-mismatch strategy. Fix-dispatch pending supervisor disposition on those 4. |
| D | Six Known Constraints classified + fixed (or v0.4-deferred per the no-known-incorrectness binding). | **AUDIT-CLOSED 2026-05-22 (team-lead refined).** 4 v0.3-gating sub-clusters: D-α closure-param-inference-loss family (KC #6(b)+(c)bubble_sort+(f)sort; 4–8h audit-first); D-β string-join Bool-receiver dispatch (KC #6(d); 1–3h); D-γ window_over_partition_by hang (KC #6(e) one test; 2–4h); **D-δ array_slice single-arg MERGED `975536d3` 2026-05-22** (root cause: UFCS extend/impl method dispatch padded missing trailing args with `Constant::Unit` instead of compiling default values; fix added `compile_missing_ufcs_default_args` helper + `end: int = -1` sentinel default to `Vec.slice`). v0.4-deferred: KC #1 + #3 + #4 + #6(a) + #6(e-feature-subset) + #6(g). **User decisions 2026-05-22:** KC #5 `object_len_function` → **DROP the test** (TypedObject does NOT implement Len; reflection uses `type_info(T).fields.len()`). KC #2 `format()` shadowing → **RESOLVE BY DELETION** — delete `format_*` prefix globals (`format_pct/format_num/format_string` + any sibling) from `intrinsics.shape` + builtins; KEEP bare `format()` global + KEEP `DateTime.format()` method. No backwards-compat shim; no rename; no UFCS-priority change. User: "we dont need backwards compatibility anyways, and we want it clean." Bare and method-form coexist via syntax distinction. Small workstream — folded alongside criterion F (Len + membership-naming). |
| E | **W18 content-rendering kind-threaded rebuild** (NEW v0.3-gating regression 2026-05-22). Phase 2b ValueWord-removal deleted ~600 LoC dispatch chain (`render_as_content` / `render_as_content_for`); kind-threaded rebuild on `slot_to_wire` / `slot_extract_content` planned but never landed (cite `content_dispatch.rs` lines 3–9 for the deletion scar). Empirical symptoms: enum `Display` leaks internal repr (`print(Color::Blue(42))` → `{__variant: 2, __payload_0: 42}` instead of `Blue(42)`); user types render via degraded fallback printer; c-string styled content verification pending. **6 renderer modules + ContentRenderer trait + RenderContext + adapter dispatch (TERMINAL/HTML/MARKDOWN/JSON/PLAIN) INTACT — gap is the user-value → ContentNode dispatch.** **AUDIT-FIRST mandatory.** ~2–4 sessions. Discipline: kind-threaded only; no ValueWord resurrection in any named guise; CLAUDE.md Forbidden Patterns hold. | **AUDIT-PENDING.** Next audit round dispatches W18 audit-day against the deleted-commit history. |
| F | **Len trait + membership-naming standardization + KC #2 format-deletion** (NEW 2026-05-22; KC #2 folded same day). `trait Len { method len() -> int; method isEmpty() -> bool { self.len() == 0 } }`; implements for String / `Array<T>` / `HashMap<K,V>` / `Set<T>` / Bytes / Deque / bounded Range if shipped. Rename `Set.size() → Set.len()`. Standardize membership: `.includes(x)` across Array/Set/HashMap; `.some(\|x\| pred)` for predicate match; **reject `.contains()` for real** (currently registered for String/Range only; align by repoint Array/Set/HashMap callers to `.includes()`). KC #2 absorbed: delete `format_*` prefix globals (keep bare `format()` + `DateTime.format()`; no backwards-compat). | **PENDING dispatch.** ~1 session. Sequencing: lands alongside W18 audit round (close-coupled territory — both touch printing + method dispatch). |
| G.1 | Doc-truth round Step 1 (DOC-vs-CODE reconciliation): parallel slice agents per book chapter; every documented feature/behavior gets a minimal program; VM + JIT runs under canonical (ii) F' harness; doc-aspirational items reclassified or deferred per (a)/(b)/(c) rule. Eq/Display struct-semantic clarifications fold into Step 1 findings classification per the standard rule. | **GATED on A–F+J close + main HEAD stable.** |
| G.2 | Doc-truth round Step 2 (USER-FOLLOWABLE validation): parallel slice agents on UPDATED docs write simple + complex examples following only book + reference; verify VM == JIT; classify (a)/(b)/(c). | **GATED on Step 1 fully merging on main.** |
| H | All round-6+ acceptance criteria (§0.B / §4 / §5) preserved: smoke matrix s1–s5 5/5 VM == JIT preserved at every close; 0 bad-code merges; no-known-incorrectness gating set empty. | **MET at HEAD; preserve through expanded-scope work.** |
| I | v0.3.0 tag lands ONLY on explicit user authorization after supervisor ratify of close evidence. | — |
| J | **Comptime trait primitive** (NEW 2026-05-22 — user landed YES). `comptime trait Name { ... }` + `comptime impl Trait for Type { ... }` parser + type-checker (methods comptime-only; clean compile error on runtime call) + comptime-evaluator dispatch. **Scope discipline:** JUST the primitive + dispatch + error. NO const-fn markers, NO compile-time generics, NO other comptime feature additions — those v0.4. Refusal #10 family applies if scope creeps. | **PENDING dispatch.** ~1–2 sessions. Audit-first recommended (verify comptime infra maturity for trait-dispatch extension). |

**All user decisions from the 2026-05-22 dialogue have landed.** Standing
disposition table: KC #5 → drop test; KC #2 → resolve by deletion (folded
into F); comptime trait primitive → YES (criterion J); Send/Sync → v0.4
design-question; Len + membership-naming (F); W18 content-rendering (E);
all 4 phase-2c supervisor-architectural rulings RULED + 2 direction-ruled-
with-text-pending. **No further user decisions outstanding from this
dialogue.**

**Sequencing (binding):** code work first (A–F + J in parallel where
territories don't overlap; serialize where they do) → doc-truth Step 1
→ Step 2 → close + tag. W18 audit-day fits the next audit round (E
audit-first mandatory; F + W18 are close-coupled in printing/dispatch
territory; J comptime-trait audit-first recommended).

**Audit-first discipline:** A, B, C, E each get an audit-day. The 6 KCs
(D) get a single discovery-audit that sizes + partitions each. D-α also
gets audit-first (the closure-param-inference-loss family root cause
needs empirical investigation before fix). J comptime-trait gets a
short audit-first (verify comptime infra readiness). Scope-expansion
risk is real for W16.2-J + phase-2c + W18 — audit-first guards it.

**Trajectory:** ~13 ± 4 sessions to v0.3 tag (holds — comptime trait
primitive J added without expanding the band; ~1–2 sessions overlaps
the F + W18 territory bundle).

**§0.B Original Phase 4 acceptance criteria (2026-05-18 — preserved for
archival; subsumed into §0.A above):**

| Criterion | Disposition at HEAD | Evidence / Phase-4 disposition |
|---|---|---|
| Operator coverage full set for user types (arith + compound-assign + comparison + bitwise + Not + Index/IndexMut), VM == JIT smoke-tested | **Substantially MET** | W1.1–W1.11 sub-clusters landed (commits `babea310` Sub through `7e57c02e` Index). Phase 4 verifies via W14 cargo-tarpaulin coverage gate per-operator. |
| LSP feature-spec en-par with rust-analyzer (core features) | **Substantially MET** | W2.1 audit-day + W2.2 / W2.4 / W2.5 / W2.6 / W2.7 / W2.8 / W2.9 / W2.3 closure-waves landed. Cost-class inlay hints (Q25.C.7 / W2.10) gated on Q25.C.6 IC devirtualization — Phase 4 carries this as a documented exception per acceptance-criteria binding. |
| Book 100% correct vs HEAD (zero made-up content; every labeled-runnable snippet runs cleanly VM == JIT) | **IN FLIGHT (W15)** | W3.1 5-slice audit + W3.2-A/B/C/D/E fix-waves landed at v0.3 close-summary. W15 (Phase 4) is the new W3-re-audit + book-truth-gate enforcing 100% correctness at CI. W3.2-A snippet matrix shows VM 21/27 + JIT 24/27 at HEAD; W15.1 re-audit measures full corpus. |
| Test coverage ≥99% per-feature with DOCUMENTED EXCEPTIONS ONLY WHEN VERY HARD TO TEST | **IN FLIGHT (W14)** | cargo-tarpaulin measurement workstream; W14.1 audit-day produces install + invocation pattern + per-crate baseline + per-feature classification. W14.2 parallel fix waves per audit. W14.3 coverage gate at Phase-4b-batch-merge + nightly post-v0.3. |
| W13 differential fuzz operational | **IN FLIGHT (W13)** | New Phase 4 workstream; W13 audit-day scopes cargo-fuzz vs custom + per-domain inventory; closure-waves wire the corpus. |
| `--mode jit` semantics binding + interpreter fall-through + `[jit-fallback]` diagnostic | **MET** | W12 close (`4083eb55`); structural close per `docs/cluster-audits/v0.3-w12-jit-mode-semantics-close.md`. |
| v2-raw residuals resolved (`length_typed_object_empty` + `w17_comptime_*` SIGABRTs) | **MET** | W5 close (`docs/cluster-audits/v0.3-w5-v2-raw-residuals.md`); commit `35b948ed`. |
| Phase 4 imprecision 83 root cause fix (return-typeless prelude trait method) | **MET** | W6 close; commit `ac885519`. |
| Smoke matrix 5/5 VM == JIT preserved at every close | **MET (corrected-harness re-verified at HEAD)** | See empirical matrix below. Phase 4 carries 5/5 preservation as merge-ceremony gate. |
| 0 bad-code merges into main preserved | **MET (corrected harness)** | See §4 discipline framing. Phase 4 preserves with W14 coverage gate at batch merges. |

Empirical smoke matrix at HEAD `4ffde515` (post round-6+ close + expanded-
scope refresh) under (ii) F' corrected canonical harness (binding
2026-05-18; supersedes prior F binding empirically falsified per
supervisor #109 acknowledgment):

```bash
out=$(timeout 30 ./target/release/shape run --mode $mode $file 2>&1)
ec=$?
last=$(echo "$out" | tail -1)
```

Rationale: prior `out=$(... | tail -1); ec=${PIPESTATUS[0]}` shape is broken
in bash — `$(...)` is assignment-in-subshell, not parent-shell pipeline;
PIPESTATUS captures nothing → ec silently defaults to 0 (masking ec=1
surfaces from upstream errors). The (ii) shape decomposes operations so
`ec=$?` captures the cmd's exit code directly.

| Smoke | VM last-line (ec) | JIT last-line (ec) | VM == JIT |
|---|---|---|---|
| s1 scalar-loop `let mut sum = 0; for i in 0..100 { sum += i }; print(sum)` | `4950` (0) | `4950` (0) | YES |
| s2 typed-array `xs.map(\|x\|x*2).sum()` | `30` (0) | `30` (0) | YES |
| s3 UFCS-dispatch user-type | `x` (0) | `x` (0) | YES |
| s4 Set basics | `2` (0) | `2` (0) | YES (R3-3 fixture restoration; /tmp/smokes/s4.shape canonical form) |
| s5 `dyn T` trait-object | `x` (0) | `x` (0) | YES — W16.2-A bare-struct-literal inference closes it |

**5/5 VM == JIT at HEAD `4ffde515`** (re-verified at 2026-05-22 refresh).
s5 closed via W16.2-A's TypedObject carrier subsuming `Array<dyn HasX>`
literals; W16.2-C empty-literal/spread/comprehension construction landed
in WS-1.

Sample W3.2-A 27-snippet runnability (corrected harness; tracks W11-fup-C close
matrix):

```
VM: 21/27; JIT: 24/27
```

Matches the W11-fup-C close doc's full-matrix claim (post-fix snapshot at
`672bda24`); pre-existing surfaces (V3-S5 ckpt-5/ckpt-6 op_new_array,
MakeRange open-range, W17 obj_spread, ARITY default-value) are deferred to
their own follow-up sub-clusters.

---

## §1 Cluster-by-cluster (Phase 3a-i → Phase 3d)

The v0.3 trajectory was dispatched across four parallel rounds, with each
round landing 5-12 sub-clusters under bulldozer cadence and territory
non-overlap. Cluster phases below mirror the merge-commit chronology
(`git log --first-parent v0.2.0..HEAD`).

### Phase 3a-i — Operator coverage closure-wave (W1.1–W1.6 + W3.2-A)

11 user-type operator-trait sub-clusters landed across 3 parallel rounds
(territory non-overlap by stdlib file + per-operator dispatch arm), plus
the W3.2-A slice-A language reference book fix-wave that consumed the
W3.1-slice-A audit findings:

| Sub-cluster | Operator(s) | Merge commit |
|---|---|---|
| W1.1 | Sub + SubAssign | `babea310` |
| W1.2 | Mul + MulAssign | `f8b4017b` |
| W1.3 | Div + DivAssign | `cf18efc0` |
| W1.4 | Mod + ModAssign | `306698c2` |
| W1.5 | Neg | `e381481c` |
| W1.6 | Not | `8dd05b28` |
| W3.2-A | Language reference slice-A fix-wave | `c96fd290` |

Each sub-cluster mirrors the Phase 4 Add/AddAssign pattern: trait
declaration in `crates/shape-runtime/stdlib-src/core/`, prelude import,
UFCS dispatch arm, smoke fixture VM == JIT verified.

### Phase 3a-ii — LSP closure-waves (W2.2 / W2.4 / W2.5 / W2.6 / W2.8 / W2.9)

After W2.1 audit-day (`d7ba5a81`, audit doc at
`docs/cluster-audits/v0.3-lsp-feature-spec.md`) ratified the
per-capability partition, 6 LSP closure-waves landed in parallel across 2
rounds. Each consumed disjoint audit rows so territory non-overlap held.

| Sub-cluster | Capability family | Merge commit |
|---|---|---|
| W2.2 | Completions polish | `8bfac43e` |
| W2.4 | Inlay-hints depth | `f7d2da5d` |
| W2.5 | Definition family extras | `3a8ab636` |
| W2.6 | References + rename cross-file | `a2b04a26` |
| W2.8 | Semantic tokens delivery | `566ca028` |
| W2.9 | Hover + signature polish | `a48ee02f` |

### Phase 3b — Operator-coverage closure (W1.7–W1.11) + cross-module + JIT-call-method (W7 + W9 + W10)

The remaining 5 operator sub-clusters + 3 architectural fixes landed in
parallel:

| Sub-cluster | Substance | Merge commit |
|---|---|---|
| W1.7 | Eq (==, !=) for user types | `6990b1de` |
| W1.8 | Ord / PartialOrd (<, <=, >, >=) | `49201e6e` |
| W1.9 | BitAnd + BitOr + BitXor + assign variants | `9b4fd88a` |
| W1.10 | Shl + Shr + assign variants | `a2b04a26` (per merge log: `a2041dd8`) |
| W1.11 | Index + IndexMut | `7e57c02e` |
| W7-impl | `TypeInfo` struct comptime-typed return implementation (`type_info` re-introduced per W7 audit Q1-Q5 user dispositions) | `a85a45b8` |
| W9 | Annotation imports fix path (i) — cross-module annotation imports work end-to-end | `f691709a` |
| W10 | JIT call-method user-trait fix — closes the v0.2.0 §1.4 caveat that JIT-tier user-type operator overloading was incomplete | `774b1712` |

W7 audit-day (`docs/cluster-audits/v0.3-w7-type_info-comptime-typed-return.md`,
commit `949ed5a7`) ratified Option (b) TypeInfo struct return per ADR-006
§2.7.5 stamp-at-compile-time + the `build_config` precedent. Implementation
landed without ADR amendment — uses existing TypedObject machinery.

### Phase 3c — LSP workspace + JIT-Aggregate (W2.7 + W11 + W3.2-C)

| Sub-cluster | Substance | Merge commit |
|---|---|---|
| W2.7 | LSP workspace integration (workspace/symbol eager indexing + didChangeWatchedFiles + executeCommand + willRenameFiles) | `343e84a4` |
| W11 | JIT MIR Rvalue::Aggregate kind-blind fallback partial close (3/27 → 12/27 JIT snippets); 5 follow-up classes surfaced | `9efa6a3f` |
| W3.2-C | Book slice-C systematic rewrite (tutorial + getting-started + examples; 7 shape-web per-territory commits) | `a8853958` |

W11 close (`docs/cluster-audits/v0.3-w11-jit-new-array-close.md`) is a
partial-surface close — landed narrow producer-side fixes for the Range
+ Array-spread Aggregate kind-blind sites + extended
`infer_array_elem_from_operands` with a copy-chain pre-pass. Five
follow-up classes (W11-fup-A binop coverage, W11-fup-B
comprehension-kindtracker, W11-fup-C print-typed-array, plus
W11-followup-slice-classify and W11-followup-unop-bitnot) deferred to
Phase 3d / v0.4.

W3.2-C close (`docs/cluster-audits/v0.3-w3-2-c-close.md`) is shape-web
book content only (NO source changes in shape/ workspace); raised slice-C
runnability 25% → 92% VM.

### Phase 3d — Final closure-wave (W12 + W11-fup-A + W11-fup-B + W2.3 + W11-fup-C)

| Sub-cluster | Substance | Merge commit |
|---|---|---|
| W11-fup-A | MIR BinOp Pow / BitAnd / BitOr / BitXor / Shl / Shr + JIT consumers; surfaces UnOp::BitNot + InstanceOf as new tracked classes | `85c38998` |
| W11-fup-B | Comprehension iteration-var kind threading for closed-Range iterables (narrow charter) | `7f6d34d1` |
| W2.3 | LSP diagnostics family (audit rows 1.17 codeDescription URLs + 1.19 UNNECESSARY tag + 1.40 quickfix code-keyed rebuild + 1.73-74 pull diagnostics) | `bc8685dc` |
| W12 | `--mode jit` semantics binding + interpreter fall-through + `[jit-fallback]` diagnostic emission | `4083eb55` |
| W2.3 post-merge | Strip "compatibility layer" rationalization rename in `tools/shape-lsp/src/code_actions.rs` | `c0ed6ef9` |
| W11-fup-C | `jit_print_typed_array` FFI body + JIT-allocator element-type stamping; closes Class C cascade end-to-end | `672bda24` |

W2.3 post-merge fix (`c0ed6ef9`) caught a rejected-rename rationalization
("compatibility layer") at post-merge audit, stripped per CLAUDE.md
§Forbidden Rationalizations.

W12 close (`docs/cluster-audits/v0.3-w12-jit-mode-semantics-close.md`)
binds `--mode jit` semantics: toplevel + per-function attempt
JIT-compile, on failure fall through to interpreter with structured
`[jit-fallback]` diagnostic (info-level + `tracing::info!` mirror at
`shape_jit::fallback`), tier-up to optimizing JIT preserved on hot
functions per T1@100 / T2@10k thresholds.

W11-fup-C close (`docs/cluster-audits/v0.3-w11-fup-c-print-typed-array-close.md`)
wires the `jit_print_typed_array` FFI body delegating to the canonical
`ValueFormatter::format_kinded` per ADR-006 §2.7.5; fixes JIT-side
allocator element-type stamping at HeapHeader `_pad` offset 7 (6 allocators
across `jit_v2_array_new_<f64,i64,i32,bool,string,decimal>`). Surfaces a
NEW class `W11-followup-jit-spread-expand` via class-shift discipline.

### Audit-only sub-clusters (no source changes)

| Sub-cluster | Deliverable | Merge commit |
|---|---|---|
| W3.1 slice A | Language reference book audit (5 parallel slice agents) | `5d399c2e` |
| W3.1 slice B | Stdlib reference book audit | `2daecf8e` |
| W3.1 slice C | Tutorial + getting-started book audit | `a3618224` |
| W3.1 slice D | AI-native + comptime + polyglot book audit | `1f90b1d8` |
| W3.1 slice E | JIT + perf + sandboxing book audit | `8e46053f` |
| W2.1 | LSP feature-spec audit-day | `d7ba5a81` |
| W7-audit | `type_info` comptime-typed-return audit-day | `949ed5a7` |
| W6 | Phase 4 imprecision 83 root cause resolution | `ac885519` |
| W5 | v2-raw residuals consolidated empirical isolation + fix | `35b948ed` |
| W8 | `interface` keyword + AST/enum-variant naming wholesale strip (60 files / +217 -738 / 192 identifier sites) | `5d8cd8ac` |
| W3.2-B | Book slice-B stdlib fix-wave (shape-web) | `da70d7ee` |
| W3.2-D | Book slice-D comptime+polyglot fix-wave (shape-web) | `63148089` |
| W3.2-E | Book slice-E JIT+perf+sandboxing fix-wave (shape-web) | `1b9d54cb` |

---

## §2 ADR amendments shipped in v0.3

The Value & Memory Model spec (`docs/adr/006-value-and-memory-model.md`)
received **no new §2.7.X amendments** during v0.3. Per
`git log v0.2.0..HEAD -- docs/adr/006-value-and-memory-model.md`, the
single touch was commit `7c1ea6c3` — a strip of dead `@ai`-positioning
material (project-design item #10 CT-AION, §11 CT-AION body, Phase 6
migration row, Q3 `@ai × var` ordering decision, W17-snapshot-callback-invoker
descriptor "for @ai-annotation callbacks" → "for annotation callbacks").
No structural amendment; no new §, no new Q.

Every sub-cluster close doc that touched ADR-006 invariants
(W5 / W7 / W10 / W11 / W11-fup-A/B/C / W12) explicitly declined ADR
amendment because the fixes operate within existing §2.3 / §2.7.5 /
§2.7.7 / §2.7.8 / §2.7.10 / §2.7.11 contracts. The W7 audit recorded a
draft §2.7.27 ("Comptime-builtin TypedObject return carrier") for future
canonicalization once a 3rd comptime-builtin-typed-return site lands;
deferred per Refuse #10 anti-deferral discipline (defer-the-meta-amendment,
not the implementation).

---

## §3 CLAUDE.md modifications shipped in v0.3

Three modifications landed during v0.3 trajectory
(`git log v0.2.0..HEAD -- CLAUDE.md`):

1. **`e756311f` (2026-05-17)** — strip deleted-dispatch-vocab +
   strip `@ai` project-positioning per user authorization. Changes:
   - Project Overview rewritten: "AI-native, statically-typed
     programming language … AI-first annotations (`@ai` for typed LLM
     output)" → "statically-typed programming language … ergonomic
     annotations + comptime (enabling user-defined LLM integration
     patterns in stdlib/userland)".
   - shape-value crate description updated: "`ValueWord` in
     `value_word.rs`" → "typed-slot carriers (NativeKind / KindedSlot
     per ADR-006 §2.3)".
   - `int` width corrected: `i48` → `i64` (vestige from pre-NaN-box-removal
     description).
   - "AI annotations: `@ai fn ...`" bullet stripped from Language Features.
   - v2 Runtime banner: "raw native values for proven types and
     `ValueWord` (8-byte tagged word) as the dynamic fallback" → "raw
     `f64`/`i64`/`i32`/`i8`/`bool`/`*const T` in 8-byte stack slots;
     opcodes encode type via per-slot `NativeKind` metadata".
   - VM Interpreter description: "typed 8-byte slots (raw native values
     for proven types, `ValueWord` dynamic fallback)" → "typed 8-byte
     slots driven by per-slot `NativeKind` metadata (parallel `Vec<u64>`
     data + `Vec<NativeKind>` kinds per ADR-006 §2.7.7)".
   - Value Representation `ValueWord` bullet (2,650 lines descriptor) +
     `SlotKind` bullet replaced with typed-slot + `NativeKind` bullets.
   - "Annotation imports: not modeled as named exports/imports" Known
     Constraint stripped (resolved by W9 cross-module annotation
     imports fix).

2. **`d2c511a3` (2026-05-17)** — strip 3 stale ValueWord/Upvalue refs
   (ASK 6) per user authorization:
   - `Upvalue::Immutable(ValueWord)` performance-feature bullet
     stripped.
   - Two additional ValueWord descriptor lines stripped (residual after
     `e756311f`).

3. **`5c42790f` (2026-05-17)** — Known Constraints v2-raw-heap-audit
   PARTIALLY RESOLVED 2026-05-17 update (the W5 close cascade — entry
   text replaced to reflect the two SIGABRT residual classes RESOLVED;
   text preserved here at `CLAUDE.md` lines 332-333).

No Forbidden Patterns section extensions landed during v0.3. The
broader-family rename-refuse regex + the §Parallel-implementation entry
+ the §Renames to refuse on sight list are all v0.2.0-shipped and
continued to apply unchanged.

---

## §4 Cumulative discipline statistics (HONEST framing per 2026-05-17 + 2026-05-18 bindings)

### Imprecisions

Cumulative count at HEAD: **~130+** across the full v0.3 trajectory, all
classified pre-merge per the no-known-incorrectness binding (user 2026-05-20).

Anchor citations (pre-round-6 trajectory):
- W2.7 close §7 (cumulative 92 → 92; 0 new).
- W2.3 close §6 (none surfaced at spec-vs-execution boundary).
- W7 audit §7.1 (candidate imprecision 87 — supervisor-prompt-imprecision
  refutation-of-correct-finding subtype).
- W12 close §11 (cumulative 92 → 92; 0 new; one `print(42)` supervisor
  reproducer noted as refinement, not imprecision).
- W11-fup-C close §6 (cumulative 96 → 97; 1 new — class
  "dispatch-empirical-close-gate-conflated-distinct-cause-families-at-HEAD-drift").

Round-6+ delta (~30-40 new imprecisions, all classified at close):
- 3 ground-truth misses (the phase-2c-muddle framing in the original
  no-known-incorrectness re-audit; the W16.2-J "~125 sites" framing the
  WS-8 audit overstated; the Decision-2 minimal-repro abbreviation —
  supervisor 2026-05-22).
- Dispatch-hygiene lesson (the `Agent isolation: worktree` stale-base +
  agents `cd` into the main repo failure mode at round-6 dispatch).
- The (B)-rationalization framing (offering "annotate your params" as a
  v0.3 language-line option — caught as a "document as out-of-scope"
  rationalization per CLAUDE.md §Forbidden Rationalizations).
- Audit-overstatement class (the WS-1 "generic-function" label for the
  sim-test cluster, corrected by WS-9 to non-generic unannotated-fn
  inference — three sub-clusters not one).
- Run-verify binding extension (every repro in a surfacing must run-verify
  at HEAD before relay — same standing as the file:line / commit-hash
  grep-verify rule).
- Per-close-report imprecision tallies in the round-6+ workstream close
  reports (each fix close noted ≥1 imprecision worth flagging).

Per close-doc tally drift across rounds reflects per-imprecision instance
classification under user disposition. Full canonical reconciliation
(every imprecision named + classified) is owed at v0.4 close-summary depth
per the prior binding.

### Bad-code merges

**0 per smoke-gate AS REPORTED + AS RE-VERIFIED with release-binary
corrected harness.**

Discipline Binding 2026-05-17 + 2026-05-18 framing (verbatim per
supervisor 2026-05-18):

- The 2026-05-17 debug-binary execution divergence (W12 §1.3
  `print(42)` reproducer + supervisor bisection report) surfaced a
  smoke-gate harness defect: pipeline ordering `2>/dev/null | tail -1`
  drops stderr error output + `tail | echo EXIT=$?` captures `tail`'s
  exit code masking shape's exit=1 signal. The W12 close §1.1 root-cause
  analysis bisected this; the W12 fall-through fix (`[jit-fallback]`
  diagnostic + fall-through to BytecodeExecutor::execute_program) lands
  the architectural correction.
- 2026-05-18 release-binary re-verification confirms smoke matrix 5/5
  VM == JIT EMPIRICALLY at HEAD `672bda24`. The binding text was always
  correct (smoke matrix preserved + 0 bad-code merges); the harness was
  the defective layer, and W12 lands the architectural fix.

### Smoke 5/5 VM == JIT empirical confirmation at HEAD

| Smoke | VM | JIT | Notes |
|---|---|---|---|
| s1 | 4950 (ec=0) | 4950 (ec=0) | scalar-loop preserved |
| s2 | 30 (ec=0) | 30 (ec=0) | typed-array `xs.map(\|x\|x*2).sum()` preserved |
| s3 | x (ec=0) | x (ec=0) | UFCS dispatch preserved |
| s4 | 2 (ec=0) | 2 (ec=0) | Set basics preserved |
| s5 | x (ec=0) | x (ec=0) | `dyn T` trait-object — closed via W16.2-A struct-literal inference + W16.2-C in WS-1 |

### Round-6+ workstreams closed (15)

Per the user 2026-05-20 no-known-incorrectness binding ("ANY incorrect and
known behaviour needs to be fixed before 0.3"). Each merged + verified
with smoke 5/5 + 0 regressions vs the round-6+ baseline:

- **WS-1** W16.2-C `op_new_array` empty-literal / spread / comprehension construction (kind-generic per audit 6.B.1 option (b)).
- **WS-1b** bare-empty-array accumulator residual (`let mut out = []` + downstream-push element-kind resolution).
- **WS-2** ζ-round: loop-body assignment inference (transitive callsite for unannotated nested fns) + `id(None)` hang (clean compile error for unresolvable generic args).
- **WS-3** Result / AnyError machinery — F1 JIT typed-array OOB raise, F2a `?` type-loss, F2b `?` `heap_to_wire` type-confusion SIGSEGV, F3 `!!` dispatch, F4 jargon-dump replaced with `Uncaught error:` clean message, array-rest clean compile error, `type_check_kinded` `"array"` arm.
- **WS-4** Object destructuring 4a/4b/4c — `type_check_kinded` `"object"` arm, schema field-type propagation, `match` struct-pattern grammar + classification.
- **WS-6** Generic free-fn args accept non-scalar arguments (struct / enum / Option / Result / HashMap; `concrete_type_for_expr` arms + `NamedTypeId`).
- **WS-6b** Generic-arg follow-up (inferred-type let-bindings + HashMap-result type tracking).
- **WS-7** JIT array-param SIGSEGV (inferred pass-by-reference optimization disabled — uniformly by-value; legacy `inline_array_get`/`inline_array_set` OOB now raises).
- **WS-8** Consumer-cascade kind-generic bundle — `.len()`/`.isEmpty()`/`.last()` header handler, decimal-element read, string `.includes()` wrong-false, string map-result binding, bool `.reverse()` + bool/string `.indexOf()` routing-to-generic.
- **WS-9** Index-access-into-unannotated-param inference + silent-wrong-result (`a[0]/a[1]` int → 3 instead of fabricated 3.5).
- **WS-9b** Property-access-into-unannotated-param inference (named-fn param-projection + struct-schema prepass + `binary_ops` PropertyAccess hint).
- **WS-9c** Anonymous-object-factory inference loss — `apply_callsite_unions` publishes `resolved` into the unifier + `Type::Concrete` recursion + `tyvar` marker for the TypeAnnotation-no-variable-variant gap.
- **WS-10b** Bytecode-verifier stale-rule fix (stops printing "Bytecode verification failed" on every program run) + 29-failure individual classification audit.
- **WS-11** REPL cross-cell binding persistence (`let` / `type` / `fn` defs persist across input lines, VM + JIT).
- **WS-12** Option `as`-cast Some-path lift (`UnwrapOption` before `ConvertTo*` + JIT preflight gating + primitive-cast result-kind tracking).

### W12 fall-through value-adding for genuine JIT Err paths

The W12 architectural fall-through is value-adding for genuine JIT
compile-failure surfaces:

- **JIT preflight rejection** (`SharedModuleBinding` opcodes
  `AllocSharedModuleBinding` / `LoadSharedModuleBinding` /
  `StoreSharedModuleBinding` per
  `crates/shape-jit/src/compiler/accessors.rs:605-612` — narrow
  three-opcode class) now falls through to interpreter execution with
  structured diagnostic emission instead of `Error: ... exit(1)` +
  stderr-dropped-under-`2>/dev/null` silent-no-output surface.
- **Kind-source gaps** at the JIT FFI return-tag boundary
  (`RETURN_TAG_NANBOXED` reaching `executor.rs:432-470`, W10-class
  territory) similarly fall through honestly to interpreter rather than
  hard-fail.

### Defection-attractor refusal — one rename caught + stripped at post-merge

The "compatibility layer" rationalization rename — caught at post-merge
in W2.3 territory `tools/shape-lsp/src/code_actions.rs` and stripped at
`c0ed6ef9` per CLAUDE.md §Forbidden Rationalizations. The
broader-family rename-refuse regex at `scripts/check-no-dynamic.sh`
catches "compatibility layer" via the post-strict-typing forbidden
phrase list; the rename was rewritten to "fallback retained only for
codeless / pre-structured-error diagnostics" (purpose-named, not
metaphorical descriptor).

The W11-fup-C close §5 also caught + rewrote a "tag-decode probe"
descriptor in a NOT-comment via the same regex; final diff zero
broader-family hits.

---

## §5 v0.4 candidate territory inventory

Exhaustive list of deferred territory beyond v0.3 close (after Phase 4
closes; not v0.3-gating). Entries previously in v0.3-only candidate
shape that moved into v0.3 Phase 4 scope per user 2026-05-18 acceptance
revision are NOT listed here — see `docs/v0.3-roadmap.md` §Phase-4
expansion (specifically W14 test coverage absorbs the prior "~48
pre-existing shape-test failures" entry; W15 book truth absorbs the
W3.2-residual snippet matrix work).

Each remaining item is a v0.4-candidate sub-cluster; not yet dispatched.

### §5.1 Phase 3e residual JIT coverage classes (9 named follow-ups)

From W11-fup-A close §4 + W11-fup-B close §4 + W11-fup-C close §4 + W12 close §4:

1. ~~**`W11-followup-open-range-iterator`**~~ **RETIRED 2026-05-19** —
   empirically falsified at HEAD `aa79854f` per R5a W14.2-E close
   `fa96ac42`: simple-reproducer `for x in arr[1..] { ... }` PASSES
   VM==JIT byte-equal via downstream W13/W14/W16.2-A close-cascade
   (audit-framing-falsified-at-HEAD imprecision class — positive
   convergence signal). Original scope: medium open-range iterator
   semantic + VM bytecode dispatch.
2. ~~**`W11-followup-slice-classify`**~~ **RETIRED 2026-05-18** —
   silently closed by Round 2 wave (suspected W11-fup-neg-int64 a5b1f123
   or LANG-9 close 0ccaea7b); empirically verified at R3-7 W13.4 CI
   close (seed c10 NOW Convergent at HEAD `494a3c44`, was divergent
   at W13.3 close b8ef6f75). Original scope: small-medium SliceStore
   MIR walker + JIT jit_slice FFI body.
3. ~~**`W11-followup-unop-bitnot`**~~ **CLOSED 2026-05-19** — landed
   in Phase 4b W14.2-A1 batch 1 (close `43067ab2`) as UnOp::BitNot
   enum extension at `crates/shape-vm/src/mir/lowering/helpers.rs:466`
   + `crates/shape-jit/src/mir_compiler/{types.rs:1221,rvalues.rs:1126}`;
   W14.2-E close `fa96ac42` re-confirms VM==JIT byte-equal at HEAD.
4. ~~**`W11-followup-instanceof-classify`**~~ **RETIRED 2026-05-19** —
   empirically falsified at HEAD `aa79854f` per R5a W14.2-E close
   `fa96ac42`: `x is Type` simple-reproducer PASSES VM==JIT byte-equal
   via downstream close-cascade. Original scope: small JIT-only
   Aggregate fallback for `Expr::InstanceOf`.
5. ~~**`W11-followup-jit-unary-neg-int64`**~~ **CLOSED 2026-05-18** at
   commit `a5b1f123` (Phase 4b Round 2 W11-followup-jit-unary-neg-int64
   close — compile_unop::Neg native-width dispatch F64→fneg / I64/I32→ineg;
   reproducer `let a=10; print(-a)` VM=JIT=-10).
6. ~~**`W12-followup-jit-cond-int-negation`**~~ **RETIRED 2026-05-18** —
   silently closed by Round 2 wave; empirically verified at R3-7 W13.4
   CI close (seed a10 NOW Convergent at HEAD `494a3c44`, was divergent
   at W13.3 close b8ef6f75). Original scope: small-medium VM `-250` vs
   JIT `124750` if/else int-return semantic divergence on tier-up.
7. **`W10-followup-named-args-default-value`** — JIT verifier
   "mismatched argument count" on default-value parameters per W11
   close §4 Class E (`fn_named_args.shape`). Scope: small (JIT
   named-arg lowering for default-value params).
8. **`W17-narrow-follow-up-spread`** — JIT obj-spread SURFACE
   `ObjectStore: schema_id not threaded from producer` per W11 close
   §4 Class D (`obj_spread_min.shape`). **Partial retirement** at HEAD
   `aa79854f` per R5a W14.2-E close `fa96ac42`: simple direct-field-
   access cases (`{...base, z: 3}.z`) PASS VM==JIT byte-equal at HEAD;
   full round-trip in `print(<spread-result>)` context still SURFACEs
   per W17-objstore class. Remaining scope: medium (full `print()`
   round-trip schema_id threading for spread-result objects).
9. **`W11-followup-comprehension-iterable-element-kind`** — NEW from
   W11-fup-B close §4: iteration-var kind threading for non-Range
   iterables (array-literal, function-call, variable-bound,
   generic-Iterable). Two architectural options (plumbing
   `ConcreteType` element through MIR vs JIT kind-tracker extension).
   Scope: large.
10. **`W11-followup-jit-spread-expand`** — NEW from W11-fup-C close §4:
    JIT-side `[...a]` spread codegen bug pushes inner array's raw
    pointer as single element instead of expanding per-element
    (`crates/shape-vm/src/mir/lowering/expr.rs:237-247` + JIT Aggregate
    consumer). Scope: medium (MIR Spread Rvalue + per-element JIT push
    iteration via `jit_v2_array_push`).

### §5.2 Kinded `jit_print` family completion (cluster-2 §E.5)

**22 / 35 remaining per-HeapKind kinded `jit_print` arms** per CLAUDE.md
v1 candidates §7.2 + v0.2.0-release-notes §5.4 inventory. Cluster-2
landed 13 / 35 (Numeric/Temporal partial); 22 remain across 8 sub-family
groupings (Numeric/Temporal residuals, DataTable/Content, Native-foreign,
Pure-discriminator, Async, Matrix, TraitObject/Closure/TypedObject,
ModuleFn). W11-fup-C added `TypedArray` to the wired set (extends
coverage by 1 — now 14/35). Scope: 22 follow-up sub-clusters per
sub-family bulldozer-wave; small per-arm.

### §5.3 Q25.C.4 `#[static_only]` resolve UNCOVERED

Per v0.2.0 release notes §5.2 + CLAUDE.md v1 candidates: not parsed at
HEAD; ETO-002 error unreachable. Binary: implement OR remove from ADR.
Scope: small (~50-100 LoC parser/semantic dispatch).

### §5.4 Q25.C.6 IC devirtualization (perf for dyn dispatch; large)

Per v0.3 roadmap §W4 + cluster-1.5 Q25.C close: dyn-dispatch fast-path
via inline-cache devirtualization. Not dispatched in v0.3. Scope: large
(~500-1500 LoC; multi-session chain candidate per D-α).

### §5.5 Q25.C.7 LSP cost-class inlay hints (depends on Q25.C.6)

Per v0.3 roadmap §W2 audit row 2.10: LSP cost-class inlay hints (Direct
/ Generic / IC). Depends on Q25.C.6 IC devirtualization. Scope:
small-medium.

### §5.6 AGENTS.md V3-S6 chain rows crate-prefix annotation

Per v0.2.0 release notes §7.7 + CLAUDE.md v1 candidates: cumulative
30+ rows backfill across cluster-2 + cluster-1.5 + v0.3 trajectory.
Scope: bulldozer-wave (doc discipline).

### §5.7 W2.10 LSP cost-class inlay hints (Round 3c)

Per v0.3 LSP audit (`docs/cluster-audits/v0.3-lsp-feature-spec.md`):
Round 3c per audit. Depends on v0.3 W4 IC devirtualization; never
started. Distinct from §5.5 (which is the ADR-side Q25.C.7 framing);
W2.10 is the LSP-side audit row consuming that work.

### §5.8 V3-S5 ckpt-5/ckpt-6 STRICT close residuals — RETIRED in v0.3

Closed by round-6+ WS-1 (W16.2-C empty-literal/spread/comprehension) +
WS-1b (bare-empty-array accumulator) + WS-8 (kind-generic consumer
bundle). `op_new_array(0)` SURFACEs gone for `arr_spread{,_min}` /
`arr_comp{,2}` / `let mut out = []` shapes; `arr.len()/.isEmpty()/.last()`
kind-generic header handler covers every element kind.

### §5.9 W16.2-J PHF-retirement workstream — FOLDED INTO v0.3 (2026-05-22)

**MOVED to §0.A criterion A.** Per user 2026-05-22 expanded-scope: retire
the per-kind `TYPED_<KIND>_ARRAY_METHODS` PHF registries (`typed_int_array_
methods.rs` + `typed_number_array_methods.rs`, ~667 LoC) into the generic
monomorphized layer at `stdlib-src/core/vec.shape`; delete the legacy
`ckpt[2-5]_surface` cascade tombstones (~125 calls across 13 files in
`executor/objects/`); macro-generate the 56 primitive opcode arms (14
TypedArrayKind variants × 4 ops); refactor the per-kind JIT typed-array
FFI. Likely needs an ADR amendment — surfaces to supervisor when text
drafted. Audit-day pending.

### §5.10 W16.2-B scalar-element `dyn Trait` arrays

`Array<dyn Speak> = [1, 2]` with `impl Speak for int` — the array stores
raw `Int64`; `arr[0].s()` fails "no method 's' on Int64". Per-element
`BoxTraitObject` boxing needed. Edge-case feature gap, errors VM==JIT;
classified v0.4 by supervisor 2026-05-22. Optional v0.3-close polish
(clean compile-reject instead of runtime "no method") if it falls out
cheaply during another workstream.

### §5.11 Native JIT decimal-scalar codegen

WS-8 [2] closed decimal-array element reads via a `MirConstant::Decimal`
carrier + JIT surface-and-stop fall-through to interpreter. Native JIT
decimal-scalar codegen (`1.5D` arithmetic on the JIT-compiled path
without falling back) remains v0.4. Tracked alongside §5.9.

### §5.12 Round-6+ polish items

- REPL declaration-statement `false` print — `let a = 42` / `type P { ... }`
  / `fn foo() { ... }` print `false` as their statement result; the
  binding works (cross-cell persistence is correct, WS-11), the print is
  a UX nit. v0.4 polish.
- WS-9c `tyvar` inference-layer string marker — `TYVAR_ANNOTATION_PREFIX
  = "\u{1}tyvar:"` encodes a `Type::Variable` in a `TypeAnnotation` slot
  because `TypeAnnotation` has no variable variant. The marker carries
  a real var resolved by the unifier (never fabricated). Promote to a
  proper `TypeAnnotation::Variable` variant in v0.4 to retire the
  marker representation.
- Prior supervisor v0.4 inventory items (carried forward; subset MOVED
  to v0.3 §0.A per 2026-05-22 expansion): W15.2-K stdlib doc pages,
  §4.D.10 emission-rename, shape-web-per-agent-worktree-infra, annotated-
  closure-param parse + `sortBy` closure defect.
  **MOVED to v0.3 §0.A:** W17.3-4 per-container `FieldType` (criterion B);
  phase-2c host-tier marshal/snapshot rebuild (criterion C); W18
  content-rendering kind-threaded rebuild (criterion E); Len trait +
  membership-naming standardization (criterion F).

### §5.13 Send/Sync equivalent — CONFIRMED v0.4 (2026-05-22)

**User annotation 2026-05-22:** "Rust way too complicated; needs deep
design thought." v0.4 planning treats this as a design-question
workstream, NOT a feature-gap. The concurrency-marker shape (auto-trait
vs explicit annotation vs structural inference; relationship to existing
`Mutex`/`Atomic`/`Lazy` HeapKinds; interaction with cross-task share
semantics) needs language-design exploration before implementation
scope can be sized.

---

*v0.3 is the first user-facing-polish step after the v0.2.0 architectural
rework. Round-6+ closed all the original Phase-4 + no-known-incorrectness
gating set. The 2026-05-22 expanded scope adds six v0.3-gating
workstreams + criteria: W16.2-J PHF-retirement (A), W17.3-4 per-container
FieldType (B), phase-2c host-tier marshal/snapshot rebuild (C), six
Known Constraints (D), W18 content-rendering kind-threaded rebuild (E),
Len trait + membership-naming standardization (F) — plus the 2-step
doc-truth + book-driven testing round (G.1/G.2). Trajectory: ~13 ± 4
sessions to tag. v0.3.0 tag annotation lands when §0.A criteria
empirically met + supervisor ratifies + explicit user authorization.
Outstanding v0.4 territory: JIT polish (Phase 3e residual classes +
22 kinded jit_print arms + IC devirtualization), LSP cost-class inlay
hints, Send/Sync design-question; see §5.*
