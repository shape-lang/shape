// Benchmark 2: Iterative Fibonacci
function fib_iter(n: int) -> int {
    var a = 0;
    var b = 1;
    var i = 0;
    while i < n {
        let t = a + b;
        a = b;
        b = t;
        i = i + 1;
    }
    return a;
}
print(fib_iter(100000000));
